openapi: 3.0.3
info:
  title: GRAND-ERP API
  version: 0.9.0
  description: >
    Backend kontrak API untuk GRAND-ERP. Setiap perubahan route, request, atau
    response di internal/delivery dan internal/model harus tercermin di sini
    dalam commit yang sama.


    Data master tidak punya endpoint DELETE. Keempat tabel (satuan, supplier,
    pelanggan, ekspedisi) dirujuk tabel transaksi, jadi menghapus baris yang
    sudah dipakai akan gagal karena foreign key atau memutus jejak audit.
    Penonaktifan dilakukan lewat `PATCH` dengan `is_aktif: false`. Hal yang sama
    berlaku untuk `role` dan `user`.


    Satu user boleh punya banyak grant. Sejak isu #12 fase 3, satu grant adalah
    pasangan (role, unit_kerja): `id_unit_kerja` kosong berarti role itu berlaku
    di seluruh unit, diisi berarti hanya di unit tersebut, dan role yang sama
    boleh dipegang di lebih dari satu unit sebagai grant terpisah. Pemberian dan
    pencabutan grant dilakukan lewat `grants` di `POST`/`PATCH /api/v1/user`,
    bukan sub-resource tersendiri, supaya baris user dan pemberian grant-nya
    berada dalam satu transaksi.


    Sejak isu #12 fase 4, sebuah **sesi** (token) mengotorisasi sebagai **satu**
    grant aktif, bukan gabungan seluruh grant yang dipegang. Login dengan tepat
    satu grant usable memilihkannya otomatis; dengan lebih dari satu, token
    terbit tanpa konteks aktif dan tidak mengotorisasi apa pun sampai
    `POST /api/v1/auth/switch-context` dipanggil. Lihat endpoint itu untuk
    detailnya, termasuk kenapa token lama tetap sah sesudahnya.


    Sejak isu #12 fase 5, `id_ruang` pada `POST /api/v1/pembelian` dan
    `id_ruang_asal` pada `POST`/`PATCH /api/v1/mutasi` divalidasi terhadap
    unit_kerja aktif sesi pemanggil — di luar unit itu → 403. Konteks aktif
    global (grant tanpa unit) tidak dibatasi. `id_ruang_tujuan` pada `mutasi`
    tidak pernah dibatasi: mutasi antar unit sengaja diperbolehkan.


    Sejak isu #12 fase 6, bacaan pada `ruang`, `pembelian`,
    `penerimaan-susulan`, `retur-pembelian`, `mutasi`, dan
    `GET /product/{id}/stok` juga disaring oleh unit_kerja aktif yang sama.
    Sebuah `GET /{id}` di luar unit menjawab 404, persis seperti id yang tidak
    ada — bukan 403, supaya bacaan yang disaring tidak sengaja membocorkan
    bahwa dokumennya ada. Sebuah `GET` daftar (dan `stok`, yang berbentuk
    daftar) diam-diam melewati baris di luar unit, tanpa error. Untuk
    `mutasi`, yang diperiksa hanya `id_ruang_asal` — sama seperti fase 5 —
    jadi dokumen yang `id_ruang_tujuan`-nya di luar unit aktif tetap terlihat
    selama `id_ruang_asal`-nya cocok. Konteks aktif global tidak dibatasi di
    sini juga. Ini bagian opsional dari fase 6 ("baru kalau terbukti perlu")
    yang dibangun; `users.id_ruang_default` dan kolom snapshot role pada
    dokumen transaksi tetap ditunda.


    Isu #8 menambah sisi baca dan koreksi `product_harga_jual`, yang sebelumnya cuma
    bisa ditambah lewat `POST .../harga-jual`. `GET /product/{id}/harga-jual`
    menjawab versi yang **berlaku** pada satu tanggal, satu baris per satuan —
    bukan seluruh riwayat seperti `GET /product/{id}`. Penentuannya `WHERE`
    biasa, bukan "yang terbaru menang": `product_harga_jual_no_overlap` menjamin
    tidak pernah ada dua versi berlaku bersamaan, jadi tidak pernah ada lebih
    dari satu kandidat untuk dipilih. `PATCH`/`DELETE
    .../harga-jual/{id_harga}` mengoreksi atau menghapus satu versi, ditolak
    **409** begitu ada baris `penjualan_detail` yang merujuknya — snapshot di
    dokumen itu tidak pernah berubah, tapi `id_harga_jual` yang menunjuk baris
    yang sudah beda makna bukan snapshot lagi. `DELETE` di sini **keras**, bukan
    lunak seperti master data lain: baris yang boleh dihapus adalah baris yang
    tidak satu dokumen pun merujuknya, jadi tidak ada jejak audit yang hilang.
    Menghapus selalu membuka kembali versi sebelumnya (`berlaku_sampai`-nya)
    supaya tidak meninggalkan rentang tanggal tanpa harga sama sekali.
    `GET /product/harga-jual` (tanpa `{id}`) adalah daftar harga lintas produk,
    `LEFT JOIN` supaya produk yang belum punya harga tetap muncul.


    Seluruh `/api/v1` butuh bearer token, kecuali `POST /api/v1/auth/login`,
    `POST /api/v1/auth/refresh`, dan `POST /api/v1/auth/logout` — dua yang
    terakhir (isu #24) memang ada untuk kasus access token-nya sudah
    kedaluwarsa, dan refresh token yang dikirim di body sudah jadi
    kredensialnya sendiri. Ambil access token dari `/auth/login` atau
    `/auth/refresh` lalu kirim sebagai `Authorization: Bearer <token>`.


    Otorisasi memakai nama role. Membaca terbuka untuk siapa pun yang sudah login —
    ini soal role guard, bukan seluruh jawaban sebuah bacaan: sejak fase 6 di atas,
    beberapa bacaan juga disaring lebih lanjut oleh unit_kerja aktif sesi; menulis
    dibagi menurut pemilik datanya — `INVENTARIS` untuk barang, satuan,
    ruang, ekspedisi, dan supplier, `CASHIER` untuk pelanggan. `SUPERADMIN` boleh
    apa saja. `role` dan `user` khusus `SUPERADMIN`, termasuk untuk membaca.


    `pembelian`, `penerimaan-susulan`, dan `retur-pembelian` dibagi menurut tahap
    alurnya, bukan menurut data yang disentuh. `INVENTARIS` mengetik faktur,
    menghitung barang, dan mengajukan; `SUPERADMIN` yang memposting, menolak, atau
    membatalkan. Alasannya: posting bukan penyuntingan — ia menambah baris ke
    `kartu_stok` yang bersifat append-only, jadi posting yang salah tidak bisa
    diperbaiki, hanya dibalik.


    `penerimaan-susulan` adalah kiriman kedua untuk barang yang belum datang di
    kiriman pertama. Ia **menambah stok dan tidak pernah menambah utang** — faktur
    supplier terbit penuh di kiriman pertama dan sudah dibukukan penuh di sana, jadi
    yang tersisa adalah barang, bukan uang. Nilai yang masuk adalah porsi sisa dari
    nilai faktur asli, sehingga satu faktur menyumbang persis nilainya sendiri ke
    persediaan berapa pun kali barangnya datang.


    `retur-pembelian` adalah cerminnya: dokumen yang sama-sama menunjuk baris
    `pembelian_detail` dan sama-sama menyalin harga pokoknya dari sana, hanya arah
    barangnya berlawanan. Yang bisa diretur adalah yang **benar-benar datang**
    (`qty_diterima_dasar + Σ susulan − Σ retur`), bukan yang difakturkan — barang yang
    tidak pernah datang dikejar dengan penerimaan susulan, bukan dikirim balik.


    `mutasi` adalah perpindahan barang antar `ruang`, dan dokumen pertama yang menulis
    `kartu_stok` ke **dua arah sekaligus**: satu baris detail menghasilkan
    `MUTASI_KELUAR` di ruang asal dan `MUTASI_MASUK` di ruang tujuan, dalam satu
    transaksi. Memecahnya jadi dua dokumen berarti barang bisa keluar dari gudang tanpa
    pernah masuk ke toko. Nilai yang masuk ke ruang tujuan **persis** nilai yang tercatat
    keluar dari ruang asal, sehingga total nilai persediaan tidak bergerak sedikit pun
    hanya karena barang pindah rak. Barang yang bertambah tanpa asal bukan mutasi
    melainkan stok opname.


    `mutasi` juga satu-satunya penulis `kartu_stok` yang **tidak punya `DIAJUKAN`** —
    alurnya `DRAFT → POSTED → BATAL`, tanpa `ajukan` dan `tolak`. Taruhannya jauh lebih
    kecil daripada tiga dokumen di atas: mutasi yang salah mencatat barang di ruang yang
    keliru, sementara total stok dan total nilai persediaan tidak berubah, dan
    koreksinya adalah mutasi lagi ke arah sebaliknya. Kendali dua orangnya tetap utuh
    lewat pembagian role: `INVENTARIS` hanya sampai `DRAFT`, `SUPERADMIN` yang
    memposting dan membatalkan. Konsekuensinya tidak ada lagi sinyal "draft ini sudah
    siap", jadi daftar berstatus `DRAFT` dengan `terlama_dulu=true` yang jadi antrean
    kerjanya.


    `pembayaran-utang` adalah uang yang keluar ke supplier, dan satu-satunya modul
    transaksi yang tidak menyentuh stok. Satu pembayaran boleh menutup beberapa faktur
    dan satu faktur boleh ditutup beberapa pembayaran, jadi alokasinya tabel tersendiri.
    Kelebihan bayar normal dan mengendap sebagai kredit. **Giro yang belum cair bukan
    pembayaran** — utangnya berkurang saat giro cair, bukan saat dokumennya diposting.


    Retur POSTED juga mengurangi utang, tetapi **bukan sebesar `total`-nya**: angka itu
    nilai persediaan menurut harga pokok, yang memuat porsi ongkir yang dibayar ke
    ekspedisi. Utang memakai `nilai_kredit_utang`, dihitung dari nilai faktur lalu
    diskalakan terhadap `pembelian.total` supaya diskon nota tidak ikut dikreditkan dua
    kali, dan dibekukan saat posting.


    `penerimaan-pembayaran` adalah cermin `pembayaran-utang` di sisi piutang — uang
    yang **masuk** dari pelanggan, alokasinya ke nota `KREDIT` yang `POSTED`, dan giro
    pelanggan yang sama-sama belum jadi pembayaran sebelum cair. Nota `TUNAI` tidak
    pernah menerima alokasi sama sekali: uangnya sudah berpindah tangan di konter, jadi
    ia tidak pernah jadi piutang. Inilah yang membuat `plafon_kredit` (`penjualan`, isu
    #10 fase 2) akhirnya bisa turun lagi — sebelum modul ini, `piutang_berjalan` hanya
    bisa naik, dan satu-satunya cara menurunkannya adalah membatalkan nota yang benar-benar
    terjadi.


    `dokumen` adalah lampiran berkas, dan ia infrastruktur lintas modul — bukan milik
    salah satu dokumen. Alurnya **unggah dulu, tempel kemudian**: foto faktur diambil
    saat box dibongkar, ketika dokumen pembeliannya belum tentu sudah dibuat, jadi
    berkas yang baru diunggah berstatus yatim (`ref_table` dan `ref_id` masih null)
    sampai ada yang mengklaimnya lewat `POST /api/v1/dokumen/{id}/tempel`. Yang tidak
    pernah diklaim disapu worker setelah `dokumen.orphan_ttl_hours`.


    Jenis berkas ditentukan dari **isi berkasnya**, bukan dari header `Content-Type`
    maupun ekstensi nama yang dikirim klien; nama simpannya dihasilkan server, dan
    `nama_asli` hanya untuk ditampilkan. Unduhan tetap di balik autentikasi dan selalu
    disajikan sebagai lampiran — foto faktur memuat harga beli dan identitas supplier.


    Uang dan kuantitas pada modul pembelian dikirim dan diterima sebagai **string
    desimal**, bukan angka JSON. `NUMERIC(20,2)` yang lewat float JSON kehilangan
    sen, dan angka-angka ini menjadi utang ke supplier sekaligus nilai persediaan.


    Token bersifat stateless: **tidak ada cara mencabutnya.** Menonaktifkan user
    atau mencabut rolenya tidak menyentuh token yang sudah keluar, dan aksesnya
    baru hilang saat token kedaluwarsa (`jwt.ttl_minutes`, default 60 menit).


    Semantik PATCH: hanya field yang benar-benar ada di body JSON yang diubah.
    Mengirim `null` pada kolom nullable **mengosongkan** kolom itu, sedangkan
    tidak mengirim field sama sekali membiarkannya apa adanya. Untuk kolom NOT
    NULL (`nama`, `is_aktif`) `null` ditolak dengan 400. Body kosong `{}` juga
    ditolak, supaya `updated_at` tidak mencatat perubahan yang tidak terjadi.


    Kunci `data` selalu ada di setiap respons. Endpoint list mengembalikan `[]`
    saat tidak ada baris, bukan `null` dan bukan kunci yang hilang; respons gagal
    mengembalikan `"data": null` berdampingan dengan `errors`.

servers:
  # 127.0.0.1, bukan localhost: port dipublikasikan compose ke 0.0.0.0 yang hanya
  # IPv4, sedangkan localhost sering diselesaikan ke ::1 lebih dulu sehingga
  # "Try it out" gagal dengan koneksi reset padahal servernya sehat.
  - url: http://127.0.0.1:3000
    description: Local development

# Berlaku untuk seluruh operasi kecuali yang menimpanya dengan `security: []` —
# yaitu halaman dokumentasi, /health, dan login.
security:
  - bearerAuth: []

tags:
  - name: auth
  - name: dokumen
  - name: pembelian
  - name: penerimaan-susulan
  - name: retur-pembelian
  - name: pembayaran-utang
  - name: mutasi
  - name: pemakaian
  - name: penjualan
  - name: penerimaan-pembayaran
  - name: stok-opname
  - name: product
  - name: laporan
  - name: periode
  - name: docs
  - name: unit-kerja
  - name: ruang
  - name: satuan
  - name: ekspedisi
  - name: supplier
  - name: pelanggan
  - name: role
  - name: user

paths:
  /:
    get:
      tags: [docs]
      summary: Swagger UI
      operationId: docsUI
      security: []
      description: >
        Halaman dokumentasi ini sendiri. Dimatikan dengan `web.swagger: false`
        (atau `WEB_SWAGGER=false`), dan saat dimatikan rutenya tidak terdaftar
        sama sekali sehingga menjawab 404, bukan halaman kosong.
      responses:
        "200":
          description: Halaman HTML Swagger UI
          content:
            text/html:
              schema: { type: string }
        "404":
          description: Dokumentasi dimatikan lewat konfigurasi

  /openapi.yaml:
    get:
      tags: [docs]
      summary: Kontrak OpenAPI ini
      operationId: docsSpec
      security: []
      description: >
        Berkas ini, ditanam ke dalam biner dengan `go:embed` lewat `docs/docs.go`,
        disajikan apa adanya. Ikut mati bersama `web.swagger: false`.


        Tombol "Try it out" mengarah ke `servers:` di atas, yaitu
        `http://127.0.0.1:3000`. Membuka dokumentasi dari host atau port lain
        berarti daftar itu perlu disesuaikan — tidak ada yang menulis ulang spec
        saat disajikan.
      responses:
        "200":
          description: Kontrak dalam YAML
          content:
            application/yaml:
              schema: { type: string }
        "404":
          description: Dokumentasi dimatikan lewat konfigurasi

  /api/v1/auth/login:
    post:
      tags: [auth]
      summary: Tukar kredensial dengan bearer token
      operationId: login
      security: []
      description: >
        Satu-satunya endpoint `/api/v1` yang tidak butuh token.


        Semua kegagalan menjawab pesan yang sama, `username or password is wrong`,
        baik usernamenya tidak ada, passwordnya salah, maupun akunnya
        `is_aktif: false`. Membedakannya memberi jalan untuk menebak username yang
        valid, dan operator tidak mendapat apa pun dari perbedaan itu.


        Username dicocokkan tanpa memperhatikan huruf besar-kecil, mengikuti
        `users_username_lower_uidx`.


        Token berisi grant yang dipegang user **saat token dibuat**, dan hanya
        yang usable — role-nya `is_aktif`, dan kalau grant-nya menyebut unit,
        unit itu juga `is_aktif`. Grant yang diberikan, dicabut, atau
        (unit/role-nya) dipensiunkan setelahnya baru terlihat pada login
        berikutnya.


        **Isu #12 fase 4 — konteks aktif.** Dengan wewenang bertempat, tidak
        ada default yang aman dipilihkan diam-diam. Memegang tepat satu grant
        usable membuatnya otomatis aktif; memegang lebih dari satu menjawab
        `aktif: null`, dan token itu **tidak mengotorisasi apa pun** sampai
        `POST /auth/switch-context` dipanggil untuk memilih salah satunya —
        lihat endpoint itu di bawah.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password]
              properties:
                username: { type: string, maxLength: 64, example: admin }
                password: { type: string, format: password, maxLength: 72 }
      responses:
        "200":
          description: Berhasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/LoginResult" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Error" }

  /api/v1/auth/me:
    get:
      tags: [auth]
      summary: Siapa yang server anggap sedang memanggil
      operationId: me
      description: >
        Membaca isi token, bukan tabel `users`. Itu memang yang dipakai server untuk
        memutuskan izin, jadi itulah yang berguna diperiksa — dan dengan token
        stateless keduanya bisa berbeda. Grant yang dicabut, atau role/unitnya
        dipensiunkan, setelah token dibuat masih muncul di sini sampai tokennya
        kedaluwarsa.
      responses:
        "200":
          description: Sesi yang sedang berlaku
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Session" }
        "401": { $ref: "#/components/responses/Error" }

  /api/v1/auth/switch-context:
    post:
      tags: [auth]
      summary: Pilih satu grant sebagai konteks aktif
      operationId: switchContext
      description: >
        Isu #12 fase 4. Menerbitkan token baru yang beraksi sebagai `id_user_role`
        yang disebut — tidak ada role guard di endpoint ini, karena sesi tanpa
        konteks aktif memang tidak mengotorisasi apa pun sama sekali, jadi tidak
        ada yang perlu dijaga selain autentikasi.


        Grant-nya dibaca ulang dari database saat itu juga, **tidak pernah
        dipercaya dari klaim token pemanggil sendiri** — satu-satunya titik di
        seluruh desain ini di mana kebasian token dibiarkan berpengaruh. Setiap
        kegagalan menjawab **403 yang sama**: grant tidak ada, grant milik user
        lain, rolenya sudah `is_aktif: false`, atau unitnya sudah
        `is_aktif: false`. Membedakannya memberi jalan menebak id grant milik
        orang lain, persis alasan `POST /auth/login` menjawab satu pesan untuk
        setiap kegagalan.


        **Token lama tetap sah.** Menukar konteks menerbitkan token baru; ia
        tidak mencabut yang lama, dan tidak bisa — desainnya JWT stateless.
        Token sebelumnya, dengan wewenang lamanya, tetap berlaku sampai
        kedaluwarsa (`jwt.ttl_minutes`). Konteks aktif adalah kendali
        kejelasan dan least-privilege, bukan batas keamanan terhadap pemegang
        tokennya sendiri.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id_user_role]
              properties:
                id_user_role: { type: integer, format: int64, minimum: 1 }
      responses:
        "200":
          description: Token baru dengan konteks aktif yang dipilih
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/LoginResult" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Error" }
        "403": { $ref: "#/components/responses/Error" }

  /api/v1/auth/me/password:
    post:
      tags: [auth]
      summary: Ganti password akun sendiri
      operationId: changeOwnPassword
      description: >
        Isu #24 fase 1. Terbuka untuk setiap pemanggil terautentikasi, tanpa
        role guard — sama seperti `auth/me` dan `switch-context`. Sesi tanpa
        konteks aktif (`aktif: null`) tetap boleh memanggilnya; orang yang
        belum memilih peran tetap berhak mengamankan akunnya sendiri.


        `password_lama` wajib diverifikasi walaupun pemanggilnya sudah
        terautentikasi — token yang dicuri tidak boleh bisa mengunci
        pemiliknya keluar dari akunnya sendiri. Salah menjawab pesan yang
        sama dengan `POST /auth/login`, `username or password is wrong`,
        tidak dibedakan dari sebab lain.


        Ini adalah aksi yang berbeda dari `PATCH /user/{id}` milik
        `SUPERADMIN`: yang itu mereset password orang lain dan tidak butuh
        `password_lama`, karena atasan memang tidak tahu password
        bawahannya. Sasaran endpoint ini selalu akun pemanggil sendiri —
        tidak ada field body yang bisa menggantinya.


        **Mencabut sesi (isu #24 fase 3).** Sukses di sini menghapus seluruh
        refresh token yang dipegang user — setiap sesi lain (device lain,
        browser lain) kehilangan akses saat access token-nya kedaluwarsa
        (`jwt.ttl_minutes`, default 15 menit), bukan seketika.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password_lama, password_baru]
              properties:
                password_lama: { type: string, format: password, maxLength: 72 }
                password_baru:
                  type: string
                  format: password
                  minLength: 8
                  maxLength: 72
      responses:
        "200":
          description: Password berhasil diganti
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/User" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Error" }

  /api/v1/auth/refresh:
    post:
      tags: [auth]
      summary: Tukar refresh token dengan sepasang token baru
      operationId: refresh
      security: []
      description: >
        Isu #24 fase 2. Tidak butuh bearer token — endpoint ini ada justru
        untuk kasus access token-nya sudah kedaluwarsa, jadi mensyaratkan
        salah satu di sini akan menggagalkan tujuannya sendiri. Refresh
        token itu sendiri adalah kredensial yang dipakai.


        **Dirotasi setiap dipakai.** Refresh token yang dikirim dihapus
        secara atomik sebelum apa pun lainnya terjadi — dipakai ulang
        (replay), baik oleh device lain yang balapan dengan rotasi yang sah
        maupun oleh penyerang yang mencegatnya, selalu ditolak dengan pesan
        yang sama dengan token yang memang sudah kedaluwarsa.


        Konteks aktif token lama dibaca ulang dari database, bukan
        dipercaya dari catatan yang tersimpan — kalau grant itu sudah
        dicabut atau dipensiunkan sejak refresh token diterbitkan, access
        token baru diterbitkan tanpa konteks aktif (sama seperti login
        dengan grant yang ambigu), dan pemanggil harus `switch-context`
        lagi.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token: { type: string }
      responses:
        "200":
          description: Sepasang token baru
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/LoginResult" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Error" }

  /api/v1/auth/logout:
    post:
      tags: [auth]
      summary: Cabut satu refresh token
      operationId: logout
      security: []
      description: >
        Isu #24 fase 2. Tidak butuh bearer token, sama seperti
        `POST /auth/refresh` — refresh token yang dikirim adalah satu-satunya
        kredensial yang dibutuhkan.


        Menjawab sukses baik refresh token-nya masih hidup maupun sudah
        kedaluwarsa/terpakai: keduanya sudah mencapai keadaan yang memang
        dituju logout, dan menjadikan yang kedua sebagai error akan memberi
        cara memeriksa apakah suatu nilai token pernah sah.


        **Access token yang sudah terlanjur terbit tidak ikut tercabut** —
        itu tetap sah sampai kedaluwarsa (`jwt.ttl_minutes`). Yang dicabut
        di sini murni kemampuan menukarnya dengan yang baru lewat
        `POST /auth/refresh`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token: { type: string }
      responses:
        "200":
          description: Refresh token tercabut (atau sudah tidak hidup sebelumnya)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      message: { type: string, example: logged out }
        "400": { $ref: "#/components/responses/ValidationError" }

  /health:
    get:
      summary: Liveness probe
      operationId: health
      security: []
      responses:
        "200":
          description: Service is up
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

  /api/v1/dokumen:
    post:
      tags: [dokumen]
      summary: Unggah satu lampiran berkas
      operationId: uploadDokumen
      description: >
        Menyimpan satu berkas dan mengembalikan metadatanya. Barisnya lahir **yatim**:
        `ref_table` dan `ref_id` masih null sampai ada yang menempelkannya lewat
        `POST /api/v1/dokumen/{id}/tempel`. Itu memang alurnya — foto faktur diambil
        sebelum dokumen pembeliannya tentu sudah ada.


        Yang diterima hanya JPEG, PNG, dan PDF, dan **jenisnya ditentukan dari isi
        berkas** (magic bytes), bukan dari header `Content-Type` atau ekstensi nama
        yang dikirim klien — keduanya sepenuhnya dikendalikan pemanggil. Berkas HTML
        yang diberi nama `faktur.pdf` ditolak 400.


        `nama_asli` disimpan apa adanya untuk ditampilkan dan **tidak pernah menjadi
        path**; nama simpan di server dihasilkan sendiri (UUID + ekstensi hasil
        deteksi). Batas ukuran (`dokumen.max_size_mb`, default 10 MB) ditegakkan saat
        berkasnya mengalir, bukan setelah utuh masuk memori.


        `created_by` diambil dari token, tidak pernah dari body.


        `duplikat_dari_id` terisi ketika sudah ada lampiran hidup dengan checksum yang
        sama. Itu peringatan, bukan penolakan: satu hasil scan sah saja ditempel ke dua
        dokumen berbeda.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [berkas]
              properties:
                berkas:
                  type: string
                  format: binary
                  description: JPEG, PNG, atau PDF. Maksimal `dokumen.max_size_mb`.
      responses:
        "201":
          description: Berkas tersimpan, masih yatim
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Dokumen" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Error" }
        "413":
          description: >
            Body melebihi batas transport. Batas Fiber diturunkan dari
            `dokumen.max_size_mb` plus satu MB untuk overhead multipart, jadi berkas
            yang hanya sedikit melewati batas tetap ditolak 400 dengan pesan yang
            menjelaskan — 413 berarti jauh lebih besar dari itu.

    get:
      tags: [dokumen]
      summary: Daftar lampiran satu dokumen, atau berkas yatim milik sendiri
      operationId: listDokumen
      description: >
        Dua mode, dipilih dari ada tidaknya referensi:


        - `ref_table` + `ref_id` diisi — lampiran dokumen itu.

        - keduanya kosong — berkas yang **pemanggil sendiri** unggah dan belum
          ditempelkan ke mana pun. Itulah yang dibutuhkan layar penerimaan untuk
          menawarkan foto yang baru diambil beberapa menit lalu.


        Mengisi salah satunya saja ditolak 400: setengah referensi menunjuk satu tabel
        utuh atau tidak menunjuk apa pun. Baris yang sudah dihapus tidak pernah ikut.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: ref_table
          in: query
          description: Harus salah satu jenis dokumen yang dikenal; selain itu 400.
          schema:
            type: string
            enum: [pembelian, penerimaan_susulan, retur_pembelian, pembayaran_utang]
        - name: ref_id
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
      responses:
        "200":
          description: Halaman hasil, terbaru dulu
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Dokumen" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Error" }

  /api/v1/dokumen/{id}:
    get:
      tags: [dokumen]
      summary: Unduh isi berkas
      operationId: getDokumenIsi
      description: >
        Mengalirkan isi berkasnya, bukan JSON. Tetap di balik autentikasi seperti
        endpoint lain — foto faktur memuat harga beli dan identitas supplier.


        Selalu disajikan sebagai unduhan: `Content-Disposition: attachment` dan
        `X-Content-Type-Options: nosniff`, sehingga HTML atau SVG yang entah bagaimana
        tersimpan tidak bisa dieksekusi di origin aplikasi. `Cache-Control:
        private, no-store` menjaganya tidak mengendap di cache bersama.


        Lampiran yang sudah dihapus menjawab 404: barisnya masih ada sebagai jejak,
        berkasnya tidak.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Isi berkas
          headers:
            Content-Disposition:
              schema: { type: string }
              description: >
                Selalu `attachment`. Nama asli dibawa di parameter `filename*`
                (RFC 5987); bentuk berkutip hanya ASCII aman, karena `nama_asli`
                berasal dari klien dan kutip atau baris baru di header adalah
                penyuntikan header.
            X-Content-Type-Options:
              schema: { type: string, example: nosniff }
          content:
            application/octet-stream:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }

    delete:
      tags: [dokumen]
      summary: Hapus lampiran (soft delete)
      operationId: deleteDokumen
      description: >
        Satu-satunya DELETE di API ini, dan ia **soft delete**: berkasnya dihapus dari
        disk, barisnya bertahan dengan `deleted_at` terisi. Jejak bahwa pernah ada
        unggahan lebih berharga daripada satu baris yang dihemat.


        Hanya boleh selama berkasnya **masih yatim atau dokumen induknya `DRAFT`**.
        Setelah dokumennya diajukan, foto faktur yang jadi dasar persetujuan adalah
        bagian dari rekaman, bukan milik pengunggah untuk ditarik kembali — 409.


        Menghapus dua kali menjawab 409, bukan 200: barisnya masih ada dan sudah
        ditandai.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Berkas terhapus
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Dokumen" }
        "401": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/dokumen/{id}/tempel:
    post:
      tags: [dokumen]
      summary: Tempelkan lampiran ke dokumen induknya
      operationId: tempelDokumen
      description: >
        Paruh kedua dari "unggah dulu, tempel kemudian", dan satu-satunya cara sebuah
        baris berhenti menjadi calon sapuan cronjob.


        Satu endpoint di modul ini, bukan field di setiap dokumen: modul yang mulai
        menerima lampiran cukup ditambahkan ke whitelist `ref_table` dan tidak perlu
        mengubah DTO-nya sendiri. Tidak ada foreign key di balik referensi polimorfik,
        jadi whitelist itulah yang memastikan `ref_table` menyebut dokumen sungguhan —
        nilai di luar daftar menjawab 400, dan `ref_id` yang tidak menunjuk baris mana
        pun juga 400.


        Ditolak 409 kalau lampirannya sudah tertempel ke dokumen lain (memindahkannya
        berarti diam-diam mencabut bukti dari dokumen yang memilikinya), kalau dokumen
        induknya sudah `BATAL`, atau kalau induknya sudah memegang 10 lampiran.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ref_table, ref_id]
              properties:
                ref_table:
                  type: string
                  enum: [pembelian, penerimaan_susulan, retur_pembelian, pembayaran_utang]
                ref_id: { type: integer, format: int64, minimum: 1 }
      responses:
        "200":
          description: Lampiran tertempel
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Dokumen" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/product:
    get:
      tags: [product]
      summary: List product
      operationId: listProduct
      description: >
        Tidak menyertakan `satuan` maupun `harga_jual` — mengambilnya berarti satu query
        per baris. Kuncinya hilang sama sekali, bukan array kosong. Pakai endpoint detail
        untuk keduanya.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: Cocokkan sebagian pada nama atau kode barang.
          schema: { type: string, maxLength: 255 }
        - $ref: "#/components/parameters/IsAktif"
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Product" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [product]
      summary: Buat produk beserta satuan konversinya
      operationId: createProduct
      description: >
        Produk dan seluruh baris `product_satuan` ditulis dalam satu transaksi.


        **Satuan dasar didaftarkan otomatis dengan `faktor = 1`** dari `id_satuan_dasar`,
        jadi `satuan` boleh dikosongkan dan produk tidak pernah berakhir tanpa satuan
        dasar. Menyebut satuan dasar sekali lagi di dalam `satuan` diperbolehkan dan
        melebur ke baris otomatis itu; menyebutnya dengan `faktor` selain 1 ditolak 400.


        `created_by` diambil dari token, tidak pernah dari body.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [kode_barang, nama, id_satuan_dasar]
              properties:
                kode_barang:
                  type: string
                  maxLength: 64
                  description: Unik. Duplikat menjawab 409.
                  example: BRG-001
                nama: { type: string, maxLength: 255, example: Kertas A4 }
                id_satuan_dasar:
                  type: integer
                  format: int64
                  minimum: 1
                  description: >
                    Harus ada di tabel `satuan`; id yang tidak dikenal menjawab 400, bukan
                    500.
                stok_minimum: { type: integer, format: int64, minimum: 0, default: 0 }
                satuan:
                  type: array
                  maxItems: 32
                  items: { $ref: "#/components/schemas/ProductSatuanInput" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Product" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/product/{id}:
    get:
      tags: [product]
      summary: Detail produk, dengan satuan dan riwayat harga jual
      operationId: getProduct
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Product" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [product]
      summary: Update product
      operationId: updateProduct
      description: >
        Hanya `nama`, `stok_minimum`, dan `is_aktif`. Ketiganya NOT NULL, jadi `null`
        ditolak 400.


        **`kode_barang` dan `id_satuan_dasar` tidak bisa diubah** dan tidak ada di body.
        `kode_barang` mengidentifikasi barang di setiap dokumen yang merujuknya, jadi
        menggantinya menulis ulang makna dokumen lama. `id_satuan_dasar` lebih parah:
        mengubahnya membatalkan seluruh `faktor` di `product_satuan` dan setiap kuantitas
        yang sudah diposting ke `kartu_stok` dalam satuan dasar lama. Pensiunkan dengan
        `is_aktif: false` lalu buat produk baru.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                nama: { type: string, maxLength: 255 }
                stok_minimum: { type: integer, format: int64, minimum: 0 }
                is_aktif: { type: boolean }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Product" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

  /api/v1/product/{id}/satuan:
    post:
      tags: [product]
      summary: Tambah satuan konversi
      operationId: addProductSatuan
      description: >
        Mengirim satuan yang sudah terdaftar memperbarui `faktor`-nya, bukan menolak —
        supaya respons sukses tidak pernah berarti konversi tersimpan berbeda dari yang
        diminta.


        `is_default_input: true` **memindahkan** penanda default: yang lama dibersihkan
        lebih dulu di transaksi yang sama, karena `product_satuan_default_uidx` hanya
        mengizinkan satu baris bertanda per produk.


        Satuan dasar hanya boleh `faktor = 1`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProductSatuanInput" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Product" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/product/{id}/harga-jual:
    get:
      tags: [product]
      summary: Versi harga yang berlaku, per satuan, pada satu tanggal
      operationId: getHargaJualBerlaku
      description: >
        Isu #8 fase 1 — ini yang sebelumnya tidak ada dan menghalangi `penjualan`:
        `penjualan_detail.id_harga_jual` mereferensi satu baris versi, bukan sebuah
        angka, dan sebelum endpoint ini satu-satunya cara mengetahuinya adalah membuka
        `GET /product/{id}` dan menyerahkan pemilihan "mana yang berlaku" ke klien.


        Satu baris per satuan yang punya harga berlaku pada `tanggal`.
        Penentuannya `WHERE` biasa (`berlaku_dari <= tanggal AND (berlaku_sampai IS
        NULL OR berlaku_sampai > tanggal)`), bukan "yang terbaru menang" — karena
        `product_harga_jual_no_overlap` menjamin tidak pernah ada dua versi berlaku
        bersamaan, tidak pernah ada lebih dari satu kandidat untuk dipilih. Kalau
        exclusion constraint itu suatu hari dilonggarkan, resolver ini ikut jadi
        ambigu.


        `tanggal` opsional, format `YYYY-MM-DD`, default hari ini di WIB
        (`Asia/Jakarta`, offset tetap +7 karena tidak ada DST). Ini bukan konversi
        `TIMESTAMPTZ` — pemanggil menyebut tanggal kalender secara langsung — tapi
        default "hari ini" tetap harus diputuskan di zona waktu yang sama supaya
        transaksi lewat tengah malam WIB tidak salah harga.


        Produk yang tidak dikenal menjawab **404**; produk yang belum punya harga
        berlaku pada tanggal itu — termasuk yang belum punya harga sama sekali —
        menjawab daftar kosong. Dua fakta berbeda, sama seperti argumen di
        `riwayat_beli`.
      parameters:
        - $ref: "#/components/parameters/ID"
        - name: tanggal
          in: query
          description: Default hari ini di WIB.
          schema: { type: string, format: date, example: "2026-08-14" }
      responses:
        "200":
          description: Versi yang berlaku, satu baris per satuan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/HargaJualBerlaku" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

    post:
      tags: [product]
      summary: Buka versi harga jual baru
      operationId: addProductHargaJual
      description: >
        Versi yang masih terbuka untuk produk dan satuan yang sama ditutup pada
        `berlaku_dari`, di transaksi yang sama. Rentangnya setengah terbuka, jadi menutup
        pada tanggal itu tidak meninggalkan celah maupun tumpang tindih — harga lama
        berlaku sampai tanggal itu, harga baru mulai dari tanggal itu.


        Periode yang tumpang tindih menjawab **409**, ditegakkan exclusion constraint
        `product_harga_jual_no_overlap`. Itu satu-satunya penjaga yang nyata: pemeriksaan
        tumpang tindih melintasi baris, jadi pengecekan di aplikasi tidak bisa
        menggantikannya.


        `id_satuan` harus sudah terdaftar di `product_satuan` — harga untuk satuan yang
        tidak dijual tidak akan pernah muncul di layar input. Tidak ada foreign key yang
        menjaga itu, jadi ditolak 400 di usecase.


        `product_harga_jual` hanya sumber pengisian harga otomatis. Harga yang
        benar-benar ditagih disalin sebagai snapshot ke `penjualan_detail`, jadi mengubah
        harga master tidak pernah menyentuh transaksi lama.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id_satuan, harga, berlaku_dari]
              properties:
                id_satuan: { type: integer, format: int64, minimum: 1 }
                harga: { $ref: "#/components/schemas/Harga" }
                berlaku_dari: { type: string, format: date, example: "2026-01-01" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Product" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/product/{id}/harga-jual/{id_harga}:
    patch:
      tags: [product]
      summary: Koreksi harga pada versi yang sudah ada
      operationId: updateProductHargaJual
      description: >
        Isu #8 fase 2. Hanya `harga`: `id_satuan` dan `berlaku_dari` tidak ada di body
        dan tidak bisa diubah lewat endpoint ini, karena `berlaku_dari` menggeser
        rentang tanggal dan bisa menabrak versi tetangganya. Koreksi untuk itu adalah
        hapus lalu ketik ulang (`DELETE` di path yang sama), dan itu memang yang
        seharusnya terlihat di jejak.


        Ditolak **409** begitu ada baris `penjualan_detail` yang merujuk
        `id_harga`. `penjualan_detail.harga_satuan_input` sudah menyimpan snapshot,
        jadi mengubah baris master ini tidak pernah mengubah nilai nota mana pun —
        tapi ia membuat `id_harga_jual` menunjuk baris yang tidak lagi menyatakan apa
        yang ditagih, dan itu satu-satunya jejak "harga ini datang dari daftar harga
        versi mana".
      parameters:
        - $ref: "#/components/parameters/ID"
        - $ref: "#/components/parameters/IDHarga"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [harga]
              properties:
                harga: { $ref: "#/components/schemas/Harga" }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Product" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

    delete:
      tags: [product]
      summary: Hapus satu versi harga dan buka kembali versi sebelumnya
      operationId: deleteProductHargaJual
      description: >
        Isu #8 fase 2. **`DELETE` di sini keras, bukan lunak** — pengecualian ketiga
        atas aturan "master data tidak punya `DELETE`", setelah `user_role` dan
        `dokumen`. Baris yang boleh dihapus adalah baris yang tidak satu dokumen pun
        merujuknya (dijaga syarat 409 yang sama seperti `PATCH`), jadi tidak ada jejak
        audit yang hilang — tidak ada yang pernah memakainya.


        Menghapus **selalu** membuka kembali versi sebelumnya dalam transaksi yang
        sama: `berlaku_sampai` versi itu dikembalikan ke `berlaku_sampai` milik versi
        yang dihapus (`null` kalau versi yang dihapus itu yang terbuka). Tanpa ini,
        menghapus versi yang baru akan meninggalkan **lubang** — rentang tanggal tanpa
        harga sama sekali, dan `GET .../harga-jual` akan menjawab "tidak ada harga"
        untuk produk yang jelas-jelas punya. Versi paling pertama sebuah produk tidak
        punya versi sebelumnya untuk dibuka kembali — itu bukan galat, cuma tidak ada
        apa-apa untuk dilakukan.
      parameters:
        - $ref: "#/components/parameters/ID"
        - $ref: "#/components/parameters/IDHarga"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Product" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/product/harga-jual:
    get:
      tags: [product]
      summary: Daftar harga lintas produk
      operationId: listDaftarHargaJual
      description: >
        Isu #8 fase 3 — sebuah read yang bukan modul, ikut pola `riwayat_beli`: tidak
        ada tabel baru, tidak ada migrasi, satu query di repository yang sudah
        memiliki `product_harga_jual`.


        Satu baris per produk + satuan yang punya harga berlaku pada `tanggal`
        (default hari ini di WIB, sama seperti `GET /product/{id}/harga-jual`),
        **plus** satu baris per produk yang belum punya harga sama sekali —
        `LEFT JOIN`, bukan `JOIN`, karena produk tanpa harga justru yang paling perlu
        muncul di sini. Untuk baris seperti itu, `id_satuan`, `id_harga_jual`, `harga`,
        `berlaku_dari`, dan `berlaku_sampai` semuanya `null`.


        Ini yang dicetak jadi daftar harga dan yang dipakai memeriksa produk mana yang
        belum punya harga sama sekali — pertanyaan yang tanpa endpoint ini hanya bisa
        dijawab dengan membuka produk satu per satu.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
        - name: tanggal
          in: query
          description: Default hari ini di WIB.
          schema: { type: string, format: date, example: "2026-08-14" }
        - $ref: "#/components/parameters/IsAktif"
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/DaftarHargaJual" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

  /api/v1/product/{id}/riwayat-beli:
    get:
      tags: [product]
      summary: Harga beli terakhir per supplier
      operationId: listRiwayatBeliProduct
      description: >
        Pengganti purchase order. Yang sebenarnya dibutuhkan sebelum memesan bukan
        dokumen pesanan melainkan perbandingan harga, dan datanya sudah terkumpul
        sendiri: setiap `pembelian` yang diposting adalah harga yang **benar-benar
        dibayar**, bukan yang dijanjikan di chat. Tidak ada yang perlu diinput dan tidak
        ada yang bisa jadi tidak sinkron.


        Satu baris per supplier — pembelian **terakhir** supplier itu atas produk ini,
        bukan satu baris per dokumen. Diurutkan dari yang terbaru.


        Hanya dokumen **POSTED** yang dihitung. Draft cuma kertas yang sudah diketik dan
        dokumen BATAL adalah pembelian yang ditarik kembali; keduanya bukan harga yang
        pernah dibayar siapa pun.


        Produk yang tidak dikenal menjawab **404**, produk yang belum pernah dibeli
        menjawab halaman kosong. Keduanya fakta yang berbeda.


        Dua harga, dan keduanya tidak saling menggantikan — lihat skema `RiwayatBeli`.
      parameters:
        - $ref: "#/components/parameters/ID"
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: id_supplier
          in: query
          description: >
            Persempit ke satu supplier. Itu yang dipakai layar input pembelian: header-nya
            sudah menyebut supplier, jadi pertanyaan yang berguna di sana adalah "terakhir
            saya bayar berapa ke supplier ini", bukan "semua orang menjual berapa".
          schema: { type: integer, format: int64, minimum: 1 }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/RiwayatBeli" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

  /api/v1/product/{id}/stok:
    get:
      tags: [product]
      summary: Saldo dan nilai persediaan produk ini per ruang
      operationId: listStokProduct
      description: >
        Bacaan langsung atas `kartu_stok`, yang merupakan satu-satunya sumber kebenaran
        stok — tidak ada tabel master yang menyimpan kolom stok dan tidak ada yang
        dihitung dengan menjumlahkan dokumen.


        Layar input mutasi tidak bisa dipakai tanpa ini: orang harus tahu ruang asal
        punya berapa sebelum mengetik. Penjualan, pemakaian, dan stok opname akan
        memakainya juga.


        **Ini bacaan, bukan penjaga.** Angkanya bisa sudah basi sebelum klien
        menindaklanjutinya; yang memutuskan saldo adalah trigger `kartu_stok`, di dalam
        advisory lock, justru supaya tidak ada pembaca yang bisa mendahuluinya. Tampilkan
        angkanya, jangan jadikan dasar keputusan.


        Tidak ada paginasi, berbeda dengan setiap daftar lain di API ini: jawabannya satu
        baris per ruang yang pernah dilalui produk ini, `ruang` tabel master yang isinya
        sedikit, dan setiap pemakainya menginginkan semuanya sekaligus untuk memilih
        ruang asal.


        Ruang yang belum pernah dilalui produk ini tidak muncul; ruang yang pernah
        dilalui lalu habis tetap muncul dengan nilai nol, karena itu fakta tentang ke
        mana barangnya pergi. Produk yang tidak dikenal menjawab **404**, produk yang
        belum pernah bergerak menjawab array kosong — keduanya fakta yang berbeda.


        Isu #12 fase 6: ruang di luar unit_kerja aktif sesi pemanggil dilewati
        begitu saja, tanpa error — sama seperti daftar lain yang disaring.
        Konteks aktif global melihat semua ruang seperti biasa.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Saldo per ruang, diurutkan menurut nama ruang
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/StokRuang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

  /api/v1/product/{id}/kartu-stok:
    get:
      tags: [product]
      summary: Riwayat pergerakan produk ini di satu ruang
      operationId: listKartuStokProduct
      description: >
        Isu #22 fase 1 — bacaan yang bukan modul: tidak ada tabel baru, tidak ada
        migrasi, satu query di `KartuStokRepository` yang sudah memiliki
        `kartu_stok`. Ini yang menjawab "kenapa saldonya jadi segini", yang
        `GET /product/{id}/stok` tidak bisa: yang itu hanya menunjukkan ekor rantai,
        ini menunjukkan seluruh rantainya.


        `id_ruang` **wajib**, berbeda dari `GET /product/{id}/stok` — rantai saldo
        dipartisi per `(id_barang, id_ruang)`, dan "riwayat" yang mencampur beberapa
        ruang akan menampilkan saldo berjalan yang tidak pernah benar-benar ada di
        satu rak mana pun. Ruang yang tidak dikenal menjawab **404**.


        Diurutkan **naik** menurut `id`, bukan tanggal dan bukan turun seperti
        daftar lain di API ini — rantai saldo dibangun berurutan menurut `id` oleh
        trigger, dan pembalikan tanggalnya `time.Now()` sehingga urutan tanggal bisa
        berbeda dari urutan sungguhan rantainya. Sebuah buku besar dibaca dari atas
        ke bawah.


        `ref_table` + `ref_id_transaksi` diterjemahkan jadi `nomor_dokumen` lewat
        `CASE` di query yang sama — tidak ada N+1 per baris untuk mencari tahu
        dokumen mana yang menghasilkannya.


        `id_kartu_stok_asal` terisi menandai baris pembalik (hasil pembatalan
        dokumen sumbernya) — klien membedakannya dari pergerakan biasa lewat kolom
        ini, tanpa menebak dari `jenis_transaksi` yang dipakai bersama antara
        posting dan pembaliknya.


        Isu #12 fase 6: ruang di luar unit_kerja aktif sesi pemanggil menjawab
        **halaman kosong**, bukan 404 — ini daftar, dan ruangnya sendiri sudah
        dijawab 404 lewat pemeriksaan "apakah ruang ini ada sama sekali" yang
        terpisah.
      parameters:
        - $ref: "#/components/parameters/ID"
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: id_ruang
          in: query
          required: true
          schema: { type: integer, format: int64, minimum: 1 }
        - name: dari
          in: query
          description: Tanggal awal (inklusif), `tanggal_transaksi`.
          schema: { type: string, format: date, example: "2026-08-01" }
        - name: sampai
          in: query
          description: Tanggal akhir (inklusif), `tanggal_transaksi`.
          schema: { type: string, format: date, example: "2026-08-31" }
      responses:
        "200":
          description: Riwayat pergerakan, urut id naik
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/KartuStok" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

  /api/v1/product/stok-minimum:
    get:
      tags: [product]
      summary: Produk yang stoknya di titik atau di bawah stok_minimum
      operationId: listStokMinimumProduct
      description: >
        Isu #22 fase 2 — pasangan alami `GET /product/{id}/riwayat-beli`: yang itu
        menjawab beli ke siapa dan berapa harganya, ini menjawab apa yang harus
        dibeli sama sekali. Berdua menggantikan purchase order yang sengaja tidak
        ada di sistem ini.


        `stok_minimum = 0` **tidak pernah muncul** — nol adalah nilai default
        kolom itu, artinya "belum diatur", bukan "boleh habis".


        Ambangnya `total_stok <= stok_minimum`, bukan `<`: menyentuh titik pesan
        ulang **adalah** saatnya memesan.


        `total_stok` adalah jumlah seluruh ruang dalam `unit_kerja` aktif sesi
        pemanggil (isu #12 fase 6), dengan rincian per ruang di `per_ruang`.
        `id_ruang` mempersempit perbandingan ke satu ruang saja — berguna untuk
        gudang yang sengaja dikosongkan karena stoknya sudah dipindah ke toko, yang
        kalau dibandingkan terhadap total perusahaan akan menyembunyikan toko yang
        benar-benar habis.


        Hanya produk `is_aktif`. Diurutkan dari yang paling parah — selisih
        terhadap `stok_minimum`, bukan abjad — karena ini daftar kerja, pola yang
        sama dengan `GET /supplier/{id}/utang`.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: id_ruang
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
      responses:
        "200":
          description: Halaman hasil, terparah dulu
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/StokMinimum" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

  /api/v1/pos/product:
    get:
      tags: [product]
      summary: Katalog produk siap pakai untuk layar POS
      operationId: listPosProduct
      description: >
        Isu #11. Satu bacaan menggantikan apa yang hari ini butuh empat: `GET
        /product` untuk daftarnya, lalu per baris `GET /product/{id}` untuk
        satuannya, `GET /product/{id}/harga-jual` untuk harganya, dan
        `GET /product/{id}/stok` untuk sisanya. Dua puluh baris jadi enam puluh
        permintaan, di layar yang paling sering dibuka di seluruh aplikasi.


        Path sendiri, bukan parameter `view` di `GET /product`: bentuk respons
        yang ditentukan satu layar boleh berubah mengikuti layar itu tanpa
        menggoyang kontrak `GET /product`, dan tidak menabrak `/product/:id` —
        `GET /product/pos` akan bergantung urutan registrasi rute.


        **Tiga query per halaman, apa pun jumlah barisnya**: produk + paginasi,
        satuan dan harga berlaku untuk seluruh baris halaman itu dalam satu
        batch, dan saldo seluruh pasangan (produk, ruang) halaman itu dalam
        satu batch — mengikuti pola yang sama seperti `FindRolesByUserIDs`,
        `FindFaktorBatch`, dan `SaldoBatch`. Mengambil satuan atau harga per
        baris akan menggagalkan seluruh tujuan endpoint ini sambil terlihat
        berfungsi.


        **Bersarang, bukan datar**: satu baris per produk, dengan `satuan`
        sebagai array. Produk dengan tiga satuan tidak memakan tiga slot dari
        halaman berisi 20 — `total_item` menghitung produk, bukan kombinasi
        (produk, satuan).


        `stok_akhir` selalu dalam satuan dasar, karena `kartu_stok` tidak
        mengenal satuan lain; konversi ke satuan tampilan urusan klien, dan
        `faktor` sudah dikirim untuk itu. `id_harga_jual` ikut karena
        `penjualan_detail.id_harga_jual` merujuknya — tanpa itu layar POS tahu
        angkanya tapi tidak tahu versi mana yang jadi asalnya.


        **Hanya barang aktif, tanpa parameter untuk menampilkan yang lain** —
        barang yang sudah dipensiunkan tidak boleh bisa dijual. Barang yang
        stoknya habis tetap muncul dengan `stok_akhir: 0`, dan barang yang
        belum punya harga tetap muncul dengan `id_harga_jual`/`harga` bernilai
        `null` pada baris satuan itu — keduanya fakta yang berguna, bukan
        alasan untuk disembunyikan.


        **HPP tidak pernah ikut terkirim.** Bacaan ini terbuka untuk siapa pun
        yang terautentikasi dan sering terlihat pembeli di meja kasir; HPP
        adalah margin toko. `GET /product/{id}/stok` yang memuatnya tetap ada
        untuk yang memang berhak.


        Kecocokan **persis** pada `kode_barang` diurutkan paling atas —
        kasir yang memindai atau mengetik kode utuh mengharapkan baris itu di
        puncak, bukan di tengah daftar terurut nama.


        `tanggal` opsional, default hari ini di WIB — resolver dan keputusan
        zona waktu yang sama dengan isu #8 (`AT TIME ZONE 'Asia/Jakarta'`,
        offset tetap +7), tidak diputuskan ulang di sini.


        `id_ruang` **wajib** dan divalidasi ada di `ruang` → **404** kalau
        tidak dikenal. Ruang yang salah ketik yang diam-diam menjawab stok nol
        di setiap baris adalah bug yang mahal untuk ditemukan.
      parameters:
        - name: id_ruang
          in: query
          required: true
          description: Ruang tempat kasir berdiri. Tidak ada default — kasir selalu tahu ruangnya.
          schema: { type: integer, format: int64, minimum: 1 }
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: Menyaring `nama` dan `kode_barang`, lewat EscapeLike.
          schema: { type: string, maxLength: 255 }
        - name: tanggal
          in: query
          description: Default hari ini di WIB. Menentukan versi harga_jual mana yang berlaku.
          schema: { type: string, format: date, example: "2026-08-14" }
      responses:
        "200":
          description: Halaman katalog POS
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/PosProduct" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

  /api/v1/laporan/nilai-persediaan:
    get:
      tags: [laporan]
      summary: Nilai persediaan saat ini, per ruang
      operationId: listLaporanNilaiPersediaan
      description: >
        Isu #22 fase 3 — bacaan yang bukan modul, tanpa tabel dan tanpa angka
        tersimpan sendiri. Jumlah `nilai_akhir` dari baris terakhir setiap pasangan
        `(barang, ruang)`, direkap per ruang. `GET /product/{id}/stok` sudah
        membaca bentuk yang sama untuk satu produk; ini rekap lintas ruang dan
        lintas produk di atasnya.


        `ruang.is_aktif` **tidak pernah** jadi saringan di sini — ruang yang sudah
        dipensiunkan sambil masih memegang barang tetap memegang nilainya, dan
        laporan yang menyembunyikannya akan berbohong tentang neraca.
      parameters:
        - name: id_ruang
          in: query
          description: Persempit ke satu ruang.
          schema: { type: integer, format: int64, minimum: 1 }
      responses:
        "200":
          description: Nilai persediaan per ruang
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/NilaiPersediaan" }
        "400": { $ref: "#/components/responses/ValidationError" }

  /api/v1/laporan/laba-kotor:
    get:
      tags: [laporan]
      summary: Laba kotor per bulan
      operationId: listLaporanLabaKotor
      description: >
        Isu #22 fase 3. `SUM(total) - SUM(total_hpp)` atas nota `penjualan`
        **POSTED**, dikelompokkan per bulan `tanggal`.


        Satu-satunya laporan di isu ini yang membaca dokumen, bukan `kartu_stok`
        — dan itu benar, bukan pengecualian: `total_hpp` sudah disalin dari
        `RETURNING kartu_stok` saat posting dan dibekukan di sana, jadi membacanya
        ulang dari `kartu_stok` hanya akan membayar query yang lebih mahal untuk
        angka yang sama.


        Nota `BATAL` dikeluarkan. `retur_penjualan` belum ada, jadi belum ada yang
        mengurangi laba kotor untuk penjualan yang akhirnya dikembalikan — begitu
        modul itu ada, kreditnya masuk ke `SUM` ini di samping `total_hpp`.
      parameters:
        - name: dari
          in: query
          description: Tanggal awal (inklusif), `tanggal` nota. Kosong berarti sejak awal.
          schema: { type: string, format: date, example: "2026-08-01" }
        - name: sampai
          in: query
          description: Tanggal akhir (inklusif), `tanggal` nota. Kosong berarti sampai sekarang.
          schema: { type: string, format: date, example: "2026-08-31" }
      responses:
        "200":
          description: Laba kotor per bulan, urut kronologis
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/LabaKotor" }
        "400": { $ref: "#/components/responses/ValidationError" }

  /api/v1/laporan/pergerakan:
    get:
      tags: [laporan]
      summary: Rekap pergerakan stok per periode
      operationId: listLaporanPergerakan
      description: >
        Isu #22 fase 3. Jumlah `stok_masuk` dan `stok_keluar` per
        `(barang, ruang, jenis_transaksi)` dalam rentang tanggal — yang menjawab
        "barang ini keluar ke mana saja bulan lalu", dan yang membuat penyusutan
        hasil `stok_opname` terlihat sebagai angka bulanan, bukan per dokumen.


        Rentangnya menyaring `kartu_stok.tanggal_transaksi`, **bukan** status atau
        tanggal dokumen — inilah yang membuat jebakan yang disebut namanya di
        CLAUDE.md terlihat, bukan tersembunyi: dokumen yang diposting di satu
        periode lalu dibatalkan di periode berikutnya menghasilkan baris pembalik
        bertanggal `time.Now()` (isu #6), sehingga baris itu muncul di rentang yang
        meliputi hari pembatalan, bukan hari dokumennya diposting.
      parameters:
        - name: dari
          in: query
          description: Tanggal awal (inklusif), `tanggal_transaksi`. Kosong berarti sejak awal.
          schema: { type: string, format: date, example: "2026-08-01" }
        - name: sampai
          in: query
          description: Tanggal akhir (inklusif), `tanggal_transaksi`. Kosong berarti sampai sekarang.
          schema: { type: string, format: date, example: "2026-08-31" }
        - name: id_ruang
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: id_product
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
      responses:
        "200":
          description: Rekap pergerakan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Pergerakan" }
        "400": { $ref: "#/components/responses/ValidationError" }

  /api/v1/pembelian:
    get:
      tags: [pembelian]
      summary: List pembelian
      operationId: listPembelian
      description: >
        Tidak menyertakan `detail` — mengambilnya berarti satu query per baris.
        Kuncinya hilang sama sekali, bukan array kosong. Pakai endpoint detail
        untuk barisnya.


        Diurutkan `tanggal DESC, id DESC`. Pemecah seri yang unik itu wajib: banyak
        dokumen bertanggal sama, dan tanpa pemecah seri satu baris bisa muncul di
        dua halaman sementara baris lain tidak pernah keluar.


        Isu #12 fase 6: dokumen yang ruangnya di luar unit_kerja aktif sesi
        pemanggil dilewati begitu saja, tanpa error — `total_item` sudah
        menghitung setelah penyaringan ini.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: Cocokkan sebagian pada nomor dokumen atau no faktur supplier.
          schema: { type: string, maxLength: 255 }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, DIAJUKAN, POSTED, BATAL] }
        - name: status_penerimaan
          in: query
          schema: { type: string, enum: [LENGKAP, KURANG] }
        - name: id_supplier
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: tanggal_dari
          in: query
          description: Inklusif, satu hari penuh.
          schema: { type: string, format: date }
        - name: tanggal_sampai
          in: query
          description: Inklusif, satu hari penuh sampai tengah malam berikutnya.
          schema: { type: string, format: date }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Pembelian" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [pembelian]
      summary: Buat draft pembelian beserta barisnya
      operationId: createPembelian
      description: >
        Dokumen selalu dibuat sebagai `DRAFT`; `status` tidak ada di body sama
        sekali. Stok bergerak saat posting dan hanya saat posting.


        **Nomor dokumen digenerate server** dengan format
        `BL/KODE/2026/08/0001` — `KODE` adalah kode unit_kerja yang menaungi
        `id_ruang` dokumen ini — mereset tiap bulan mengikuti `tanggal`
        dokumen — faktur Juli yang baru diketik bulan Agustus tetap dapat
        nomor Juli. Sebuah unit tanpa kode tidak bisa menerbitkan nomor sama
        sekali (400). Counter-nya diambil dengan satu
        `INSERT ... ON CONFLICT DO UPDATE RETURNING` di dalam transaksi
        dokumen, jadi dua request bersamaan antre alih-alih menghasilkan nomor
        kembar. Nomor bisa berlubang kalau transaksinya rollback; itu jauh lebih
        murah daripada dua dokumen bernomor sama.


        **`qty_diterima` boleh dikosongkan** dan artinya sama dengan `qty_faktur`
        — kasus biasa di mana semua yang tertulis memang ada di dalam box.
        Mengisinya adalah cara mencatat kiriman kurang, dan saat berbeda
        `keterangan_selisih` jadi wajib.


        `qty_diterima` **tidak boleh melebihi** `qty_faktur`: barang yang tidak
        difakturkan tidak punya nilai, sehingga `nilai_masuk` proporsionalnya akan
        melebihi nilai faktur dan rata-rata bergerak jadi salah permanen.


        `faktor_konversi` diambil dari `product_satuan` dan disimpan sebagai
        snapshot. Satuan yang belum terdaftar di produk itu ditolak 400 — tidak ada
        foreign key yang mengikat `id_satuan_input` ke `product_satuan`.
        `qty x faktor` juga harus bilangan bulat, karena `qty_dasar` bertipe
        `BIGINT`.


        `subtotal`, `total`, `biaya_angkut`, dan `status_penerimaan` dihitung ulang
        server dari barisnya; mengirimnya tidak ada gunanya. `biaya_angkut =
        total_koli x tarif_per_koli` dan **bukan** bagian dari `total` — itu
        tagihan ekspedisi, bukan utang ke supplier.


        `created_by` diambil dari token, tidak pernah dari body.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tanggal, id_supplier, id_ruang, detail]
              properties:
                tanggal: { type: string, format: date, example: "2026-08-11" }
                id_supplier: { type: integer, format: int64, minimum: 1 }
                id_ruang:
                  type: integer
                  format: int64
                  minimum: 1
                  description: >
                    Ruang tujuan seluruh baris. Satu dokumen tidak bisa masuk ke dua
                    ruang.


                    Isu #12 fase 5: divalidasi terhadap unit_kerja aktif sesi
                    pemanggil. Konteks aktif global (grant tanpa unit, bentuk
                    SUPERADMIN) tidak dibatasi; konteks aktif yang menyebut
                    satu unit hanya boleh memilih ruang di unit itu, kalau
                    tidak → 403.
                no_faktur_supplier:
                  type: string
                  nullable: true
                  maxLength: 64
                  description: >
                    Unik per supplier dan tidak peduli huruf besar-kecil. Tanpa
                    purchase order, ini satu-satunya jejak faktur supplier — nota
                    yang sama diinput dua kali akan menaikkan stok dua kali. Dokumen
                    `BATAL` melepas nomornya kembali. Boleh kosong, dan berapa pun
                    dokumen boleh sama-sama kosong.
                tanggal_faktur: { type: string, format: date, nullable: true }
                diskon_nota:
                  type: string
                  description: Desimal. Tidak boleh melebihi subtotal.
                  example: "0"
                ppn: { type: string, example: "0" }
                ppn_dikreditkan:
                  type: boolean
                  default: false
                  description: >
                    `false` berarti PPN ikut masuk harga pokok; `true` berarti jadi
                    pajak masukan dan tidak menyentuh harga pokok.
                pembulatan:
                  type: string
                  description: Boleh negatif — ini baris pembulatan nota.
                  example: "0"
                id_ekspedisi: { type: integer, format: int64, nullable: true, minimum: 1 }
                no_resi: { type: string, nullable: true, maxLength: 64 }
                total_koli: { type: string, nullable: true, example: "3" }
                tarif_per_koli: { type: string, nullable: true, example: "50000" }
                ditanggung_supplier:
                  type: boolean
                  default: false
                  description: >
                    `true` berarti ongkir sudah termasuk nota supplier dan **tidak
                    dialokasikan lagi** — mengalokasikannya berarti rupiah yang sama
                    masuk harga pokok dua kali.
                metode_alokasi_angkut:
                  type: string
                  enum: [KOLI, QTY]
                  default: KOLI
                jenis_pembayaran:
                  type: string
                  enum: [TUNAI, KREDIT]
                  default: TUNAI
                detail:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PembelianDetailInput" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembelian/{id}:
    get:
      tags: [pembelian]
      summary: Detail pembelian beserta barisnya
      operationId: getPembelian
      description: >
        Isu #12 fase 6: dokumen yang ruangnya di luar unit_kerja aktif sesi
        pemanggil menjawab 404, sama seperti id yang tidak ada. Konteks aktif
        global tidak dibatasi.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [pembelian]
      summary: Ubah header pembelian
      operationId: updatePembelian
      description: >
        **Hanya saat `DRAFT`.** Status lain menjawab 409; dokumen yang sudah
        diajukan tertutup untuk diedit, dan itulah gunanya mengajukan.


        `nomor`, `id_supplier`, dan `id_ruang` tidak ada di sini dengan sengaja.
        Nomor mengidentifikasi dokumen, supplier menentukan utangnya milik siapa,
        dan ruang menentukan saldo stok mana yang disentuh setiap barisnya.
        Batalkan lalu input ulang kalau salah satu keliru.


        Total header dihitung ulang setelah patch, karena `diskon_nota`, `ppn`,
        `pembulatan`, `total_koli`, dan `tarif_per_koli` semuanya memberi makan
        angka itu.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tanggal: { type: string, format: date }
                no_faktur_supplier: { type: string, nullable: true, maxLength: 64 }
                tanggal_faktur: { type: string, format: date, nullable: true }
                diskon_nota: { type: string }
                ppn: { type: string }
                ppn_dikreditkan: { type: boolean }
                pembulatan: { type: string }
                id_ekspedisi: { type: integer, format: int64, nullable: true }
                no_resi: { type: string, nullable: true }
                total_koli: { type: string, nullable: true }
                tarif_per_koli: { type: string, nullable: true }
                ditanggung_supplier: { type: boolean }
                metode_alokasi_angkut: { type: string, enum: [KOLI, QTY] }
                jenis_pembayaran: { type: string, enum: [TUNAI, KREDIT] }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembelian/{id}/detail:
    put:
      tags: [pembelian]
      summary: Ganti seluruh baris pembelian
      operationId: replacePembelianDetail
      description: >
        `PUT`, bukan `PATCH`: barisnya diganti sekaligus. Baris satu dokumen adalah
        satu kesatuan yang diketik ulang dari satu lembar kertas, dan edit sebagian
        akan meninggalkan total header berbeda dari barisnya di antara request.


        **Hanya saat `DRAFT`.** Baris yang sudah diposting adalah yang ditunjuk
        `retur_pembelian_detail` dan sumber harga pokok setiap pembalikan, jadi
        menghapusnya akan memutus jejak audit.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [detail]
              properties:
                detail:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PembelianDetailInput" }
      responses:
        "200":
          description: Replaced
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembelian/{id}/bagi-rata-koli:
    post:
      tags: [pembelian]
      summary: Bagi total_koli proporsional ke qty_dasar
      operationId: bagiRataKoliPembelian
      description: >
        Membagi `total_koli` ke seluruh baris sebanding `qty_dasar`, dan hasilnya
        berjumlah **persis** `total_koli`.


        Ada karena posting menolak dokumen yang jumlah kolinya tidak cocok, dan
        mengetik jatah per baris dengan tangan persis jenis aritmetika yang
        menghasilkan dokumen meleset 0,01. Butuh `total_koli` sudah terisi, dan
        hanya berlaku saat `DRAFT`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Dibagi
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembelian/{id}/ajukan:
    post:
      tags: [pembelian]
      summary: Ajukan draft untuk disetujui
      operationId: ajukanPembelian
      description: >
        `DRAFT` → `DIAJUKAN`. Tanpa body: semua yang dibutuhkan sudah ada di
        dokumen. Setelah ini dokumen tertutup untuk diedit, dan jalan keluarnya
        hanya posting atau penolakan. Dokumen tanpa baris ditolak 400.


        Role: `INVENTARIS` atau `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Diajukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembelian/{id}/tolak:
    post:
      tags: [pembelian]
      summary: Tolak pengajuan, kembalikan ke DRAFT
      operationId: tolakPembelian
      description: >
        `DIAJUKAN` → `DRAFT`, dengan `alasan_tolak` terisi dan penanda pengaju
        dikosongkan — pengajuan berikutnya adalah pengajuan baru.


        `alasan` wajib: penolakan tanpa alasan tidak memberi operator apa pun untuk
        diperbaiki, dan catatan itu satu-satunya jalur balik.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan]
              properties:
                alasan: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Ditolak
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembelian/{id}/posting:
    post:
      tags: [pembelian]
      summary: Setujui dan tulis ke kartu stok
      operationId: postingPembelian
      description: >
        `DIAJUKAN` → `POSTED`. Satu transaksi menghitung alokasi ongkir, menulis
        `alokasi_biaya` dan `harga_pokok_satuan_dasar` ke setiap baris, menambah
        satu baris `kartu_stok` per baris yang benar-benar menerima barang, dan
        menutup dokumen.


        **Nilai yang masuk kartu stok proporsional terhadap yang datang, bukan
        nilai faktur penuh.** Faktur 100 pcs senilai 1.050.000 yang datang 95
        masuk sebagai 997.500, bukan 1.050.000. Ini yang paling mudah salah dan
        akibatnya permanen: `kartu_stok` memakai rata-rata bergerak, baris keluar
        mengunci harga pokok yang berlaku saat itu, dan penjualan yang terjadi
        sebelum susulan datang akan membukukan harga pokok yang melambung itu
        secara permanen. Tabelnya append-only — tidak bisa diperbaiki, hanya
        dibalik.


        Baris yang tidak menerima apa pun tidak ditulis ke kartu stok sama sekali.


        Ditolak 400 kalau jumlah `jumlah_koli` seluruh baris tidak sama dengan
        `total_koli`, kalau `diskon_nota` melebihi subtotal, kalau periode dokumen
        berstatus `TUTUP`, atau kalau saldo stok tidak mencukupi. Bulan tanpa baris
        `periode` dihitung terbuka.


        Periode yang tertutup diperiksa lebih dulu di Go supaya pesannya menyebut
        bulannya — "periode 2026-07 sudah TUTUP". Yang menegakkannya tetap trigger
        `kartu_stok`, dan penutupan yang commit di sela pemeriksaan dan penulisan tetap
        tertangkap di sana, hanya dengan pesan yang lebih kabur.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Posted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembelian/{id}/batal:
    post:
      tags: [pembelian]
      summary: Batalkan dokumen POSTED dengan baris pembalik
      operationId: batalPembelian
      description: >
        `POSTED` → `BATAL`. Tidak ada `DELETE` di modul ini: pembatalan menambah
        satu baris `kartu_stok` pembalik untuk setiap pergerakan yang dibuat
        dokumen ini, dengan `id_kartu_stok_asal` menunjuk baris aslinya. Tidak ada
        yang dihapus dan tidak ada yang diubah — `kartu_stok` melarang keduanya
        lewat trigger.


        **Catatan penting soal nilainya:** baris pembalik dinilai pada rata-rata
        bergerak yang berlaku sekarang, bukan pada harga pokok baris aslinya.
        Trigger `kartu_stok_hitung_saldo` menimpa `nilai_keluar` dan
        `harga_pokok_satuan` setiap baris keluar dengan saldo yang berlaku saat
        itu, jadi harga pokok yang dikirim aplikasi diabaikan. Kalau barangnya
        sudah bercampur dengan stok lama dan sebagian terjual, pembalikannya
        mengeluarkan harga pokok campuran. Itu sifat metode rata-rata bergerak,
        bukan jalan pintas di sini.


        **Catatan penting soal tanggalnya:** baris pembalik bertanggal **hari ini**,
        bukan tanggal dokumennya. Akibatnya membatalkan dokumen yang periodenya sudah
        ditutup tetap berhasil — pembaliknya jatuh di periode berjalan yang masih
        terbuka, dan angka bulan tertutupnya tidak bergeser sedikit pun. Itu perlakuan
        akuntansi yang lazim: koreksi atas periode tertutup dibukukan di periode
        berjalan, bukan dengan membongkar yang sudah ditutup, dan sebaliknya akan
        membuat dokumen salah ketik dari bulan tertutup tidak punya jalan keluar sama
        sekali.


        Yang perlu diketahui klien: setelah itu **status dokumen tidak lagi sinkron
        dengan buku bulan tersebut**. Dokumennya `BATAL`, sementara kartu stok bulan
        tertutup masih memuat pergerakannya, diimbangi baris pembalik di bulan lain.
        Laporan per periode harus membaca `kartu_stok`, bukan status dokumen.


        Ditolak 400 kalau stok sudah terpakai sehingga saldo akan negatif, atau kalau
        **periode berjalan** yang sedang `TUTUP` — bukan periode dokumennya. Kalau itu
        terjadi, tidak ada tempat membukukan koreksinya sampai ada yang membuka bulan
        berjalan kembali.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan_batal]
              properties:
                alasan_batal:
                  type: string
                  maxLength: 500
                  description: >
                    Wajib. Pembalikan yang tidak dijelaskan siapa pun tidak bisa
                    dibedakan dari kekeliruan, dan `kartu_stok` menyimpan keduanya
                    selamanya.
      responses:
        "200":
          description: Dibatalkan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembelian/{id}/sisa:
    get:
      tags: [pembelian]
      summary: Baris yang belum lengkap diterima
      operationId: sisaPembelian
      description: >
        Hanya baris yang `qty_diterima_dasar` kurang dari `qty_dasar`. Baris yang
        datang lengkap tidak muncul — ini daftar kerja untuk mengejar susulan lewat
        WhatsApp, dan baris yang lengkap tidak ada di dalamnya.


        Isu #12 fase 6: penyaringan yang sama dengan `GET /pembelian/{id}` —
        dokumen di luar unit_kerja aktif menjawab 404.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/SisaPembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-susulan:
    get:
      tags: [penerimaan-susulan]
      summary: List penerimaan susulan
      operationId: listPenerimaanSusulan
      description: >
        Tidak menyertakan `detail`; kuncinya hilang sama sekali, bukan array kosong.
        `search` mencocokkan nomor dokumen ini maupun nomor pembelian asalnya.


        Isu #12 fase 6: dokumen yang ruang pembelian asalnya di luar unit_kerja
        aktif sesi pemanggil dilewati begitu saja, tanpa error.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, DIAJUKAN, POSTED, BATAL] }
        - name: id_pembelian
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: tanggal_dari
          in: query
          schema: { type: string, format: date }
        - name: tanggal_sampai
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/PenerimaanSusulan" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [penerimaan-susulan]
      summary: Buat draft penerimaan susulan
      operationId: createPenerimaanSusulan
      description: >
        Kiriman kedua untuk barang yang belum datang di kiriman pertama. **Menambah
        stok, tidak menambah utang** — fakturnya terbit penuh di kiriman pertama.


        Bentuknya cermin dari retur pembelian: sama-sama menunjuk baris
        `pembelian_detail`, arah barangnya saja yang berlawanan. Menaikkan
        `qty_diterima` pada baris yang sudah diposting bukan pilihan — itu melanggar
        imutabilitas dokumen POSTED, membuat pembatalan mustahil diaudit, dan
        menghapus jejak kapan susulan benar-benar datang.


        **Pembelian asalnya harus `POSTED`.** Sebelum itu barisnya belum punya
        `harga_pokok_satuan_dasar` untuk disalin dan belum punya sisa yang pasti.


        `id_supplier` dan `id_ruang` tidak ada di body: keduanya disalin dari
        pembelian. Suppliernya sudah tertentu, dan ruangnya sudah ditetapkan dokumen
        asal — barang yang perlu pindah ruang setelah diterima adalah pekerjaan
        mutasi.


        Nomor digenerate server dengan seri sendiri, `PS/2026/08/0001`.


        Setiap baris tidak boleh melebihi sisa
        (`qty_dasar − qty_diterima_dasar − Σ susulan POSTED`). Pengecekan yang
        menentukan terjadi saat posting, di bawah row lock pembelian; yang di sini
        hanya memberi error lebih cepat.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id_pembelian, tanggal, detail]
              properties:
                id_pembelian: { type: integer, format: int64, minimum: 1 }
                tanggal: { type: string, format: date, example: "2026-08-20" }
                keterangan: { type: string, nullable: true, maxLength: 1000 }
                detail:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PenerimaanSusulanDetailInput" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanSusulan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-susulan/{id}:
    get:
      tags: [penerimaan-susulan]
      summary: Detail penerimaan susulan beserta barisnya
      operationId: getPenerimaanSusulan
      description: >
        Isu #12 fase 6: dokumen yang ruang pembelian asalnya di luar
        unit_kerja aktif sesi pemanggil menjawab 404, sama seperti id yang
        tidak ada.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanSusulan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [penerimaan-susulan]
      summary: Ubah header penerimaan susulan
      operationId: updatePenerimaanSusulan
      description: >
        **Hanya saat `DRAFT`.** Hanya dua field yang benar-benar milik operator yang
        bisa diubah; `id_pembelian`, `id_supplier`, dan `id_ruang` menentukan dokumen
        ini sisa dari apa dan tidak ada di sini.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tanggal: { type: string, format: date }
                keterangan: { type: string, nullable: true, maxLength: 1000 }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanSusulan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-susulan/{id}/detail:
    put:
      tags: [penerimaan-susulan]
      summary: Ganti seluruh baris penerimaan susulan
      operationId: replacePenerimaanSusulanDetail
      description: >
        `PUT`, bukan `PATCH`: barisnya diganti sekaligus, karena baris satu kiriman
        adalah satu kesatuan yang dihitung bersamaan. **Hanya saat `DRAFT`.**
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [detail]
              properties:
                detail:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PenerimaanSusulanDetailInput" }
      responses:
        "200":
          description: Replaced
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanSusulan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-susulan/{id}/ajukan:
    post:
      tags: [penerimaan-susulan]
      summary: Ajukan draft untuk disetujui
      operationId: ajukanPenerimaanSusulan
      description: >
        `DRAFT` → `DIAJUKAN`. Tanpa body. Dokumen tanpa baris ditolak 400.
        Role: `INVENTARIS` atau `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Diajukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanSusulan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-susulan/{id}/tolak:
    post:
      tags: [penerimaan-susulan]
      summary: Tolak pengajuan, kembalikan ke DRAFT
      operationId: tolakPenerimaanSusulan
      description: "`DIAJUKAN` → `DRAFT`, `alasan` wajib. Role: `SUPERADMIN`."
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan]
              properties:
                alasan: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Ditolak
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanSusulan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-susulan/{id}/posting:
    post:
      tags: [penerimaan-susulan]
      summary: Setujui dan tulis ke kartu stok
      operationId: postingPenerimaanSusulan
      description: >
        `DIAJUKAN` → `POSTED`. Satu transaksi menulis satu baris `kartu_stok` per
        baris dokumen dengan `jenis_transaksi = PENERIMAAN_SUSULAN`, lalu menghitung
        ulang `pembelian.status_penerimaan`.


        Nilai yang masuk disalin dari `pembelian_detail.harga_pokok_satuan_dasar`,
        bukan dihitung ulang dan bukan dibaca dari rata-rata terkini: harga barang
        sudah ditetapkan faktur, dan kiriman keduanya tidak mengubahnya. Karena itu
        pembelian dan seluruh susulannya berjumlah **persis** nilai faktur.


        Sisa per baris diperiksa lagi di sini, di bawah row lock pembelian — dan itu
        pemeriksaan yang menentukan. Di antara draft ditulis dan diposting, susulan
        lain untuk pembelian yang sama bisa lebih dulu menghabiskan sisanya.


        Ditolak 400 kalau periode dokumen berstatus `TUTUP`; pesannya menyebut bulannya.
        Bulan tanpa baris `periode` dihitung terbuka.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Posted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanSusulan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-susulan/{id}/batal:
    post:
      tags: [penerimaan-susulan]
      summary: Batalkan dokumen POSTED dengan baris pembalik
      operationId: batalPenerimaanSusulan
      description: >
        `POSTED` → `BATAL`. Menambah satu baris `kartu_stok` pembalik untuk setiap
        pergerakan yang dibuat dokumen ini, lalu mengembalikan sisanya ke pembelian
        dengan menghitung ulang `status_penerimaan`.


        Berlaku dua catatan yang sama seperti pembatalan pembelian. Baris pembalik
        dinilai pada rata-rata bergerak yang berlaku sekarang, bukan pada harga pokok
        baris aslinya, karena trigger menimpa harga pokok setiap baris keluar. Dan
        bertanggal hari ini, jadi dokumen yang periodenya sudah ditutup tetap bisa
        dibatalkan — koreksinya masuk periode berjalan. Yang bisa menghalangi justru
        periode **berjalan** yang sedang `TUTUP`.


        **Membatalkan pembelian yang punya susulan POSTED ditolak 409** — batalkan
        susulannya lebih dulu. Pembatalan pembelian hanya membalik baris yang ditulis
        pembelian itu sendiri, jadi stok susulannya akan tertinggal tanpa dokumen yang
        menjelaskannya, dan setelah itu tidak bisa dibalik lagi karena jalur
        pembatalannya menuntut pembelian yang masih POSTED.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan_batal]
              properties:
                alasan_batal: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Dibatalkan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanSusulan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/retur-pembelian:
    get:
      tags: [retur-pembelian]
      summary: List retur pembelian
      operationId: listReturPembelian
      description: >
        Tidak menyertakan `detail`; kuncinya hilang sama sekali, bukan array kosong.
        `search` mencocokkan nomor dokumen ini maupun nomor pembelian asalnya.


        Isu #12 fase 6: dokumen yang ruang pembelian asalnya di luar unit_kerja
        aktif sesi pemanggil dilewati begitu saja, tanpa error.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, DIAJUKAN, POSTED, BATAL] }
        - name: id_pembelian
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: id_supplier
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: tanggal_dari
          in: query
          schema: { type: string, format: date }
        - name: tanggal_sampai
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/ReturPembelian" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [retur-pembelian]
      summary: Buat draft retur pembelian
      operationId: createReturPembelian
      description: >
        Barang yang dikembalikan ke supplier. Cermin dari penerimaan susulan:
        sama-sama menunjuk baris `pembelian_detail` dan sama-sama menyalin harga
        pokoknya dari sana, arah barangnya saja yang berlawanan.


        **Pembelian asalnya harus `POSTED`.** Sebelum itu barisnya belum punya
        `harga_pokok_satuan_dasar` untuk disalin, dan belum ada barang yang datang
        untuk dikirim balik.


        `id_supplier` dan `id_ruang` tidak ada di body: keduanya disalin dari
        pembelian. Suppliernya adalah yang menjual barangnya, dan ruangnya adalah
        tempat pembelian itu menaruhnya — barang yang perlu dikembalikan dari ruang
        lain adalah pekerjaan mutasi lebih dulu.


        Nomor digenerate server dengan seri sendiri, `RB/2026/08/0001`.


        Setiap baris tidak boleh melebihi yang bisa diretur
        (`qty_diterima_dasar + Σ susulan POSTED − Σ retur POSTED`). Perhatikan
        kuantitas yang **tidak** ikut: `qty_dasar`. Yang difakturkan adalah yang
        ditagih supplier, dan barang yang tidak pernah datang tidak bisa dikirim
        balik — kekurangan kiriman dikejar dengan penerimaan susulan, bukan retur.


        Pengecekan yang menentukan terjadi saat posting, di bawah row lock pembelian;
        yang di sini hanya memberi error lebih cepat.


        `alasan` wajib meski kolomnya nullable. Ia satu-satunya catatan kenapa barang
        yang sudah dibayar dikirim balik, dan itu yang dibacakan ke supplier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id_pembelian, tanggal, alasan, detail]
              properties:
                id_pembelian: { type: integer, format: int64, minimum: 1 }
                tanggal: { type: string, format: date, example: "2026-08-15" }
                alasan: { type: string, maxLength: 1000, example: barang rusak saat diterima }
                detail:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { $ref: "#/components/schemas/ReturPembelianDetailInput" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ReturPembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/retur-pembelian/{id}:
    get:
      tags: [retur-pembelian]
      summary: Detail retur pembelian beserta barisnya
      operationId: getReturPembelian
      description: >
        Isu #12 fase 6: dokumen yang ruang pembelian asalnya di luar
        unit_kerja aktif sesi pemanggil menjawab 404, sama seperti id yang
        tidak ada.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ReturPembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [retur-pembelian]
      summary: Ubah header retur pembelian
      operationId: updateReturPembelian
      description: >
        **Hanya saat `DRAFT`.** Hanya dua field yang benar-benar milik operator yang
        bisa diubah; `id_pembelian`, `id_supplier`, dan `id_ruang` menentukan dokumen
        ini retur atas apa dan tidak ada di sini.


        `alasan: null` ditolak 400 walaupun kolomnya nullable: jalur create menuntut
        alasan, jadi membiarkan patch mengosongkannya akan menghasilkan dokumen yang
        tidak mungkin dibuat lewat create.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tanggal: { type: string, format: date }
                alasan: { type: string, maxLength: 1000 }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ReturPembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/retur-pembelian/{id}/detail:
    put:
      tags: [retur-pembelian]
      summary: Ganti seluruh baris retur pembelian
      operationId: replaceReturPembelianDetail
      description: >
        `PUT`, bukan `PATCH`: barisnya diganti sekaligus, karena baris satu kiriman
        balik adalah satu kesatuan yang dikemas dan dihitung bersamaan.
        **Hanya saat `DRAFT`.**
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [detail]
              properties:
                detail:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { $ref: "#/components/schemas/ReturPembelianDetailInput" }
      responses:
        "200":
          description: Replaced
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ReturPembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/retur-pembelian/{id}/ajukan:
    post:
      tags: [retur-pembelian]
      summary: Ajukan draft untuk disetujui
      operationId: ajukanReturPembelian
      description: >
        `DRAFT` → `DIAJUKAN`. Tanpa body. Dokumen tanpa baris ditolak 400.
        Role: `INVENTARIS` atau `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Diajukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ReturPembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/retur-pembelian/{id}/tolak:
    post:
      tags: [retur-pembelian]
      summary: Tolak pengajuan, kembalikan ke DRAFT
      operationId: tolakReturPembelian
      description: "`DIAJUKAN` → `DRAFT`, `alasan` wajib. Role: `SUPERADMIN`."
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan]
              properties:
                alasan: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Ditolak
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ReturPembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/retur-pembelian/{id}/posting:
    post:
      tags: [retur-pembelian]
      summary: Setujui dan keluarkan dari kartu stok
      operationId: postingReturPembelian
      description: >
        `DIAJUKAN` → `POSTED`. Satu transaksi menulis satu baris `kartu_stok` per baris
        dokumen dengan `jenis_transaksi = RETUR_PEMBELIAN` dan `stok_keluar` terisi.


        **`total` dokumen ini dan nilai yang dicatat kartu stok tidak selalu sama, dan
        itu bukan bug.** `total` adalah nilai barang menurut faktur, dihitung dari
        harga pokok yang disalin dari baris pembelian — itu yang membuat pembelian dan
        returnya saling menghapus. Sementara `kartu_stok` menilai setiap baris keluar
        pada rata-rata bergerak yang berlaku saat itu, karena barangnya sudah tercampur
        dengan stok lama sejak ia datang dan tidak ada lagi batch yang bisa dipisahkan.


        `pembelian.status_penerimaan` sengaja **tidak** dihitung ulang: mengirim barang
        balik tidak membuat kiriman jadi tidak lengkap, dan tidak membuat supplier
        berutang barang lagi.


        Yang bisa diretur diperiksa lagi di sini, di bawah row lock pembelian — dan itu
        pemeriksaan yang menentukan. Di antara draft ditulis dan diposting, retur lain
        untuk pembelian yang sama bisa lebih dulu menghabiskannya.


        Ditolak 400 kalau kuantitasnya melebihi yang bisa diretur, kalau saldo stok di
        ruang itu tidak mencukupi, atau kalau periode dokumen berstatus `TUTUP`. Bulan
        tanpa baris `periode` dihitung terbuka, dan periode tertutup diperiksa lebih
        dulu di Go supaya pesannya menyebut bulannya.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Posted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ReturPembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/retur-pembelian/{id}/batal:
    post:
      tags: [retur-pembelian]
      summary: Batalkan dokumen POSTED dengan baris pembalik
      operationId: batalReturPembelian
      description: >
        `POSTED` → `BATAL`. Menambah satu baris `kartu_stok` pembalik untuk setiap
        pergerakan yang dibuat dokumen ini: barangnya kembali masuk, dinilai persis
        sebesar yang tercatat saat ia keluar — satu-satunya angka yang membuat
        pasangannya berjumlah nol.


        Seperti setiap pembalikan di sistem ini, barisnya bertanggal hari ini. Retur
        yang periodenya sudah ditutup tetap bisa dibatalkan — pembaliknya masuk periode
        berjalan — dan yang bisa menghalangi justru periode **berjalan** yang sedang
        `TUTUP`.


        **Membatalkan pembelian yang punya retur POSTED ditolak 409** — batalkan
        returnya lebih dulu. Pembatalan pembelian membalik seluruh kuantitas yang
        diterima, sementara returnya sudah mengeluarkan sebagiannya, jadi pembalikannya
        akan menekan saldo di bawah nol; dan kalaupun saldonya cukup, returnya akan
        tertinggal menunjuk pembelian `BATAL` yang pembalikannya sudah memperhitungkan
        barang yang sama — kekurangan yang sama dihitung dua kali.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan_batal]
              properties:
                alasan_batal: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Dibatalkan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ReturPembelian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/mutasi:
    get:
      tags: [mutasi]
      summary: List mutasi antar ruang
      operationId: listMutasi
      description: >
        Tidak menyertakan `detail`; kuncinya hilang sama sekali, bukan array kosong.
        `search` mencocokkan nomor dokumen maupun keterangannya.


        `status=DRAFT` dengan `terlama_dulu=true` adalah **antrean kerja
        `SUPERADMIN`**. Karena mutasi tidak punya `DIAJUKAN`, tidak ada sinyal lain
        bahwa sebuah draft sudah siap diposting, dan daftar inilah penggantinya —
        diurutkan paling lama dulu, seperti `GET /supplier/{id}/utang`, karena antrean
        dibaca untuk dikerjakan, bukan untuk melihat yang terbaru.


        Isu #12 fase 6: dokumen yang `id_ruang_asal`-nya di luar unit_kerja
        aktif sesi pemanggil dilewati begitu saja, tanpa error —
        `id_ruang_tujuan` tidak pernah diperiksa, mengikuti aturan yang sama
        dengan validasi tulis di fase 5.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, POSTED, BATAL] }
        - name: id_ruang_asal
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: id_ruang_tujuan
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: tanggal_dari
          in: query
          schema: { type: string, format: date }
        - name: tanggal_sampai
          in: query
          schema: { type: string, format: date }
        - name: terlama_dulu
          in: query
          description: Urutkan paling lama dulu, untuk dibaca sebagai antrean.
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Mutasi" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [mutasi]
      summary: Buat draft mutasi
      operationId: createMutasi
      description: >
        Nomor diambil dari `document_counter` dengan prefix `MT`, direset per bulan dan
        dikunci pada tanggal dokumennya.


        `detail` boleh kosong di sini — mutasi biasanya dibuka saat troli masih dimuat —
        tetapi posting menolak dokumen tanpa baris. Produk yang sama boleh muncul lebih
        dari sekali, tidak seperti `penerimaan-susulan` dan `retur-pembelian`: di sana
        kuotanya per baris dokumen induk, di sini kuotanya saldo ruang asal dan seluruh
        baris dijumlahkan terhadapnya.


        `id_ruang_asal` harus berbeda dari `id_ruang_tujuan` (`mutasi_ruang_check`).


        Tidak ada angka uang sama sekali di dokumen ini. Harga pokoknya baru terisi saat
        posting, dari rata-rata bergerak ruang asal yang hanya diketahui trigger
        `kartu_stok`.


        **Isu #12 fase 5:** `id_ruang_asal` divalidasi terhadap unit_kerja aktif
        sesi pemanggil, kalau tidak → 403. `id_ruang_tujuan` **tidak pernah**
        dibatasi — mutasi antar unit sengaja diperbolehkan (isu #12 fase 1); yang
        diperiksa hanya wewenang atas ruang tempat barang dinyatakan keluar.


        Role: `INVENTARIS`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tanggal, id_ruang_asal, id_ruang_tujuan]
              properties:
                tanggal: { type: string, format: date }
                id_ruang_asal: { type: integer, format: int64, minimum: 1 }
                id_ruang_tujuan: { type: integer, format: int64, minimum: 1 }
                keterangan: { type: string, maxLength: 1000 }
                detail:
                  type: array
                  maxItems: 500
                  items: { $ref: "#/components/schemas/MutasiDetailInput" }
      responses:
        "201":
          description: Draft dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Mutasi" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/mutasi/{id}:
    get:
      tags: [mutasi]
      summary: Detail mutasi beserta barisnya
      operationId: getMutasi
      description: >
        Isu #12 fase 6: dokumen yang `id_ruang_asal`-nya di luar unit_kerja
        aktif sesi pemanggil menjawab 404, sama seperti id yang tidak ada.
        `id_ruang_tujuan` tidak pernah diperiksa — dokumen yang tujuannya di
        unit lain tetap terlihat selama `id_ruang_asal`-nya cocok.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Mutasi" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [mutasi]
      summary: Ubah header mutasi
      operationId: updateMutasi
      description: >
        **Hanya saat `DRAFT`.** Kedua ruang boleh diubah, berbeda dengan
        `pembayaran-utang` yang mengeluarkan `id_supplier` dari DTO update-nya: di sana
        alokasi menunjuk faktur supplier tertentu, sedangkan di sini tidak ada baris
        detail yang menunjuk ruang — ruangnya hanya ada di header, jadi tidak ada yang
        tertinggal salah.


        Ruang asal dan tujuan tetap tidak boleh sama, dan itu diperiksa terhadap baris
        yang tersimpan: mengubah salah satunya saja bisa bertabrakan dengan yang sudah
        ada di sana.


        **Isu #12 fase 5:** kalau `id_ruang_asal` dikirim, nilai barunya divalidasi
        terhadap unit_kerja aktif sesi pemanggil, kalau tidak → 403. Tidak dikirim
        berarti tidak diperiksa ulang — ruang tersimpan sudah pasti valid, karena
        `ruang` sendiri tidak punya `PATCH` sehingga unitnya tidak bisa berubah.
        `id_ruang_tujuan` tidak pernah dibatasi, alasannya sama seperti di `POST`.


        Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tanggal: { type: string, format: date }
                id_ruang_asal: { type: integer, format: int64, minimum: 1 }
                id_ruang_tujuan: { type: integer, format: int64, minimum: 1 }
                keterangan: { type: string, maxLength: 1000, nullable: true }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Mutasi" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/mutasi/{id}/detail:
    put:
      tags: [mutasi]
      summary: Ganti seluruh baris mutasi
      operationId: replaceMutasiDetail
      description: >
        **Hanya saat `DRAFT`.** Barisnya diganti sekaligus, bukan satu per satu: baris
        satu mutasi adalah satu hal yang dihitung dan dimuat bersama, dan penyuntingan
        sebagian meninggalkan dokumen yang tidak konsisten dengan dirinya sendiri di
        antara dua request.


        Berbeda dengan `POST /api/v1/mutasi`, di sini `detail` wajib berisi minimal satu
        baris — mengosongkan dokumen lewat jalur yang berarti "inilah barisnya" adalah
        permintaan yang lebih baik ditolak.


        Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [detail]
              properties:
                detail:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { $ref: "#/components/schemas/MutasiDetailInput" }
      responses:
        "200":
          description: Baris diganti
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Mutasi" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/mutasi/{id}/posting:
    post:
      tags: [mutasi]
      summary: Posting mutasi ke kartu stok
      operationId: postingMutasi
      description: >
        `DRAFT` → `POSTED`, langsung tanpa `DIAJUKAN`. Menulis **dua** baris
        `kartu_stok` per baris detail: `MUTASI_KELUAR` di ruang asal lalu `MUTASI_MASUK`
        di ruang tujuan, dalam satu transaksi.


        Urutan kedua insert itu terpaksa, dan keterpaksaannya yang membuat modul ini
        benar. Nilai yang masuk ke ruang tujuan adalah **persis** `nilai_keluar` yang
        dikembalikan baris keluar — bukan angka yang dihitung ulang, bukan pula harga
        pokok yang diketik saat draft. Rata-rata bergerak ruang asal hanya diketahui
        trigger, yang membacanya di dalam advisory lock, dan trigger juga menimpa
        `nilai_keluar` pada setiap baris keluar. Hasilnya: Σ `nilai_akhir` seluruh ruang
        tidak bergerak sedikit pun.


        `mutasi_detail.harga_pokok_satuan_dasar` ikut terisi di sini, dari
        `harga_pokok_satuan` yang dilaporkan baris keluar.


        Saldo ruang asal yang tidak mencukupi ditolak 400 dengan pesan yang menyebut
        produk dan kedua angkanya; periode yang sudah `TUTUP` ditolak 400 dengan pesan
        yang menyebut bulannya. Keduanya dijaga trigger — pemeriksaan di aplikasi hanya
        untuk kalimatnya.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Diposting
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Mutasi" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/mutasi/{id}/batal:
    post:
      tags: [mutasi]
      summary: Batalkan mutasi POSTED dengan baris pembalik
      operationId: batalMutasi
      description: >
        `POSTED` → `BATAL`. Menambah baris pembalik untuk setiap pergerakan yang dibuat
        dokumen ini: barangnya masuk lagi ke ruang asal dan keluar dari ruang tujuan.


        **Pembatalannya tidak simetris nilainya, dan itu tidak bisa diperbaiki.** Baris
        yang keluar dari ruang tujuan dinilai dengan rata-rata bergerak ruang tujuan
        saat ini, yang bisa sudah bergeser karena barang lain masuk ke sana. Jadi mutasi
        dan pembatalannya selalu berjumlah nol dalam kuantitas, tetapi tidak selalu
        dalam nilai. Keterbatasan yang sama sudah tercatat untuk `pembelian`: trigger
        yang memiliki harga pokok setiap baris keluar, jadi menyalin nilai aslinya
        memang tidak tersedia.


        **Pembatalannya juga bisa ditolak 400.** Kalau barangnya sudah keluar lagi dari
        ruang tujuan, baris pembaliknya menyeret saldo ruang itu ke negatif dan trigger
        menolaknya. Itu memang benar: barang yang sudah terjual dari toko tidak bisa
        dikembalikan ke gudang lewat pembatalan — obatnya mutasi lagi.


        Barisnya bertanggal hari ini, seperti setiap pembalikan di sistem ini. Mutasi
        yang periodenya sudah ditutup tetap bisa dibatalkan — pembaliknya masuk periode
        berjalan — dan yang menghalangi justru periode **berjalan** yang sedang `TUTUP`.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan_batal]
              properties:
                alasan_batal: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Dibatalkan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Mutasi" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pemakaian:
    get:
      tags: [pemakaian]
      summary: List permintaan pemakaian internal
      operationId: listPemakaian
      description: >
        Tidak menyertakan `detail`; kuncinya hilang sama sekali, bukan array kosong.
        `search` mencocokkan nomor dokumen maupun keperluannya.


        `status=DIAJUKAN` dengan `terlama_dulu=true` adalah **antrean persetujuan**
        — diurutkan paling lama dulu, seperti `GET /supplier/{id}/utang`, karena
        antrean dibaca untuk dikerjakan.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
        - name: status
          in: query
          schema:
            type: string
            enum: [DRAFT, DIAJUKAN, DISETUJUI, DITOLAK, POSTED, BATAL]
        - name: id_ruang
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: id_pemohon
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: tanggal_dari
          in: query
          schema: { type: string, format: date }
        - name: tanggal_sampai
          in: query
          schema: { type: string, format: date }
        - name: terlama_dulu
          in: query
          description: Urutkan paling lama dulu, untuk dibaca sebagai antrean.
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Pemakaian" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [pemakaian]
      summary: Buat draft permintaan pemakaian
      operationId: createPemakaian
      description: >
        Nomor diambil dari `document_counter` dengan prefix `PM`, direset per bulan
        dan dikunci pada tanggal dokumennya.


        `detail` boleh kosong di sini — permintaan biasanya dibuka saat daftarnya
        masih disusun — tetapi mengajukan menolak dokumen tanpa baris. Produk yang
        sama boleh muncul lebih dari sekali, mengikuti `mutasi`: kuotanya saldo
        ruang, dijumlahkan per produk saat posting.


        Role: `INVENTARIS`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tanggal, id_ruang, id_pemohon, keperluan]
              properties:
                tanggal: { type: string, format: date }
                id_ruang: { type: integer, format: int64, minimum: 1 }
                id_pemohon: { type: integer, format: int64, minimum: 1 }
                keperluan: { type: string, maxLength: 1000 }
                detail:
                  type: array
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PemakaianDetailInput" }
      responses:
        "201":
          description: Draft dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pemakaian" }
        "400": { $ref: "#/components/responses/ValidationError" }

  /api/v1/pemakaian/{id}:
    get:
      tags: [pemakaian]
      summary: Detail pemakaian beserta barisnya
      operationId: getPemakaian
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pemakaian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [pemakaian]
      summary: Ubah header pemakaian
      operationId: updatePemakaian
      description: >
        **Hanya saat `DRAFT`.** Keempat kolomnya adalah kolom `NOT NULL`: boleh
        diubah, tidak boleh dikosongkan — `null` eksplisit ditolak 400.


        Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tanggal: { type: string, format: date }
                id_ruang: { type: integer, format: int64, minimum: 1 }
                id_pemohon: { type: integer, format: int64, minimum: 1 }
                keperluan: { type: string, maxLength: 1000 }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pemakaian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pemakaian/{id}/detail:
    put:
      tags: [pemakaian]
      summary: Ganti seluruh baris pemakaian
      operationId: replacePemakaianDetail
      description: >
        **Hanya saat `DRAFT`.** Barisnya diganti sekaligus, bukan satu per satu,
        seperti setiap dokumen lain di API ini. `detail` wajib berisi minimal satu
        baris di sini, tidak seperti `POST /api/v1/pemakaian`.


        Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [detail]
              properties:
                detail:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PemakaianDetailInput" }
      responses:
        "200":
          description: Baris diganti
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pemakaian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pemakaian/{id}/ajukan:
    post:
      tags: [pemakaian]
      summary: Ajukan draft pemakaian untuk disetujui
      operationId: ajukanPemakaian
      description: >
        `DRAFT` → `DIAJUKAN`. Menolak dokumen tanpa baris. Tidak membawa body —
        semua yang dibutuhkan sudah ada di dokumennya.


        Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Diajukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pemakaian" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pemakaian/{id}/setujui:
    post:
      tags: [pemakaian]
      summary: Setujui pemakaian dan tentukan qty per baris
      operationId: setujuiPemakaian
      description: >
        `DIAJUKAN` → `DISETUJUI`. `pemakaian_penyetuju_check` menolak penyetuju yang
        sama dengan `id_pemohon` — ditangkap lebih dulu di usecase supaya pesannya
        menyebutkan sebabnya.


        `detail` bersifat opsional per baris: baris yang tidak disebut memakai
        `qty_dasar`-nya sendiri secara penuh (kasus biasa, semua yang diminta
        diberikan). Menyebut sebuah baris adalah caranya memangkas, dan 0 berarti
        baris itu ditolak sendirian meski dokumennya disetujui.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                catatan: { type: string, maxLength: 1000 }
                detail:
                  type: array
                  maxItems: 500
                  items: { $ref: "#/components/schemas/SetujuiPemakaianDetailInput" }
      responses:
        "200":
          description: Disetujui
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pemakaian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pemakaian/{id}/tolak:
    post:
      tags: [pemakaian]
      summary: Tolak pemakaian
      operationId: tolakPemakaian
      description: >
        `DIAJUKAN` → `DITOLAK`. **Terminal, tidak kembali ke `DRAFT`** — berbeda
        dari `pembelian`. Penolakan di sini adalah keputusan bisnis (barangnya tidak
        diberikan), bukan kertas yang salah ketik; kalau pemohon masih menginginkan
        barangnya, ia mengajukan permintaan baru. Alasannya masuk
        `catatan_persetujuan`, kolom yang sama dipakai persetujuan.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan]
              properties:
                alasan: { type: string, maxLength: 1000 }
      responses:
        "200":
          description: Ditolak
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pemakaian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pemakaian/{id}/posting:
    post:
      tags: [pemakaian]
      summary: Posting pemakaian ke kartu stok
      operationId: postingPemakaian
      description: >
        `DISETUJUI` → `POSTED`. Yang ditulis ke `kartu_stok` adalah
        `qty_disetujui_dasar`, **bukan** `qty_dasar` — jebakan paling mahal di modul
        ini. Baris dengan `qty_disetujui_dasar` nol dilewati begitu saja, tidak
        ditulis sebagai baris bernilai nol; kalau semua baris nol, posting ditolak
        400 sepenuhnya.


        `hpp_satuan_dasar` dan `hpp_total` tiap baris, serta `total_hpp` header,
        terisi di sini dari `harga_pokok_satuan`/`nilai_keluar` yang dilaporkan
        setiap baris `kartu_stok` — aplikasi tidak menghitungnya sendiri.


        Saldo ruang yang tidak mencukupi ditolak 400 dengan pesan yang menyebut
        produk dan ruangnya; periode yang sudah `TUTUP` ditolak 400 dengan pesan
        yang menyebut bulannya. Keduanya dijaga trigger — pemeriksaan di aplikasi
        hanya untuk kalimatnya.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Diposting
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pemakaian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pemakaian/{id}/batal:
    post:
      tags: [pemakaian]
      summary: Batalkan pemakaian POSTED dengan baris pembalik
      operationId: batalPemakaian
      description: >
        `POSTED` → `BATAL`. Menambah baris pembalik `PEMBATALAN_TRANSAKSI` untuk
        setiap baris `kartu_stok` yang ditulis dokumen ini — barangnya masuk lagi ke
        ruang. Karena pembatalan hanya menambah stok, tidak pernah bisa menyeret
        saldo ke negatif.


        Barisnya bertanggal hari ini, seperti setiap pembalikan di sistem ini.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan_batal]
              properties:
                alasan_batal: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Dibatalkan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pemakaian" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penjualan:
    get:
      tags: [penjualan]
      summary: List nota penjualan
      operationId: listPenjualan
      description: >
        Tidak menyertakan `detail`; kuncinya hilang sama sekali, bukan array kosong.
        `search` mencocokkan nomor dokumen.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, POSTED, BATAL] }
        - name: status_pembayaran
          in: query
          schema: { type: string, enum: [BELUM, SEBAGIAN, LUNAS] }
        - name: jenis_pembayaran
          in: query
          schema: { type: string, enum: [TUNAI, KREDIT] }
        - name: id_ruang
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: id_pelanggan
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: tanggal_dari
          in: query
          schema: { type: string, format: date }
        - name: tanggal_sampai
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Penjualan" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [penjualan]
      summary: Buat draft nota penjualan
      operationId: createPenjualan
      description: >
        Nomor diambil dari `document_counter` dengan prefix `PJ`, direset per bulan
        dan dikunci pada tanggal dokumennya.


        `detail` boleh kosong di sini — nota biasanya dibuka sambil barang masih
        dipindai — tetapi posting menolak dokumen tanpa baris. Produk yang sama
        boleh muncul lebih dari sekali, mengikuti `mutasi`/`pemakaian`: kuotanya
        saldo ruang, dijumlahkan seluruh baris terhadapnya.


        `jenis_pembayaran` default `TUNAI`. **`KREDIT` mewajibkan `id_pelanggan`**
        (`penjualan_kredit_pelanggan_check`) — nota tunai di depan meja tidak perlu
        pelanggan terdaftar.


        `subtotal` adalah jumlah `subtotal` setiap baris (`qty_input x
        harga_satuan_input - diskon_baris`); `total = subtotal - diskon_nota +
        pembulatan`. `diskon_nota` tidak boleh melebihi `subtotal`, dan `total`
        tidak boleh negatif. `pembulatan` boleh negatif — itu baris pembulatan
        nota, bukan galat.


        `harga_pokok_satuan_dasar`/`hpp_total`/`total_hpp` baru terisi saat
        posting, dari apa yang benar-benar dicatat `kartu_stok` — bukan dihitung
        di sini.


        Role: `CASHIER`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tanggal, id_ruang]
              properties:
                tanggal: { type: string, format: date }
                id_ruang: { type: integer, format: int64, minimum: 1 }
                id_pelanggan:
                  type: integer
                  format: int64
                  minimum: 1
                  nullable: true
                  description: Wajib diisi kalau `jenis_pembayaran` KREDIT.
                jenis_pembayaran:
                  type: string
                  enum: [TUNAI, KREDIT]
                  default: TUNAI
                diskon_nota: { type: string, example: "0" }
                pembulatan: { type: string, example: "0" }
                detail:
                  type: array
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PenjualanDetailInput" }
      responses:
        "201":
          description: Draft dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Penjualan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penjualan/{id}:
    get:
      tags: [penjualan]
      summary: Detail nota penjualan beserta barisnya
      operationId: getPenjualan
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Penjualan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [penjualan]
      summary: Ubah header nota penjualan
      operationId: updatePenjualan
      description: >
        **Hanya saat `DRAFT`.** `id_ruang`, `id_pelanggan`, dan `jenis_pembayaran`
        boleh semua diubah — tidak ada baris detail yang menunjuk salah satunya,
        jadi tidak ada yang tertinggal salah. `nomor`, `total_hpp`, dan
        `status_pembayaran` tidak pernah ada di sini: yang pertama identitas, dua
        lainnya turunan.


        Aturan KREDIT-wajib-pelanggan diperiksa ulang terhadap nilai efektif
        setelah patch, bukan hanya field yang dikirim — mengirim
        `jenis_pembayaran: KREDIT` saat `id_pelanggan` tersimpan masih kosong tetap
        ditolak.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                tanggal: { type: string, format: date }
                id_ruang: { type: integer, format: int64, minimum: 1 }
                id_pelanggan: { type: integer, format: int64, minimum: 1, nullable: true }
                jenis_pembayaran: { type: string, enum: [TUNAI, KREDIT] }
                diskon_nota: { type: string }
                pembulatan: { type: string }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Penjualan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penjualan/{id}/detail:
    put:
      tags: [penjualan]
      summary: Ganti seluruh baris nota penjualan
      operationId: replacePenjualanDetail
      description: >
        **Hanya saat `DRAFT`.** Barisnya diganti sekaligus, bukan satu per satu, dan
        `subtotal`/`total` dihitung ulang dari baris yang baru.


        Berbeda dengan `POST /api/v1/penjualan`, di sini `detail` wajib berisi
        minimal satu baris.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [detail]
              properties:
                detail:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PenjualanDetailInput" }
      responses:
        "200":
          description: Baris diganti
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Penjualan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penjualan/{id}/posting:
    post:
      tags: [penjualan]
      summary: Posting nota ke kartu stok
      operationId: postingPenjualan
      description: >
        `DRAFT` → `POSTED`, langsung tanpa `DIAJUKAN` — kasir tidak bisa membuat
        pembeli menunggu persetujuan atasan untuk nota tunai. HPP setiap baris dan
        `total_hpp` diisi di sini, dari `harga_pokok_satuan`/`nilai_keluar` yang
        dilaporkan `kartu_stok`, bukan dihitung ulang.


        Nota `TUNAI` yang POSTED langsung `LUNAS` — uangnya diterima di meja dan
        tidak ada dokumen alokasi yang menunjuknya. Nota `KREDIT` tetap `BELUM`
        sampai modul `penerimaan-pembayaran` ada.


        **Untuk nota KREDIT, ini juga satu-satunya titik `plafon_kredit`
        ditegakkan** (isu #10 fase 2): piutang berjalan pelanggan ditambah nota ini
        tidak boleh melampaui `plafon_kredit`-nya. `plafon_kredit` `NULL` berarti
        tanpa batas dan tidak pernah menolak. Tidak ada jalur tembus untuk
        `SUPERADMIN` — posting sudah di tangan `CASHIER` saja.


        Saldo ruang yang tidak mencukupi ditolak 400 dengan pesan yang menyebut
        produk dan ruang; periode yang sudah `TUTUP` ditolak 400 dengan pesan yang
        menyebut bulannya.


        Role: `CASHIER`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Diposting
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Penjualan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penjualan/{id}/batal:
    post:
      tags: [penjualan]
      summary: Batalkan nota POSTED dengan baris pembalik
      operationId: batalPenjualan
      description: >
        `POSTED` → `BATAL`. Menambah baris pembalik `PEMBATALAN_TRANSAKSI` untuk
        setiap baris `kartu_stok` yang ditulis dokumen ini — barangnya masuk lagi
        ke ruang, dinilai dengan rata-rata bergerak ruang itu saat ini.
        `hpp_satuan_dasar`/`hpp_total`/`total_hpp` **tidak dikosongkan**: itu
        catatan apa yang benar-benar terjadi, dan `status = BATAL` sudah
        menyatakan nota itu tidak berlaku.


        Barisnya bertanggal hari ini, seperti setiap pembalikan di sistem ini. Nota
        yang periodenya sudah ditutup tetap bisa dibatalkan — pembaliknya masuk
        periode berjalan.


        Ini satu-satunya rem begitu nota POSTED — kasir yang memposting tidak
        boleh membatalkannya sendiri. Kesalahan ketik dikoreksi lewat
        `retur-penjualan` (begitu modul itu ada) atau pembatalan oleh atasan.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan_batal]
              properties:
                alasan_batal: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Dibatalkan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Penjualan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembayaran-utang:
    get:
      tags: [pembayaran-utang]
      summary: List pembayaran utang
      operationId: listPembayaranUtang
      description: >
        Tidak menyertakan `alokasi`; kuncinya hilang sama sekali, bukan array kosong —
        untuk dokumen ini array kosong adalah klaim yang bermakna (uang titipan yang
        belum dialokasikan), jadi menghilangkan kuncinya lebih jujur pada endpoint yang
        tidak membacanya. `search` mencocokkan nomor dokumen maupun `no_referensi`.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, POSTED, BATAL] }
        - name: metode
          in: query
          schema: { type: string, enum: [TUNAI, TRANSFER, GIRO] }
        - name: status_giro
          in: query
          description: Daftar giro yang belum cair adalah pekerjaan harian keuangan.
          schema: { type: string, enum: [BELUM_CAIR, CAIR, TOLAK] }
        - name: id_supplier
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: tanggal_dari
          in: query
          schema: { type: string, format: date }
        - name: tanggal_sampai
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/PembayaranUtang" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [pembayaran-utang]
      summary: Buat draft pembayaran utang
      operationId: createPembayaranUtang
      description: >
        Uang yang dibayarkan ke supplier, beserta pembagiannya ke faktur-faktur.


        **Tidak ada tahap `DIAJUKAN`**, dan itu keputusan, bukan kelalaian meniru
        pembelian. Tahap persetujuan di sana ada karena `kartu_stok` append-only:
        posting yang salah hanya bisa dibalik, dan pembalikannya dinilai pada rata-rata
        yang sudah bergeser. Alokasi pembayaran tidak begitu — ia bisa dibatalkan dan
        seluruh cache dihitung ulang persis. Kontrol dua orangnya tetap ada, satu state
        lebih sedikit: `CASHIER` menyiapkan, `SUPERADMIN` melepas uangnya.


        `alokasi` boleh kosong atau tidak dikirim sama sekali. Uang kadang dibayarkan
        sebelum diputuskan menutup faktur yang mana, dan sisanya mengendap sebagai
        titipan — aturan yang sama dengan `jumlah_dialokasikan` yang boleh lebih kecil
        dari `jumlah`.


        Dua batas diperiksa dan keduanya pertanyaan yang berbeda: total alokasi tidak
        boleh melebihi `jumlah` pembayaran, dan setiap alokasi tidak boleh melebihi sisa
        utang fakturnya. Satu request bisa lolos yang satu dan gagal yang lain.


        Fakturnya harus `POSTED` dan milik supplier yang sama. `DRAFT` adalah halaman
        yang diketik, bukan utang; `BATAL` adalah utang yang ditarik kembali.


        `metode: GIRO` mengisi `status_giro` menjadi `BELUM_CAIR` secara otomatis.
        `tanggal_jatuh_tempo_giro` hanya diterima untuk GIRO.


        Nomor digenerate server dengan seri sendiri, `PU/2026/08/0001`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id_supplier, tanggal, metode, jumlah]
              properties:
                id_supplier: { type: integer, format: int64, minimum: 1 }
                tanggal: { type: string, format: date, example: "2026-08-20" }
                metode: { type: string, enum: [TUNAI, TRANSFER, GIRO] }
                no_referensi:
                  type: string
                  nullable: true
                  maxLength: 64
                  description: Nomor transfer, nomor warkat giro, atau nomor kuitansi.
                nama_bank: { type: string, nullable: true, maxLength: 128 }
                tanggal_jatuh_tempo_giro:
                  type: string
                  format: date
                  nullable: true
                  description: "Hanya untuk `metode: GIRO`; ditolak 400 pada metode lain."
                jumlah:
                  type: string
                  description: String desimal, harus lebih dari nol.
                  example: "400000"
                keterangan: { type: string, nullable: true, maxLength: 1000 }
                alokasi:
                  type: array
                  maxItems: 500
                  description: Boleh kosong. Satu faktur hanya boleh muncul sekali.
                  items: { $ref: "#/components/schemas/PembayaranUtangAlokasiInput" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PembayaranUtang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembayaran-utang/{id}:
    get:
      tags: [pembayaran-utang]
      summary: Detail pembayaran utang beserta alokasinya
      operationId: getPembayaranUtang
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PembayaranUtang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [pembayaran-utang]
      summary: Ubah header pembayaran utang
      operationId: updatePembayaranUtang
      description: >
        **Hanya saat `DRAFT`.**


        `id_supplier` tidak ada di sini: ia adalah tujuan uangnya, dan mengubahnya akan
        membuat seluruh alokasi menunjuk faktur supplier lain. `metode` juga tidak,
        karena ia yang menentukan boleh-tidaknya kolom giro terisi — ganti dengan
        membatalkan lalu menginput ulang, yang juga bentuk yang akan terlihat di rekening
        koran.


        Menurunkan `jumlah` di bawah yang sudah dialokasikan ditolak 400 dengan menyebut
        angkanya, bukan dibiarkan gagal di constraint.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tanggal: { type: string, format: date }
                no_referensi: { type: string, nullable: true, maxLength: 64 }
                nama_bank: { type: string, nullable: true, maxLength: 128 }
                tanggal_jatuh_tempo_giro: { type: string, format: date, nullable: true }
                jumlah: { type: string }
                keterangan: { type: string, nullable: true, maxLength: 1000 }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PembayaranUtang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembayaran-utang/{id}/alokasi:
    put:
      tags: [pembayaran-utang]
      summary: Ganti seluruh alokasi pembayaran
      operationId: replacePembayaranUtangAlokasi
      description: >
        `PUT`, bukan `PATCH`: alokasinya diganti sekaligus, karena pembagian satu
        pembayaran ke beberapa faktur adalah satu keputusan, dan suntingan sebagian akan
        membuat `jumlah_dialokasikan` di header berselisih dengan barisnya sendiri di
        antara dua request. **Hanya saat `DRAFT`.**


        Array kosong diterima dan berarti "jangan alokasikan apa pun" — berbeda dari baris
        detail dokumen yang tidak boleh kosong. Pembayaran tanpa alokasi adalah titipan
        yang mengendap di supplier, dan itu keadaan yang nyata.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                alokasi:
                  type: array
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PembayaranUtangAlokasiInput" }
      responses:
        "200":
          description: Replaced
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PembayaranUtang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembayaran-utang/{id}/posting:
    post:
      tags: [pembayaran-utang]
      summary: Sahkan pembayaran dan lunasi fakturnya
      operationId: postingPembayaranUtang
      description: >
        `DRAFT` → `POSTED`. Satu transaksi memeriksa ulang setiap alokasi terhadap sisa
        utang fakturnya, menutup dokumen, lalu menghitung ulang
        `pembelian.status_pembayaran` untuk setiap faktur yang disentuhnya.


        Pemeriksaan yang menentukan terjadi di sini, di bawah row lock tiap faktur. Yang
        saat draft hanya memberi error lebih cepat: di antara draft ditulis dan diposting,
        pembayaran lain atau sebuah retur bisa lebih dulu menghabiskan sisa yang dituju.


        **Giro diposting seperti yang lain dan belum melunasi apa pun.** Dokumennya jadi
        nyata, alokasinya jadi nyata, seluruh cache dihitung ulang — dan untuk giro
        `BELUM_CAIR` hasilnya membiarkan utangnya tepat di tempatnya. Itu memang
        maksudnya: kertasnya sudah berpindah tangan, uangnya belum.


        Ditolak 400 kalau ada alokasi yang melebihi sisa utang fakturnya, kalau fakturnya
        bukan `POSTED`, atau kalau fakturnya milik supplier lain.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Posted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PembayaranUtang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembayaran-utang/{id}/batal:
    post:
      tags: [pembayaran-utang]
      summary: Batalkan pembayaran yang sudah diposting
      operationId: batalPembayaranUtang
      description: >
        `POSTED` → `BATAL`. Setiap faktur yang ditutupnya kembali berutang seperti
        sebelumnya.


        Tidak ada dokumen pembalik dan tidak ada yang dihapus — ini satu-satunya tempat
        modul ini benar-benar lebih sederhana daripada sisi stok. `status_pembayaran`
        adalah fungsi murni dari baris-baris POSTED, jadi memindahkan dokumen ini keluar
        dari POSTED lalu menghitung ulang sudah persis. Tidak ada residu penilaian yang
        tertinggal, tidak seperti pembalikan `kartu_stok`.


        Baris alokasinya tetap ada, jadi pembatalannya bisa diaudit: apa yang diklaim
        dilunasi pembayaran ini tetap terbaca di samping `alasan_batal`.


        **Membatalkan pembelian yang punya pembayaran POSTED ditolak 409** — batalkan
        pembayarannya lebih dulu. Uangnya sudah keluar dari bank, dan ke mana seharusnya
        uang itu pergi adalah keputusan, bukan langkah aritmetika. Giro yang belum cair
        ikut memblokir: ia tidak mengurangi utang, tetapi ia kertas yang menunjuk faktur
        itu dan akan mustahil dijelaskan saat cair.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan_batal]
              properties:
                alasan_batal: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Dibatalkan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PembayaranUtang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembayaran-utang/{id}/cair:
    post:
      tags: [pembayaran-utang]
      summary: Catat giro sudah cair di bank
      operationId: cairkanGiro
      description: >
        `status_giro: BELUM_CAIR` → `CAIR`. **Ini momen utangnya benar-benar berkurang**,
        bukan saat gironya diserahkan. Sampai di sini kertasnya cuma janji, dan
        memperlakukan janji sebagai pembayaran adalah cara supplier berakhir menagih
        faktur yang menurut sistem sudah lunas.


        Sisa utang setiap faktur diperiksa lagi di sini, dan itu bukan paranoia:
        alokasinya tidak berarti apa-apa selama giro menggantung, jadi tidak ada yang
        menahan pembayaran lain melunasi faktur yang sama lebih dulu — dan giro bisa
        menggantung berminggu-minggu. Kalau itu terjadi, pencairannya ditolak 400 dan
        gironya tetap `BELUM_CAIR`.


        Butuh dokumen `POSTED` dengan `metode: GIRO` dan `status_giro: BELUM_CAIR`. Giro
        yang sudah cair atau sudah ditolak tidak bisa dicairkan lagi.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tanggal_cair]
              properties:
                tanggal_cair:
                  type: string
                  format: date
                  description: Tidak boleh sebelum tanggal pembayarannya.
                  example: "2026-09-22"
      responses:
        "200":
          description: Cair
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PembayaranUtang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pembayaran-utang/{id}/tolak-giro:
    post:
      tags: [pembayaran-utang]
      summary: Catat giro ditolak bank
      operationId: tolakGiro
      description: >
        `status_giro: BELUM_CAIR` → `TOLAK`. Tidak ada yang perlu dikembalikan dan tidak
        ada cache yang berubah, karena giro yang belum cair memang belum pernah mengurangi
        utang apa pun. Fakturnya masih terbuka penuh dan masih bisa dibayar dengan cara
        lain.


        `tanggal_cair` tetap kosong: giro yang ditolak tidak punya tanggal cair, dan
        constraint `pembayaran_utang_tanggal_cair_check` menegakkan itu.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                alasan:
                  type: string
                  maxLength: 500
                  description: Ditambahkan ke `keterangan` dokumen bila dikirim.
                  example: dana tidak cukup
      responses:
        "200":
          description: Ditolak
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PembayaranUtang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-pembayaran:
    get:
      tags: [penerimaan-pembayaran]
      summary: List penerimaan pembayaran
      operationId: listPenerimaanPembayaran
      description: >
        Cermin `GET /pembayaran-utang`, uang mengalir ke arah sebaliknya. Tidak
        menyertakan `alokasi`; kuncinya hilang sama sekali, bukan array kosong — untuk
        dokumen ini array kosong adalah klaim yang bermakna (uang titipan yang belum
        dialokasikan). `search` mencocokkan nomor dokumen maupun `no_referensi`.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, POSTED, BATAL] }
        - name: metode
          in: query
          schema: { type: string, enum: [TUNAI, TRANSFER, GIRO] }
        - name: status_giro
          in: query
          description: Daftar giro pelanggan yang belum cair adalah pekerjaan harian keuangan.
          schema: { type: string, enum: [BELUM_CAIR, CAIR, TOLAK] }
        - name: id_pelanggan
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: tanggal_dari
          in: query
          schema: { type: string, format: date }
        - name: tanggal_sampai
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/PenerimaanPembayaran" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [penerimaan-pembayaran]
      summary: Buat draft penerimaan pembayaran
      operationId: createPenerimaanPembayaran
      description: >
        Uang yang diterima dari pelanggan, beserta pembagiannya ke nota-nota `KREDIT`.
        Cermin `POST /pembayaran-utang`, uang mengalir ke arah sebaliknya.


        **Tidak ada tahap `DIAJUKAN`**, alasannya persis sama seperti di sisi utang:
        alokasi tidak meninggalkan residu penilaian apa pun, ia bisa dibatalkan dan
        seluruh cache dihitung ulang persis. Kontrol dua orangnya tetap ada, satu state
        lebih sedikit: `CASHIER` menyiapkan, `SUPERADMIN` melepas kreditnya.


        `alokasi` boleh kosong atau tidak dikirim sama sekali. Uang kadang diterima
        sebelum diputuskan menutup nota yang mana, dan sisanya mengendap sebagai
        titipan — aturan yang sama dengan `jumlah_dialokasikan` yang boleh lebih kecil
        dari `jumlah`.


        Dua batas diperiksa dan keduanya pertanyaan yang berbeda: total alokasi tidak
        boleh melebihi `jumlah` pembayaran, dan setiap alokasi tidak boleh melebihi sisa
        piutang notanya. Satu request bisa lolos yang satu dan gagal yang lain.


        Notanya harus `POSTED`, `KREDIT`, dan milik pelanggan yang sama. `DRAFT` adalah
        halaman yang diketik, bukan piutang; `BATAL` adalah piutang yang ditarik
        kembali; **`TUNAI` tidak pernah jadi piutang sama sekali** — uangnya sudah
        berpindah tangan di konter.


        `metode: GIRO` mengisi `status_giro` menjadi `BELUM_CAIR` secara otomatis.
        `tanggal_jatuh_tempo_giro` hanya diterima untuk GIRO.


        Nomor digenerate server dengan seri sendiri, `PP/2026/08/0001`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id_pelanggan, tanggal, metode, jumlah]
              properties:
                id_pelanggan: { type: integer, format: int64, minimum: 1 }
                tanggal: { type: string, format: date, example: "2026-08-20" }
                metode: { type: string, enum: [TUNAI, TRANSFER, GIRO] }
                no_referensi:
                  type: string
                  nullable: true
                  maxLength: 64
                  description: Nomor transfer, nomor warkat giro, atau nomor kuitansi.
                nama_bank: { type: string, nullable: true, maxLength: 128 }
                tanggal_jatuh_tempo_giro:
                  type: string
                  format: date
                  nullable: true
                  description: "Hanya untuk `metode: GIRO`; ditolak 400 pada metode lain."
                jumlah:
                  type: string
                  description: String desimal, harus lebih dari nol.
                  example: "600000"
                keterangan: { type: string, nullable: true, maxLength: 1000 }
                alokasi:
                  type: array
                  maxItems: 500
                  description: Boleh kosong. Satu nota hanya boleh muncul sekali.
                  items: { $ref: "#/components/schemas/PembayaranAlokasiInput" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanPembayaran" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-pembayaran/{id}:
    get:
      tags: [penerimaan-pembayaran]
      summary: Detail penerimaan pembayaran beserta alokasinya
      operationId: getPenerimaanPembayaran
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanPembayaran" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [penerimaan-pembayaran]
      summary: Ubah header penerimaan pembayaran
      operationId: updatePenerimaanPembayaran
      description: >
        **Hanya saat `DRAFT`.**


        `id_pelanggan` tidak ada di sini: ia adalah pemilik piutang yang ditutup
        alokasinya, dan mengubahnya akan membuat seluruh alokasi menunjuk nota
        pelanggan lain. `metode` juga tidak, karena ia yang menentukan boleh-tidaknya
        kolom giro terisi — ganti dengan membatalkan lalu menginput ulang, yang juga
        bentuk yang akan terlihat di rekening koran.


        Menurunkan `jumlah` di bawah yang sudah dialokasikan ditolak 400 dengan menyebut
        angkanya, bukan dibiarkan gagal di constraint.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tanggal: { type: string, format: date }
                no_referensi: { type: string, nullable: true, maxLength: 64 }
                nama_bank: { type: string, nullable: true, maxLength: 128 }
                tanggal_jatuh_tempo_giro: { type: string, format: date, nullable: true }
                jumlah: { type: string }
                keterangan: { type: string, nullable: true, maxLength: 1000 }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanPembayaran" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-pembayaran/{id}/alokasi:
    put:
      tags: [penerimaan-pembayaran]
      summary: Ganti seluruh alokasi pembayaran
      operationId: replacePenerimaanPembayaranAlokasi
      description: >
        `PUT`, bukan `PATCH`: alokasinya diganti sekaligus, karena pembagian satu
        pembayaran ke beberapa nota adalah satu keputusan, dan suntingan sebagian akan
        membuat `jumlah_dialokasikan` di header berselisih dengan barisnya sendiri di
        antara dua request. **Hanya saat `DRAFT`.**


        Array kosong diterima dan berarti "jangan alokasikan apa pun" — berbeda dari baris
        detail dokumen yang tidak boleh kosong. Pembayaran tanpa alokasi adalah titipan
        yang mengendap di pelanggan, dan itu keadaan yang nyata.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                alokasi:
                  type: array
                  maxItems: 500
                  items: { $ref: "#/components/schemas/PembayaranAlokasiInput" }
      responses:
        "200":
          description: Replaced
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanPembayaran" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-pembayaran/{id}/posting:
    post:
      tags: [penerimaan-pembayaran]
      summary: Sahkan pembayaran dan lunasi notanya
      operationId: postingPenerimaanPembayaran
      description: >
        `DRAFT` → `POSTED`. Satu transaksi memeriksa ulang setiap alokasi terhadap sisa
        piutang notanya, menutup dokumen, lalu menghitung ulang
        `penjualan.status_pembayaran` untuk setiap nota yang disentuhnya.


        Pemeriksaan yang menentukan terjadi di sini, di bawah row lock tiap nota. Yang
        saat draft hanya memberi error lebih cepat: di antara draft ditulis dan diposting,
        pembayaran lain bisa lebih dulu menghabiskan sisa yang dituju.


        **Giro diposting seperti yang lain dan belum melunasi apa pun.** Dokumennya jadi
        nyata, alokasinya jadi nyata, seluruh cache dihitung ulang — dan untuk giro
        `BELUM_CAIR` hasilnya membiarkan piutangnya tepat di tempatnya. Itu memang
        maksudnya: kertasnya sudah berpindah tangan, uangnya belum.


        Ditolak 400 kalau ada alokasi yang melebihi sisa piutang notanya, kalau notanya
        bukan `POSTED`/`KREDIT`, atau kalau notanya milik pelanggan lain.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Posted
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanPembayaran" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-pembayaran/{id}/batal:
    post:
      tags: [penerimaan-pembayaran]
      summary: Batalkan pembayaran yang sudah diposting
      operationId: batalPenerimaanPembayaran
      description: >
        `POSTED` → `BATAL`. Setiap nota yang ditutupnya kembali berpiutang seperti
        sebelumnya.


        Tidak ada dokumen pembalik dan tidak ada yang dihapus — sama seperti di sisi
        utang. `status_pembayaran` adalah fungsi murni dari baris-baris POSTED, jadi
        memindahkan dokumen ini keluar dari POSTED lalu menghitung ulang sudah persis.


        Baris alokasinya tetap ada, jadi pembatalannya bisa diaudit: apa yang diklaim
        dilunasi pembayaran ini tetap terbaca di samping `alasan_batal`.


        **Membatalkan nota penjualan yang punya pembayaran POSTED ditolak 409** —
        batalkan pembayarannya lebih dulu. Uangnya sudah masuk, dan ke mana seharusnya
        uang itu pergi adalah keputusan, bukan langkah aritmetika. Giro yang belum cair
        ikut memblokir: ia tidak mengurangi piutang, tetapi ia kertas yang menunjuk nota
        itu dan akan mustahil dijelaskan saat cair.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan_batal]
              properties:
                alasan_batal: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Dibatalkan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanPembayaran" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-pembayaran/{id}/cair:
    post:
      tags: [penerimaan-pembayaran]
      summary: Catat giro pelanggan sudah cair di bank
      operationId: cairkanGiroPelanggan
      description: >
        `status_giro: BELUM_CAIR` → `CAIR`. **Ini momen piutangnya benar-benar
        berkurang**, bukan saat gironya diserahkan. Sampai di sini kertasnya cuma janji,
        dan memperlakukan janji sebagai pembayaran adalah cara piutang yang menurut
        sistem sudah lunas justru masih disengketakan pelanggan.


        Sisa piutang setiap nota diperiksa lagi di sini, dan itu bukan paranoia:
        alokasinya tidak berarti apa-apa selama giro menggantung, jadi tidak ada yang
        menahan pembayaran lain melunasi nota yang sama lebih dulu — dan giro bisa
        menggantung berminggu-minggu. Kalau itu terjadi, pencairannya ditolak 400 dan
        gironya tetap `BELUM_CAIR`.


        Butuh dokumen `POSTED` dengan `metode: GIRO` dan `status_giro: BELUM_CAIR`. Giro
        yang sudah cair atau sudah ditolak tidak bisa dicairkan lagi.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tanggal_cair]
              properties:
                tanggal_cair:
                  type: string
                  format: date
                  description: Tidak boleh sebelum tanggal pembayarannya.
                  example: "2026-09-22"
      responses:
        "200":
          description: Cair
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanPembayaran" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/penerimaan-pembayaran/{id}/tolak-giro:
    post:
      tags: [penerimaan-pembayaran]
      summary: Catat giro pelanggan ditolak bank
      operationId: tolakGiroPelanggan
      description: >
        `status_giro: BELUM_CAIR` → `TOLAK`. Tidak ada yang perlu dikembalikan dan tidak
        ada cache yang berubah, karena giro yang belum cair memang belum pernah mengurangi
        piutang apa pun. Notanya masih terbuka penuh dan masih bisa ditagih dengan cara
        lain.


        `tanggal_cair` tetap kosong: giro yang ditolak tidak punya tanggal cair, dan
        constraint `penerimaan_pembayaran_tanggal_cair_check` menegakkan itu.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                alasan:
                  type: string
                  maxLength: 500
                  description: Ditambahkan ke `keterangan` dokumen bila dikirim.
                  example: dana tidak cukup
      responses:
        "200":
          description: Ditolak
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/PenerimaanPembayaran" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/stok-opname:
    get:
      tags: [stok-opname]
      summary: List stok opname
      operationId: listStokOpname
      description: >
        Tidak menyertakan `detail`; kuncinya hilang sama sekali, bukan array kosong.


        `status=DIAJUKAN` dengan `terlama_dulu=true` adalah antrean verifikasi, dan
        sejak isu #15 juga daftar ruang yang sedang berhenti bekerja.


        Isu #12 fase 6: dokumen yang `id_ruang`-nya di luar unit_kerja aktif sesi
        pemanggil dilewati begitu saja, tanpa error.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, DIAJUKAN, POSTED, BATAL] }
        - name: id_ruang
          in: query
          schema: { type: integer, format: int64, minimum: 1 }
        - name: tanggal_dari
          in: query
          schema: { type: string, format: date }
        - name: tanggal_sampai
          in: query
          schema: { type: string, format: date }
        - name: terlama_dulu
          in: query
          description: Urutkan paling lama dulu, untuk dibaca sebagai antrean.
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/StokOpname" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [stok-opname]
      summary: Buka sesi hitung fisik
      operationId: createStokOpname
      description: >
        Nomor diambil dari `document_counter` dengan prefix `SO`. `ts_cutoff` diisi
        server dari `now()`, tidak pernah dari body. Ruang yang disebut **mulai beku
        seketika**: sejak baris ini ada, trigger `kartu_stok` menolak posting apa pun
        ke ruang itu dari modul mana pun, sampai dokumen ini `POSTED` atau `BATAL`
        (isu #15).


        Ditolak 409 kalau ruang itu masih punya opname lain yang belum selesai
        (`stok_opname_ruang_terbuka_uidx`).


        **Isu #12 fase 5:** `id_ruang` divalidasi terhadap unit_kerja aktif sesi
        pemanggil, kalau tidak → 403.


        Role: `INVENTARIS`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id_ruang]
              properties:
                id_ruang: { type: integer, format: int64, minimum: 1 }
                uraian_so: { type: string, maxLength: 1000, nullable: true }
      responses:
        "201":
          description: Sesi dibuka
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/stok-opname/{id}:
    get:
      tags: [stok-opname]
      summary: Detail stok opname beserta barisnya
      operationId: getStokOpname
      description: >
        Isu #12 fase 6: menjawab 404 kalau ruangnya di luar unit_kerja aktif sesi
        pemanggil, seperti id yang benar-benar tidak ada.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [stok-opname]
      summary: Ubah uraian_so
      operationId: updateStokOpname
      description: >
        Satu-satunya field header yang boleh diubah lewat PATCH — `id_ruang` tidak
        bisa berubah, karena snapshot cutoff dan seluruh barisnya sudah menunjuk
        ruang tempat dokumen ini dibuka.


        Hanya saat `DRAFT`. Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                uraian_so: { type: string, maxLength: 1000, nullable: true }
      responses:
        "200":
          description: Diubah
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/stok-opname/{id}/tarik-saldo:
    post:
      tags: [stok-opname]
      summary: Isi baris dari saldo ruang saat ini
      operationId: tarikSaldoStokOpname
      description: >
        Mengisi satu baris untuk setiap produk yang pernah bergerak di ruang ini:
        `stok_awal` = saldo saat ini, `id_kartu_stok_cutoff` = baris `kartu_stok`
        acuannya, `stok_so` dibiarkan `null` (belum dihitung). Karena ruangnya sudah
        beku sejak dokumen dibuka, "saldo sekarang" dan "saldo saat cutoff" adalah
        angka yang sama.


        Hanya boleh sekali — ditolak 409 kalau dokumen sudah punya baris. Barang
        yang terlewat tetap bisa ditambah manual lewat `PUT .../detail`.


        Hanya saat `DRAFT`. Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Baris terisi
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/stok-opname/{id}/detail:
    put:
      tags: [stok-opname]
      summary: Ganti seluruh baris stok opname
      operationId: replaceStokOpnameDetail
      description: >
        Mengganti seluruh baris sekaligus — juga cara menambah barang yang terlewat
        `tarik-saldo` secara manual. Setiap `id_product` yang disebut harus sudah
        punya baris `kartu_stok` untuk `(produk, ruang ini)`; yang belum pernah
        bergerak di ruang ini ditolak 400 — lihat "Barang yang sistem belum pernah
        lihat" di CLAUDE.md.


        `stok_so` setiap baris kembali ke `null` (belum dihitung) — isi ulang lewat
        `PATCH .../detail/{id_detail}`.


        Hanya saat `DRAFT`. Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [detail]
              properties:
                detail:
                  type: array
                  minItems: 1
                  maxItems: 1000
                  items: { $ref: "#/components/schemas/StokOpnameDetailInput" }
      responses:
        "200":
          description: Baris diganti
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/stok-opname/{id}/detail/{id_detail}:
    patch:
      tags: [stok-opname]
      summary: Isi hasil hitung satu baris
      operationId: updateStokOpnameDetail
      description: >
        **Satu-satunya pengecualian di seluruh API ini** terhadap "baris diganti
        sekaligus, tidak pernah satu-satu" — diisi bertahap oleh orang yang berjalan
        menyusuri rak, bukan diketik ulang sekali seperti baris `pembelian`.


        `stok_selisih_lebih`/`stok_selisih_kurang` **selalu dihitung ulang** dari
        `stok_so` terhadap `stok_awal` oleh statement ini sendiri — tidak pernah
        diterima dari body.


        Hanya saat `DRAFT`. Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
        - name: id_detail
          in: path
          required: true
          schema: { type: integer, format: int64, minimum: 1 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                stok_so:
                  type: integer
                  format: int64
                  minimum: 0
                  nullable: true
                  description: >
                    `null` berarti "belum dihitung", bukan nol — lihat `StokOpnameDetail`.
                keterangan: { type: string, maxLength: 500, nullable: true }
      responses:
        "200":
          description: Baris diisi
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/stok-opname/{id}/ajukan:
    post:
      tags: [stok-opname]
      summary: Ajukan draft hitung untuk diverifikasi
      operationId: ajukanStokOpname
      description: >
        `DRAFT` → `DIAJUKAN`. Ditolak 400 kalau tidak satu pun baris punya `stok_so`
        terisi — dokumen kosong, bukan hitungan. Tidak ditolak karena masih ada
        baris `null`: hitung sebagian (satu rak, satu kategori) sah, dan responsnya
        melaporkan `jumlah_belum_dihitung` supaya verifikator memutuskan dengan
        sadar. Ruangnya tetap beku setelah ini.


        Role: `INVENTARIS`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Diajukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/stok-opname/{id}/tolak:
    post:
      tags: [stok-opname]
      summary: Kembalikan hitungan ke DRAFT untuk dihitung ulang
      operationId: tolakStokOpname
      description: >
        `DIAJUKAN` → `DRAFT` — koreksi kertas, bukan keputusan bisnis seperti
        penolakan `pemakaian`, sehingga bukan status terminal. Ruangnya **tetap
        beku**: `DRAFT` dan `DIAJUKAN` sama-sama membekukan. Tidak menerima body —
        skemanya tidak punya kolom alasan penolakan untuk dokumen ini.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Dikembalikan ke DRAFT
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/stok-opname/{id}/posting:
    post:
      tags: [stok-opname]
      summary: Posting selisih ke kartu stok dan lepas beku
      operationId: postingStokOpname
      description: >
        `DIAJUKAN` → `POSTED`. Menulis `SO_SURPLUS`/`SO_DEFISIT` untuk setiap baris
        yang selisihnya tidak nol; baris `null` atau berselisih nol tidak menulis
        apa pun ke `kartu_stok`. Kalau semua baris nol/belum dihitung, dokumen tetap
        boleh diposting tanpa satu baris pun — beda dengan `pemakaian` yang menolak
        nota kosong.


        Sebelum menulis, saldo ruang saat ini diverifikasi ulang di bawah kunci
        saldo terhadap `stok_awal` yang dibekukan saat `tarik-saldo` — menolak 409
        kalau berbeda. Di bawah pembekuan yang utuh cabang ini tidak akan pernah
        menyala.


        Baris penyesuaian bertanggal `ts_cutoff`, bukan hari ini, sehingga ditolak
        400 kalau periode `ts_cutoff` sudah `TUTUP`.


        Ruangnya **lepas beku** setelah ini.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Diposting
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/stok-opname/{id}/batal:
    post:
      tags: [stok-opname]
      summary: Batalkan dari status mana pun
      operationId: batalStokOpname
      description: >
        **Boleh dipanggil dari status apa pun, bukan hanya `POSTED`** — satu-satunya
        transisi yang begitu di seluruh API ini, supaya opname yang ditinggalkan di
        `DRAFT`/`DIAJUKAN` tidak membekukan ruangnya selamanya.


        Dari `DRAFT`/`DIAJUKAN`: belum ada baris `kartu_stok`, jadi hanya status yang
        berubah. Dari `POSTED`: menambah baris pembalik `PEMBATALAN_TRANSAKSI`
        bertanggal hari ini untuk setiap penyesuaian yang ditulis, dinilai dengan
        rata-rata bergerak ruang itu saat ini.


        Ruangnya **selalu lepas beku** setelah ini, dari status mana pun ia berasal.


        Role: `SUPERADMIN`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [alasan_batal]
              properties:
                alasan_batal: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Dibatalkan
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/StokOpname" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/periode:
    get:
      tags: [periode]
      summary: List periode
      description: >
        Halaman baris `periode` yang tersimpan, bulan terbaru lebih dulu.


        Perhatikan apa yang **tidak** muncul di sini: bulan yang belum pernah ditutup
        tidak punya baris sama sekali, jadi ia tidak ada di daftar ini betapapun
        terbukanya. Itu bukan kekurangan — tabelnya mencatat penutupan, bukan
        kalender. Untuk menanyakan satu bulan tertentu pakai
        `GET /api/v1/periode/{tahun}/{bulan}`, yang menjawab untuk bulan mana pun.
      operationId: listPeriode
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: tahun
          in: query
          description: Kosongkan untuk seluruh tahun.
          schema: { type: integer, minimum: 2000, maximum: 9999 }
        - name: status
          in: query
          description: Kosongkan untuk kedua status.
          schema: { type: string, enum: [BUKA, TUTUP] }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Periode" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

  /api/v1/periode/{tahun}/{bulan}:
    get:
      tags: [periode]
      summary: Status tutup buku satu bulan
      description: >
        Menjawab untuk bulan mana pun, ada barisnya atau tidak.


        Bulan yang belum pernah ditutup menjawab **`BUKA` sintetis**, bukan 404: baris
        `periode` yang tidak ada memang berarti bulannya terbuka — itu keputusan
        migrasi `000004`, supaya database baru tidak macet sebelum ada data. Menjawab
        404 akan mengundang klien menyimpulkan bulannya tidak ada.


        Responsnya tidak memuat `id`. Identitas sebuah periode adalah pasangan
        `(tahun, bulan)`, dan dengan begitu bulan yang belum pernah ditutup punya
        bentuk respons yang sama persis dengan yang sudah.
      operationId: getPeriode
      parameters:
        - $ref: "#/components/parameters/Tahun"
        - $ref: "#/components/parameters/Bulan"
      responses:
        "200":
          description: Status bulan itu
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Periode" }
        "400": { $ref: "#/components/responses/ValidationError" }

  /api/v1/periode/{tahun}/{bulan}/tutup:
    post:
      tags: [periode]
      summary: Tutup buku satu bulan
      description: >
        Menutup buku bulan itu: sejak commit, setiap dokumen yang menulis `kartu_stok`
        dengan `tanggal_transaksi` di dalamnya ditolak. Penegakannya di trigger, jadi
        modul yang belum ditulis pun mewarisinya tanpa satu baris kode.


        Tidak ada body. Yang ditanyakan seluruhnya ada di path, dan pelakunya diambil
        dari token — `ditutup_oleh` tidak pernah datang dari request.


        Bulan yang belum punya baris akan **dibuatkan** barisnya, bukan diubah.
        Jejak pembukaan sebelumnya (`dibuka_oleh`, `ts_buka`) sengaja tidak dihapus:
        itu satu-satunya catatan bahwa bulan ini pernah dibuka kembali.


        Menutup **tidak harus berurutan** — Agustus boleh ditutup selagi Juli masih
        terbuka. Menuntut urutan berarti memaksa menutup bulan-bulan yang tidak pernah
        dipakai lebih dulu, dan tidak ada yang bisa rusak karena selanya.
      operationId: tutupPeriode
      parameters:
        - $ref: "#/components/parameters/Tahun"
        - $ref: "#/components/parameters/Bulan"
      responses:
        "200":
          description: Bulan itu sekarang TUTUP
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Periode" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Error" }
        # Bulan itu memang sudah TUTUP.
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/periode/{tahun}/{bulan}/buka:
    post:
      tags: [periode]
      summary: Buka kembali bulan yang sudah ditutup
      description: >
        Kebalikan dari `tutup`, dan `SUPERADMIN` juga — bulan yang bisa dibuka siapa
        saja tidak pernah benar-benar tertutup. Mengisi `dibuka_oleh` dan `ts_buka`
        tanpa menyentuh `ditutup_oleh`/`ts_tutup`, sehingga riwayat penutupan yang
        dibatalkan tetap terbaca.


        Bulan yang tidak sedang `TUTUP` ditolak **409**, termasuk bulan yang tidak
        punya baris sama sekali. Keduanya sudah terbuka, dan menjawab 200 untuk
        "bukalah ini" akan membuat pemanggil mengira ada yang berubah.


        Tidak ada `DELETE`. Bulan yang keliru ditutup dibuka kembali, tidak dihapus.
      operationId: bukaPeriode
      parameters:
        - $ref: "#/components/parameters/Tahun"
        - $ref: "#/components/parameters/Bulan"
      responses:
        "200":
          description: Bulan itu sekarang BUKA
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Periode" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Error" }
        # Bulan itu tidak sedang TUTUP, termasuk kalau barisnya belum pernah ada.
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/unit-kerja:
    get:
      tags: [unit-kerja]
      summary: List unit kerja
      operationId: listUnitKerja
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: >
            Cocokkan sebagian pada kode atau nama unit. `%` dan `_` diperlakukan
            sebagai teks biasa, bukan wildcard.
          schema: { type: string, maxLength: 255 }
        - $ref: "#/components/parameters/IsAktif"
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/UnitKerja" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [unit-kerja]
      summary: Create unit kerja
      operationId: createUnitKerja
      description: >
        Isu #12 fase 2: lokasi organisasi yang jadi induk setiap `ruang`.
        `user_role.id_unit_kerja` dan konteks aktif sesi belum ada — lihat
        catatan "Unit kerja and location-bound authority" di CLAUDE.md.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nama]
              properties:
                kode:
                  type: string
                  maxLength: 32
                  nullable: true
                  description: >
                    Opsional. Unik tanpa memperhatikan huruf besar-kecil, tapi
                    beberapa unit boleh sama-sama tidak punya kode.
                  example: "PUSAT"
                nama: { type: string, maxLength: 255, example: "Unit Utama" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/UnitKerja" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/unit-kerja/{id}:
    get:
      tags: [unit-kerja]
      summary: Get unit kerja by id
      operationId: getUnitKerja
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/UnitKerja" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [unit-kerja]
      summary: Update unit kerja
      operationId: updateUnitKerja
      description: >
        Field yang tidak dikirim tidak diubah. Mengirim `null` pada kode
        mengosongkan kolom itu. `id`, `created_at`, dan `created_by` tidak
        pernah diambil dari body.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                kode: { type: string, maxLength: 32, nullable: true }
                nama: { type: string, maxLength: 255 }
                is_aktif: { type: boolean }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/UnitKerja" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/ruang:
    get:
      tags: [ruang]
      summary: List ruang
      operationId: listRuang
      description: >
        Isu #12 fase 6: ruang di luar unit_kerja aktif sesi pemanggil
        dilewati begitu saja, tanpa error. Konteks aktif global melihat
        semua ruang seperti biasa.
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: >
            Cocokkan sebagian pada kode atau nama ruang. `%` dan `_` diperlakukan
            sebagai teks biasa, bukan wildcard.
          schema: { type: string, maxLength: 255 }
        - $ref: "#/components/parameters/IsAktif"
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Ruang" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [ruang]
      summary: Create ruang
      operationId: createRuang
      description: >
        `id_unit_kerja` wajib dan divalidasi menunjuk unit kerja yang aktif
        (isu #12 fase 2) — ruang tanpa unit adalah ruang yang tidak bisa
        diputuskan siapa berhak memakainya. `PATCH /api/v1/ruang/{id}` (isu #23
        fase 2) tidak menyentuh `id_unit_kerja` sama sekali, jadi unitnya tidak
        bisa diganti lewat API ini setelah dibuat.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nama_ruang, id_unit_kerja]
              properties:
                kode:
                  type: string
                  maxLength: 32
                  nullable: true
                  description: >
                    Opsional. Unik tanpa memperhatikan huruf besar-kecil, tapi
                    beberapa ruang boleh sama-sama tidak punya kode.
                  example: "GD-UTM"
                nama_ruang: { type: string, maxLength: 255, example: "Gudang Utama" }
                id_unit_kerja:
                  type: integer
                  format: int64
                  minimum: 1
                  description: Harus menunjuk unit_kerja yang masih is_aktif.
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Ruang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/ruang/{id}:
    get:
      tags: [ruang]
      summary: Get ruang by id
      operationId: getRuang
      description: >
        Isu #12 fase 6: ruang di luar unit_kerja aktif sesi pemanggil
        menjawab 404, sama seperti id yang tidak ada.
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Ruang" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [ruang]
      summary: Update ruang
      operationId: updateRuang
      description: >
        Isu #23 fase 2. Field yang tidak dikirim tidak diubah. `id_unit_kerja`
        sengaja tidak ada di body ini — lihat deskripsi `POST /api/v1/ruang`.
        Mempensiunkan (`is_aktif: false`) ditolak **409** kalau ruangnya masih
        memegang barang (ada `(barang, ruang)` dengan `stok_akhir > 0`; kosongkan
        dulu dengan `mutasi` atau `pemakaian`) atau sedang dibekukan `stok_opname`
        terbuka (isu #15) — pesannya menyebut nomor opname yang menahan, sama
        seperti `nomor_opname_beku` pada `Ruang`.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                kode:
                  type: string
                  maxLength: 32
                  nullable: true
                  description: Unik tanpa memperhatikan huruf besar-kecil. `null` mengosongkannya.
                  example: "GD-UTM"
                nama_ruang: { type: string, maxLength: 255, example: "Gudang Utama" }
                is_aktif:
                  type: boolean
                  description: >
                    Pensiunkan ruang yang salah ketik dengan `false` — ditolak
                    kalau masih bersaldo atau sedang diopname.
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Ruang" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/satuan:
    get:
      tags: [satuan]
      summary: List satuan
      operationId: listSatuan
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: Cocokkan sebagian pada nama satuan.
          schema: { type: string, maxLength: 255 }
        - $ref: "#/components/parameters/IsAktif"
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Satuan" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [satuan]
      summary: Create satuan
      operationId: createSatuan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nama]
              properties:
                nama:
                  type: string
                  maxLength: 64
                  description: Unik tanpa memperhatikan huruf besar-kecil.
                  example: PCS
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Satuan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/satuan/{id}:
    get:
      tags: [satuan]
      summary: Get satuan by id
      operationId: getSatuan
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Satuan" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [satuan]
      summary: Update satuan
      operationId: updateSatuan
      description: Field yang tidak dikirim tidak diubah.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                nama: { type: string, maxLength: 64, example: PCS }
                is_aktif:
                  type: boolean
                  description: Pensiunkan satuan yang salah ketik dengan `false`.
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Satuan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/ekspedisi:
    get:
      tags: [ekspedisi]
      summary: List ekspedisi
      operationId: listEkspedisi
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: Cocokkan sebagian pada nama atau telepon.
          schema: { type: string, maxLength: 255 }
        - $ref: "#/components/parameters/IsAktif"
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Ekspedisi" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [ekspedisi]
      summary: Create ekspedisi
      operationId: createEkspedisi
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nama]
              properties:
                nama:
                  type: string
                  maxLength: 255
                  description: Unik tanpa memperhatikan huruf besar-kecil.
                  example: JNE
                telepon: { type: string, maxLength: 32, nullable: true, example: "021-1234567" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Ekspedisi" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/ekspedisi/{id}:
    get:
      tags: [ekspedisi]
      summary: Get ekspedisi by id
      operationId: getEkspedisi
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Ekspedisi" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [ekspedisi]
      summary: Update ekspedisi
      operationId: updateEkspedisi
      description: >
        Field yang tidak dikirim tidak diubah. `"telepon": null` mengosongkan
        nomor telepon.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                nama: { type: string, maxLength: 255 }
                telepon: { type: string, maxLength: 32, nullable: true }
                is_aktif: { type: boolean }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Ekspedisi" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/supplier:
    get:
      tags: [supplier]
      summary: List supplier
      operationId: listSupplier
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: Cocokkan sebagian pada kode atau nama supplier.
          schema: { type: string, maxLength: 255 }
        - $ref: "#/components/parameters/IsAktif"
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Supplier" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [supplier]
      summary: Create supplier
      operationId: createSupplier
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nama]
              properties:
                kode:
                  type: string
                  maxLength: 32
                  nullable: true
                  description: >
                    Opsional. Unik tanpa memperhatikan huruf besar-kecil, tapi
                    beberapa supplier boleh sama-sama tidak punya kode.
                  example: SUP-01
                nama: { type: string, maxLength: 255, example: "PT Sumber Makmur" }
                telepon: { type: string, maxLength: 32, nullable: true }
                alamat: { type: string, maxLength: 1000, nullable: true }
                npwp: { type: string, maxLength: 32, nullable: true }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Supplier" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/supplier/{id}:
    get:
      tags: [supplier]
      summary: Get supplier by id
      operationId: getSupplier
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Supplier" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [supplier]
      summary: Update supplier
      operationId: updateSupplier
      description: >
        Field yang tidak dikirim tidak diubah. Mengirim `null` pada kode, telepon,
        alamat, atau npwp mengosongkan kolom itu. `id`, `created_at`, dan
        `created_by` tidak pernah diambil dari body.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                kode: { type: string, maxLength: 32, nullable: true }
                nama: { type: string, maxLength: 255 }
                telepon: { type: string, maxLength: 32, nullable: true }
                alamat: { type: string, maxLength: 1000, nullable: true }
                npwp: { type: string, maxLength: 32, nullable: true }
                is_aktif: { type: boolean }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Supplier" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/supplier/{id}/utang:
    get:
      tags: [supplier]
      summary: Faktur supplier yang masih terbuka
      operationId: listUtangSupplier
      description: >
        Daftar kerja untuk menyusun pembayaran: faktur mana milik supplier ini yang masih
        punya sisa, dan berapa.


        Query, bukan modul — tidak ada tabel dan tidak ada migrasi di belakangnya. Seluruh
        jawabannya turunan dari dokumen yang memang sudah diposting: total fakturnya,
        pembayaran yang efektif atasnya, dan kredit yang dihasilkan returnya.


        **Diurutkan dari yang paling lama**, berbeda dari setiap list lain di API ini. Ini
        antrean yang harus dikerjakan, dan faktur yang paling lama menunggu adalah yang
        berikutnya dibayar.


        Supplier yang tidak dikenal menjawab 404; supplier tanpa utang menjawab halaman
        kosong. Dua fakta yang berbeda.
      parameters:
        - $ref: "#/components/parameters/ID"
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: termasuk_lunas
          in: query
          schema: { type: boolean, default: false }
          description: >
            Ikut membawa faktur yang sudah tidak punya sisa. Mati secara default, karena
            gunanya daftar ini justru yang masih terbuka.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/UtangSupplier" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "404": { $ref: "#/components/responses/Error" }

  /api/v1/role:
    get:
      tags: [role]
      summary: List role
      operationId: listRole
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: Cocokkan sebagian pada nama role.
          schema: { type: string, maxLength: 255 }
        - $ref: "#/components/parameters/IsAktif"
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Role" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [role]
      summary: Create role
      operationId: createRole
      description: >
        Tiga role bawaan (`SUPERADMIN`, `CASHIER`, `INVENTARIS`) dipasang lewat
        `db/seeder_postgres/003_role.sql`, bukan endpoint ini. Endpoint ini untuk
        role tambahan.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nama]
              properties:
                nama:
                  type: string
                  maxLength: 64
                  description: >
                    Unik tanpa memperhatikan huruf besar-kecil, jadi `CASHIER` dan
                    `cashier` bertabrakan.
                  example: AUDITOR
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Role" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/role/{id}:
    get:
      tags: [role]
      summary: Get role by id
      operationId: getRole
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Role" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [role]
      summary: Update role
      operationId: updateRole
      description: >
        Field yang tidak dikirim tidak diubah. Kedua kolom NOT NULL, jadi `null`
        ditolak 400 untuk keduanya.


        **Hati-hati mengganti `nama`.** Nama role akan dipakai pengecekan izin
        begitu middleware otorisasi dibangun, dan mengganti nama role yang sudah
        dipakai memutus pengecekan yang mengacu nama lama. Tidak ada constraint
        database yang bisa mencegahnya. Pensiunkan dengan `is_aktif: false` lalu
        buat role baru.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                nama: { type: string, maxLength: 64 }
                is_aktif: { type: boolean }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Role" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/user:
    get:
      tags: [user]
      summary: List user
      operationId: listUser
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: Cocokkan sebagian pada username, nama lengkap, atau email.
          schema: { type: string, maxLength: 255 }
        - $ref: "#/components/parameters/IsAktif"
        - name: role_id
          in: query
          description: >
            Batasi ke pemegang satu role — "tampilkan semua kasir". Seorang user
            yang punya beberapa role tetap muncul satu kali; filter ini `EXISTS`,
            bukan join, supaya baris tidak berlipat sebanyak rolenya.
          schema: { type: integer, format: int64, minimum: 1 }
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/User" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [user]
      summary: Create user
      operationId: createUser
      description: >
        User dan seluruh grant-nya ditulis dalam satu transaksi: kalau pemberian
        grant gagal, usernya juga tidak jadi dibuat. `grants` boleh dikosongkan —
        user tanpa role bisa dibuat lalu diberi grant lewat PATCH.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password]
              properties:
                username:
                  type: string
                  maxLength: 64
                  description: >
                    Unik tanpa memperhatikan huruf besar-kecil, jadi `Budi` dan
                    `budi` bertabrakan.
                  example: kasir_pagi
                email:
                  type: string
                  format: email
                  maxLength: 255
                  nullable: true
                  description: >
                    Opsional dan unik tanpa memperhatikan huruf besar-kecil.
                    Beberapa user boleh sama-sama tidak punya email — indeks unik
                    PostgreSQL tidak mengikat NULL.
                password: { $ref: "#/components/schemas/Password" }
                nama_lengkap: { type: string, maxLength: 255, nullable: true }
                grants: { $ref: "#/components/schemas/Grants" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/User" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/user/{id}:
    get:
      tags: [user]
      summary: Get user by id
      operationId: getUser
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/User" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [user]
      summary: Update user, termasuk mengatur ulang grant-nya
      operationId: updateUser
      description: >
        Field yang tidak dikirim tidak diubah. Mengirim `null` pada `email` atau
        `nama_lengkap` mengosongkan kolom itu; `username`, `password`, dan
        `is_aktif` NOT NULL sehingga `null` ditolak 400.


        `grants` **mengganti seluruh himpunan**, bukan menambah: tidak dikirim
        berarti grant dibiarkan apa adanya, `[]` mencabut semuanya, dan sebuah
        daftar membuat user berakhir dengan tepat grant yang disebut di dalamnya.
        `null` ditolak, karena `[]` sudah berarti "tanpa grant".


        Grant yang tetap dipegang — pasangan (id_role, id_unit_kerja) yang sama
        persis — tidak dicabut lalu diberikan ulang, jadi `user_role.created_at`
        tetap mencatat kapan pemberian itu benar-benar dimulai. Perbandingannya
        NULL-safe: dua grant global (id_unit_kerja tidak diisi) untuk role yang
        sama dianggap grant yang sama, bukan dua baris berbeda.


        Body yang hanya berisi `grants` tetap menggerakkan `updated_at` user —
        itu tetap perubahan pada user tersebut.


        Perubahan user dan grant-nya berada dalam satu transaksi.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                username: { type: string, maxLength: 64 }
                email: { type: string, format: email, maxLength: 255, nullable: true }
                password: { $ref: "#/components/schemas/Password" }
                nama_lengkap: { type: string, maxLength: 255, nullable: true }
                is_aktif: { type: boolean }
                grants: { $ref: "#/components/schemas/Grants" }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/User" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pelanggan:
    get:
      tags: [pelanggan]
      summary: List pelanggan
      operationId: listPelanggan
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
        - name: search
          in: query
          description: Cocokkan sebagian pada kode atau nama pelanggan.
          schema: { type: string, maxLength: 255 }
        - $ref: "#/components/parameters/IsAktif"
      responses:
        "200":
          description: Halaman hasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Pelanggan" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "400": { $ref: "#/components/responses/ValidationError" }

    post:
      tags: [pelanggan]
      summary: Create pelanggan
      operationId: createPelanggan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nama]
              properties:
                kode:
                  type: string
                  maxLength: 32
                  nullable: true
                  description: >
                    Opsional. Unik tanpa memperhatikan huruf besar-kecil, tapi
                    beberapa pelanggan boleh sama-sama tidak punya kode.
                  example: PLG-01
                nama: { type: string, maxLength: 255, example: "Toko Sejahtera" }
                telepon: { type: string, maxLength: 32, nullable: true }
                alamat: { type: string, maxLength: 1000, nullable: true }
                npwp: { type: string, maxLength: 32, nullable: true }
                plafon_kredit: { $ref: "#/components/schemas/PlafonKredit" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pelanggan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pelanggan/{id}:
    get:
      tags: [pelanggan]
      summary: Get pelanggan by id
      operationId: getPelanggan
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          description: Found
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pelanggan" }
        "404": { $ref: "#/components/responses/Error" }

    patch:
      tags: [pelanggan]
      summary: Update pelanggan
      operationId: updatePelanggan
      description: >
        Field yang tidak dikirim tidak diubah. `"plafon_kredit": null` mencabut
        batas kredit sepenuhnya — itu bukan sama dengan menyetelnya ke 0.
      parameters:
        - $ref: "#/components/parameters/ID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                kode: { type: string, maxLength: 32, nullable: true }
                nama: { type: string, maxLength: 255 }
                telepon: { type: string, maxLength: 32, nullable: true }
                alamat: { type: string, maxLength: 1000, nullable: true }
                npwp: { type: string, maxLength: 32, nullable: true }
                plafon_kredit: { $ref: "#/components/schemas/PlafonKredit" }
                is_aktif: { type: boolean }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Pelanggan" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

  /api/v1/pelanggan/{id}/piutang:
    get:
      tags: [pelanggan]
      summary: Nota KREDIT pelanggan yang masih terbuka
      operationId: listPiutangPelanggan
      description: >
        Isu #10 fase 2 — cerminan `GET /supplier/{id}/utang` di sisi piutang.
        Query, bukan modul: tidak ada tabel dan tidak ada migrasi di belakangnya.


        Hanya nota `KREDIT` yang `POSTED`. Nota tunai tidak pernah jadi piutang —
        ia `LUNAS` sesaat setelah diposting.


        **Diurutkan dari yang paling lama**, sama seperti `utang` di sisi
        supplier: ini antrean tagihan, bukan riwayat.


        `sisa_piutang` sama dengan `total` untuk saat ini — belum ada
        `retur-penjualan` maupun `penerimaan-pembayaran` yang bisa
        menguranginya. Begitu salah satunya ada, angka ini akan mulai
        memperhitungkannya tanpa bentuk responsnya berubah.


        Pelanggan yang tidak dikenal menjawab 404; pelanggan tanpa piutang
        menjawab halaman kosong. Dua fakta yang berbeda.
      parameters:
        - $ref: "#/components/parameters/ID"
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/Size"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/PiutangPelanggan" }
                  paging: { $ref: "#/components/schemas/PageMetadata" }
        "404": { $ref: "#/components/responses/Error" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >
        Token dari `POST /api/v1/auth/login`, dikirim sebagai
        `Authorization: Bearer <token>`.


        Token bersifat stateless dan **tidak bisa dicabut**. Menonaktifkan user atau
        mencabut rolenya tidak mempengaruhi token yang sudah keluar; aksesnya baru
        hilang saat token kedaluwarsa, sesuai `jwt.ttl_minutes` (default 60 menit).

  parameters:
    ID:
      name: id
      in: path
      required: true
      schema: { type: integer, format: int64 }

    IDHarga:
      name: id_harga
      in: path
      required: true
      description: Id baris `product_harga_jual`, bukan id product — lihat `{id}` di path yang sama.
      schema: { type: integer, format: int64 }

    # periode dikunci (tahun, bulan) dan bukan id, menyimpang dari pola /{id} di modul
    # lain dengan sengaja: pasangan itulah identitas sebenarnya sebuah periode, seperti
    # yang sudah dinyatakan periode_tahun_bulan_uidx. Rute ber-id malah tidak bisa
    # menyebut kasus yang paling sering terjadi — bulan yang belum pernah ditutup tidak
    # punya baris, jadi tidak punya id.
    Tahun:
      name: tahun
      in: path
      required: true
      schema: { type: integer, minimum: 2000, maximum: 9999 }

    Bulan:
      name: bulan
      in: path
      required: true
      schema: { type: integer, minimum: 1, maximum: 12 }

    Page:
      name: page
      in: query
      schema: { type: integer, minimum: 1, default: 1 }

    Size:
      name: size
      in: query
      schema: { type: integer, minimum: 1, maximum: 100, default: 20 }

    IsAktif:
      name: is_aktif
      in: query
      description: Kosongkan untuk menampilkan baris aktif maupun tidak aktif.
      schema: { type: boolean }

  schemas:
    Dokumen:
      type: object
      description: >
        Metadata satu lampiran. Isinya diambil lewat `GET /api/v1/dokumen/{id}`, tidak
        pernah disisipkan di sini.


        `path_simpan` sengaja tidak ada di respons: itu letak berkasnya di disk server,
        dan klien tidak mendapat apa pun darinya yang tidak sudah didapat dari endpoint
        unduhan.
      properties:
        id: { type: integer, format: int64 }
        nama_asli:
          type: string
          description: >
            Nama dari klien, disimpan apa adanya **hanya untuk ditampilkan**. Tidak
            pernah dipakai sebagai path.
          example: faktur-sumber-jaya.jpg
        mime:
          type: string
          description: Hasil deteksi isi berkas, bukan header dari klien.
          enum: [image/jpeg, image/png, application/pdf]
        ukuran_byte:
          type: integer
          format: int64
          description: Yang benar-benar terhitung saat ditulis, bukan yang diklaim klien.
        checksum_sha256: { type: string, nullable: true, minLength: 64, maxLength: 64 }
        ref_table:
          type: string
          nullable: true
          description: Null selama masih yatim. Terisi bersamaan dengan `ref_id`.
          enum: [pembelian, penerimaan_susulan, retur_pembelian, pembayaran_utang]
        ref_id: { type: integer, format: int64, nullable: true }
        created_by: { type: integer, format: int64 }
        nama_pembuat: { type: string, nullable: true }
        created_at:
          type: string
          format: date-time
          description: Dasar penentuan umur berkas yatim.
        deleted_at:
          type: string
          format: date-time
          nullable: true
          description: >
            Terisi berarti berkasnya sudah tidak ada di disk — dihapus operator atau
            disapu worker. Barisnya sengaja dipertahankan sebagai jejak.
        duplikat_dari_id:
          type: integer
          format: int64
          nullable: true
          description: >
            Hanya muncul pada respons unggah, dan hanya kalau sudah ada lampiran hidup
            dengan checksum yang sama. Peringatan, bukan penolakan.

    Periode:
      type: object
      description: >
        Status tutup buku satu bulan.


        Tidak ada `id`, dan itu disengaja: identitas sebuah periode adalah pasangan
        `(tahun, bulan)`. Menghilangkan kunci penggantinya membuat bulan yang belum
        pernah ditutup — yang memang tidak punya baris — bisa dijawab dengan bentuk
        yang sama persis, tanpa id null atau nol karangan.
      properties:
        tahun: { type: integer, example: 2026 }
        bulan: { type: integer, minimum: 1, maximum: 12, example: 7 }
        status:
          type: string
          enum: [BUKA, TUTUP]
          description: >
            `TUTUP` menolak setiap penulisan `kartu_stok` bertanggal di bulan ini.
            Bulan tanpa baris menjawab `BUKA`.
        ditutup_oleh: { type: integer, format: int64, nullable: true }
        nama_penutup: { type: string, nullable: true }
        ts_tutup:
          type: string
          format: date-time
          nullable: true
          description: Penutupan **terakhir**, bukan yang pertama.
        dibuka_oleh: { type: integer, format: int64, nullable: true }
        nama_pembuka: { type: string, nullable: true }
        ts_buka:
          type: string
          format: date-time
          nullable: true
          description: >
            Terisi berarti bulan ini pernah dibuka kembali setelah ditutup. Muncul
            bersama `ts_tutup` berarti ditutup, dibuka, lalu ditutup lagi.

    Ruang:
      type: object
      properties:
        id: { type: integer, format: int64 }
        kode: { type: string, nullable: true }
        nama_ruang: { type: string }
        is_aktif: { type: boolean }
        id_unit_kerja: { type: integer, format: int64 }
        nama_unit_kerja:
          type: string
          description: Diambil sekalian lewat JOIN ke unit_kerja, bukan query terpisah.
        created_at: { type: string, format: date-time }
        created_by: { type: integer, format: int64, nullable: true }
        updated_at: { type: string, format: date-time }
        updated_by: { type: integer, format: int64, nullable: true }
        nomor_opname_beku:
          type: string
          nullable: true
          description: >
            Nomor `stok_opname` yang sedang membekukan ruang ini — `null` kalau bebas
            (isu #15). Diambil lewat satu `LEFT JOIN` ke opname berstatus terbuka, bukan
            endpoint terpisah.
          example: SO/KODE/2026/08/0001

    UnitKerja:
      type: object
      properties:
        id: { type: integer, format: int64 }
        kode: { type: string, nullable: true }
        nama: { type: string }
        is_aktif: { type: boolean }
        created_at: { type: string, format: date-time }
        created_by: { type: integer, format: int64, nullable: true }
        nama_pembuat:
          type: string
          nullable: true
          description: >
            Diambil sekalian lewat LEFT JOIN ke users, bukan query terpisah per
            baris. Null selama pembuatnya tidak diisi.
        updated_at: { type: string, format: date-time }
        updated_by: { type: integer, format: int64, nullable: true }

    Satuan:
      type: object
      properties:
        id: { type: integer, format: int64 }
        nama: { type: string }
        is_aktif: { type: boolean }
        created_at: { type: string, format: date-time }
        created_by: { type: integer, format: int64, nullable: true }
        updated_at: { type: string, format: date-time }
        updated_by: { type: integer, format: int64, nullable: true }

    Ekspedisi:
      type: object
      properties:
        id: { type: integer, format: int64 }
        nama: { type: string }
        telepon: { type: string, nullable: true }
        is_aktif: { type: boolean }
        created_at: { type: string, format: date-time }
        created_by: { type: integer, format: int64, nullable: true }
        updated_at: { type: string, format: date-time }
        updated_by: { type: integer, format: int64, nullable: true }

    Supplier:
      type: object
      properties:
        id: { type: integer, format: int64 }
        kode: { type: string, nullable: true }
        nama: { type: string }
        telepon: { type: string, nullable: true }
        alamat: { type: string, nullable: true }
        npwp: { type: string, nullable: true }
        is_aktif: { type: boolean }
        created_at: { type: string, format: date-time }
        created_by: { type: integer, format: int64, nullable: true }
        nama_pembuat:
          type: string
          nullable: true
          description: >
            Diambil sekalian lewat LEFT JOIN ke users, bukan query terpisah per
            baris. Null selama modul autentikasi belum ada.
        updated_at: { type: string, format: date-time }
        updated_by: { type: integer, format: int64, nullable: true }

    Pelanggan:
      type: object
      properties:
        id: { type: integer, format: int64 }
        kode: { type: string, nullable: true }
        nama: { type: string }
        telepon: { type: string, nullable: true }
        alamat: { type: string, nullable: true }
        npwp: { type: string, nullable: true }
        plafon_kredit: { $ref: "#/components/schemas/PlafonKredit" }
        is_aktif: { type: boolean }
        created_at: { type: string, format: date-time }
        created_by: { type: integer, format: int64, nullable: true }
        nama_pembuat:
          type: string
          nullable: true
          description: Diambil lewat LEFT JOIN ke users, bukan query per baris.
        updated_at: { type: string, format: date-time }
        updated_by: { type: integer, format: int64, nullable: true }

    PlafonKredit:
      type: string
      nullable: true
      maxLength: 21
      pattern: '^[0-9]+(\.[0-9]+)?$'
      description: >
        Batas kredit sebagai string desimal, bukan angka floating point, supaya
        nilai yang disimpan PostgreSQL sampai ke klien tanpa pembulatan biner.


        `null` berarti **tanpa batas**, bukan nol. Nilai negatif ditolak 400.
      example: "15000000.00"

    Grant:
      type: object
      description: >
        Satu grant yang dipegang user saat token dibuat — bentuk yang mengisi
        menu penukar konteks (isu #12 fase 4). Cuma yang usable yang muncul di
        sini: role-nya `is_aktif`, dan kalau grant-nya menyebut unit, unit itu
        juga `is_aktif`. Sama seperti `roles` pada `User`, role yang sama boleh
        muncul lebih dari sekali — satu baris per unit tempat ia diberikan.
      properties:
        id_user_role:
          type: integer
          format: int64
          description: Id baris user_role — inilah yang dikirim ke switch-context.
        id_role: { type: integer, format: int64 }
        role: { type: string, example: CASHIER }
        id_unit_kerja:
          type: integer
          format: int64
          nullable: true
          description: 'null berarti grant ini berlaku di seluruh unit.'
        nama_unit_kerja:
          type: string
          nullable: true

    ActiveContext:
      type: object
      nullable: true
      description: >
        Grant yang sedang dipakai sesi ini — isu #12 fase 4. `null` berarti
        pemanggil belum memilih: itu terjadi saat login dengan lebih dari satu
        grant usable, dan sesi seperti itu tidak mengotorisasi apa pun sampai
        `POST /auth/switch-context` dipanggil — `role tidak mencukupi` akan
        menjawab setiap endpoint yang butuh role, termasuk endpoint yang
        sebenarnya dipegang pemanggil di grant lain.
      properties:
        id_user_role: { type: integer, format: int64 }
        role: { type: string, example: CASHIER }
        id_unit_kerja:
          type: integer
          format: int64
          nullable: true

    LoginResult:
      type: object
      properties:
        token:
          type: string
          description: 'Kirim sebagai header: Authorization: Bearer <token>'
        token_type: { type: string, example: Bearer }
        expires_at:
          type: string
          format: date-time
          description: >
            Waktu absolut, bukan durasi, supaya klien tidak perlu menebak kapan
            responsnya diterima. Access token ini sendiri tidak bisa dicabut —
            isu #24 — jadi ini juga batas jendela sisa setelah sesi dicabut
            lewat logout, ganti password, is_aktif=false, atau seluruh grant
            dicabut.
        refresh_token:
          type: string
          nullable: true
          description: >
            Hanya diisi oleh `POST /auth/login` dan `POST /auth/refresh` —
            isu #24 fase 2. `POST /auth/switch-context` menerbitkan access
            token baru tanpa refresh token, persis seperti sebelum isu #24.
            Kirim ke `POST /auth/refresh` untuk menukar sepasang token baru,
            atau ke `POST /auth/logout` untuk mencabutnya. Sekali pakai:
            dipakai ulang (misalnya setelah `refresh`) akan ditolak.
        user: { $ref: "#/components/schemas/User" }
        grants:
          type: array
          description: Seluruh grant usable yang dipegang user. Selalu array, tidak pernah null.
          items: { $ref: "#/components/schemas/Grant" }
        aktif:
          allOf: [{ $ref: "#/components/schemas/ActiveContext" }]
          description: >
            Grant yang aktif di token ini. `null` berarti klien harus memanggil
            `POST /auth/switch-context` sebelum bisa memakai endpoint mana pun
            yang butuh role.

    Session:
      type: object
      description: Isi token, yaitu yang dipakai server untuk memutuskan izin.
      properties:
        user_id: { type: integer, format: int64 }
        username: { type: string }
        grants:
          type: array
          description: >
            Seluruh grant usable yang dipegang user saat token dibuat. Selalu
            array, tidak pernah null.
          items: { $ref: "#/components/schemas/Grant" }
        aktif:
          allOf: [{ $ref: "#/components/schemas/ActiveContext" }]
          description: Grant yang sedang aktif — satu-satunya yang dipakai mengotorisasi.

    Product:
      type: object
      properties:
        id: { type: integer, format: int64 }
        kode_barang: { type: string, example: BRG-001 }
        nama: { type: string, example: Kertas A4 }
        id_satuan_dasar: { type: integer, format: int64 }
        nama_satuan_dasar:
          type: string
          description: Ikut lewat JOIN ke satuan, bukan query terpisah per baris.
        stok_minimum: { type: integer, format: int64 }
        is_aktif: { type: boolean }
        satuan:
          type: array
          description: >
            Hanya pada endpoint detail; tidak ada di list. Diurutkan berdasarkan faktor,
            jadi satuan dasar selalu pertama.
          items: { $ref: "#/components/schemas/ProductSatuan" }
        harga_jual:
          type: array
          description: Hanya pada endpoint detail. Terbaru lebih dulu.
          items: { $ref: "#/components/schemas/ProductHargaJual" }
        created_at: { type: string, format: date-time }
        created_by:
          type: integer
          format: int64
          description: >
            NOT NULL, diambil dari token. Ini modul pertama yang benar-benar mengisi
            kolom pelaku.
        updated_at: { type: string, format: date-time }
        updated_by: { type: integer, format: int64, nullable: true }

    ProductSatuan:
      type: object
      properties:
        id: { type: integer, format: int64 }
        id_satuan: { type: integer, format: int64 }
        nama_satuan: { type: string }
        faktor:
          type: integer
          format: int64
          minimum: 1
          description: >
            Berapa satuan dasar yang dimuat satu satuan ini. Bilangan bulat, karena
            kolomnya BIGINT — satuan yang memuat 2,5 satuan dasar tidak bisa diwakili.
        is_default_input: { type: boolean }

    ProductSatuanInput:
      type: object
      required: [id_satuan, faktor]
      properties:
        id_satuan: { type: integer, format: int64, minimum: 1 }
        faktor: { type: integer, format: int64, minimum: 1 }
        is_default_input:
          type: boolean
          default: false
          description: >
            Maksimal satu per produk, ditegakkan partial unique index
            `product_satuan_default_uidx` dari migrasi 000011. Mengirim dua yang bertanda
            dalam satu request buat menjawab 400.

    ProductHargaJual:
      type: object
      properties:
        id: { type: integer, format: int64 }
        id_satuan: { type: integer, format: int64 }
        nama_satuan: { type: string }
        harga: { $ref: "#/components/schemas/Harga" }
        berlaku_dari: { type: string, format: date }
        berlaku_sampai:
          type: string
          format: date
          nullable: true
          description: >
            **Eksklusif**, dan `null` berarti tanpa batas akhir. Versi yang berlaku pada
            suatu tanggal adalah yang `berlaku_dari <= tanggal` dan `berlaku_sampai` null
            atau lebih besar dari tanggal itu.
        created_at: { type: string, format: date-time }
        created_by: { type: integer, format: int64 }

    Harga:
      type: string
      maxLength: 21
      pattern: "^[0-9]+([.][0-9]+)?$"
      description: >
        String desimal, bukan angka floating point, supaya NUMERIC(20,2) yang disimpan
        PostgreSQL sampai ke klien tanpa pembulatan biner.
      example: "50000.00"

    HargaJualBerlaku:
      type: object
      description: >
        Isu #8 fase 1. Satu versi `product_harga_jual` yang berlaku pada tanggal yang
        diminta — bukan seluruh riwayat seperti `ProductHargaJual` di dalam `Product`.
      properties:
        id_harga_jual:
          type: integer
          format: int64
          description: Ini yang dikirim balik sebagai `penjualan_detail.id_harga_jual`.
        id_satuan: { type: integer, format: int64 }
        nama_satuan: { type: string }
        harga: { $ref: "#/components/schemas/Harga" }
        berlaku_dari: { type: string, format: date }
        berlaku_sampai:
          type: string
          format: date
          nullable: true
          description: Eksklusif; `null` berarti tanpa batas akhir. Sama seperti `ProductHargaJual`.

    DaftarHargaJual:
      type: object
      description: >
        Isu #8 fase 3. Satu baris per produk + satuan yang punya harga berlaku pada
        `tanggal`, plus satu baris per produk yang belum punya harga sama sekali —
        untuk baris itu setiap field di bawah `id_satuan` bernilai `null`.
      properties:
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string, example: BRG-001 }
        nama_product: { type: string }
        is_aktif: { type: boolean }
        id_satuan: { type: integer, format: int64, nullable: true }
        nama_satuan: { type: string, nullable: true }
        id_harga_jual: { type: integer, format: int64, nullable: true }
        harga:
          allOf: [{ $ref: "#/components/schemas/Harga" }]
          nullable: true
        berlaku_dari: { type: string, format: date, nullable: true }
        berlaku_sampai: { type: string, format: date, nullable: true }

    PosProduct:
      type: object
      description: >
        Isu #11. Satu baris per produk untuk layar POS — bersarang, bukan datar:
        `satuan` sebagai array dalam satu baris produk, supaya `total_item` menghitung
        produk dan bukan kombinasi (produk, satuan). Tidak pernah memuat HPP maupun
        kolom audit — lihat deskripsi endpoint.
      properties:
        id: { type: integer, format: int64 }
        kode_barang: { type: string, example: BRG-001 }
        nama: { type: string, example: Aqua Botol 600ml }
        satuan:
          type: array
          description: Tidak pernah kosong — setiap produk selalu punya satuan dasarnya sendiri.
          items: { $ref: "#/components/schemas/PosProductSatuan" }
        stok_akhir:
          type: integer
          format: int64
          description: >
            Selalu dalam satuan dasar, milik `id_ruang` yang diminta saja. Bacaan, bukan
            jaminan — lihat `StokRuang`. Produk yang stoknya habis tetap muncul, dengan
            nilai 0.
          example: 137

    PosProductSatuan:
      type: object
      properties:
        id_satuan: { type: integer, format: int64 }
        nama_satuan: { type: string }
        faktor:
          type: integer
          format: int64
          minimum: 1
          description: Berapa satuan dasar yang dimuat satu satuan ini.
        is_default_input: { type: boolean }
        id_harga_jual:
          type: integer
          format: int64
          nullable: true
          description: >
            Yang dikirim balik sebagai `penjualan_detail.id_harga_jual`. `null` bersama
            `harga` kalau tidak ada versi harga yang berlaku untuk satuan ini pada
            tanggal yang diminta — harga adalah usulan, bukan syarat, dan barang tetap
            boleh dijual tanpanya.
        harga:
          allOf: [{ $ref: "#/components/schemas/Harga" }]
          nullable: true

    Pembelian:
      type: object
      description: >
        Satu dokumen pembelian. Seluruh angka uang dan kuantitas berupa string
        desimal — lihat `Harga` untuk alasannya.
      properties:
        id: { type: integer, format: int64 }
        nomor:
          type: string
          description: Digenerate server, format `BL/2026/08/0001`, mereset tiap bulan.
          example: BL/KODE/2026/08/0001
        tanggal: { type: string, format: date-time }
        id_supplier: { type: integer, format: int64 }
        nama_supplier: { type: string }
        id_ruang: { type: integer, format: int64 }
        nama_ruang: { type: string }
        no_faktur_supplier: { type: string, nullable: true }
        tanggal_faktur: { type: string, format: date, nullable: true }
        subtotal:
          type: string
          description: Jumlah subtotal seluruh baris, mengikuti `qty_faktur`. Dihitung server.
          example: "1000000.00"
        diskon_nota: { type: string, example: "0.00" }
        ppn: { type: string, example: "0.00" }
        ppn_dikreditkan: { type: boolean }
        pembulatan: { type: string, example: "0.00" }
        total:
          type: string
          description: >
            `subtotal - diskon_nota + ppn + pembulatan`. **`biaya_angkut` sengaja
            tidak termasuk** — itu tagihan ekspedisi, bukan utang ke supplier, dan
            masuk ke pembukuan lewat `alokasi_biaya` di tiap baris.
          example: "1000000.00"
        id_ekspedisi: { type: integer, format: int64, nullable: true }
        no_resi: { type: string, nullable: true }
        total_koli: { type: string, nullable: true, example: "3.00" }
        tarif_per_koli: { type: string, nullable: true, example: "50000.00" }
        biaya_angkut:
          type: string
          description: >
            `total_koli x tarif_per_koli`, atau `0.00` kalau `ditanggung_supplier`.
          example: "150000.00"
        ditanggung_supplier: { type: boolean }
        metode_alokasi_angkut:
          type: string
          enum: [KOLI, QTY]
          description: >
            Disimpan per dokumen, jadi mengubah metodenya belakangan tidak pernah
            menulis ulang alokasi lama. `KOLI` jatuh ke `QTY` saat seluruh
            `jumlah_koli` nol, supaya tidak membagi dengan nol.
        jenis_pembayaran: { type: string, enum: [TUNAI, KREDIT] }
        status_pembayaran:
          type: string
          enum: [BELUM, SEBAGIAN, LUNAS]
          description: >
            Cache, dihitung ulang dari alokasi POSTED dan retur POSTED. Tidak
            pernah di-set dari form. Modul pembayaran utang yang mengisinya.
        status_penerimaan:
          type: string
          enum: [LENGKAP, KURANG]
          description: >
            Cache atas baris detailnya: `KURANG` kalau ada baris yang diterima
            kurang dari faktur. Dihitung ulang, tidak pernah di-set dari form.
        status: { type: string, enum: [DRAFT, DIAJUKAN, POSTED, BATAL] }
        detail:
          type: array
          description: Hanya pada endpoint detail. Kuncinya hilang pada list, bukan array kosong.
          items: { $ref: "#/components/schemas/PembelianDetail" }
        created_by: { type: integer, format: int64 }
        created_at: { type: string, format: date-time }
        diajukan_oleh: { type: integer, format: int64, nullable: true }
        diajukan_pada: { type: string, format: date-time, nullable: true }
        disetujui_oleh: { type: integer, format: int64, nullable: true }
        disetujui_pada: { type: string, format: date-time, nullable: true }
        posted_at: { type: string, format: date-time, nullable: true }
        dibatalkan_oleh: { type: integer, format: int64, nullable: true }
        alasan_batal: { type: string, nullable: true }
        alasan_tolak: { type: string, nullable: true }

    PembelianDetail:
      type: object
      description: >
        Satu baris faktur, dengan empat kuantitas dalam dua pasang.
        `qty_faktur`/`qty_dasar` adalah yang tertulis di kertas dan menggerakkan
        utang; `qty_diterima`/`qty_diterima_dasar` adalah hasil hitung fisik dan
        menggerakkan stok. Yang berakhiran `_dasar` dalam satuan dasar produk.
      properties:
        id: { type: integer, format: int64 }
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string }
        nama_product: { type: string }
        qty_faktur: { type: string, example: "100.0000" }
        qty_diterima: { type: string, example: "95.0000" }
        id_satuan_input: { type: integer, format: int64 }
        nama_satuan: { type: string }
        faktor_konversi:
          type: integer
          format: int64
          description: >
            Snapshot dari `product_satuan` saat baris ditulis. Master boleh berubah;
            angka ini tidak, atau dokumen lama diam-diam berganti arti.
        qty_dasar: { type: integer, format: int64, example: 100 }
        qty_diterima_dasar: { type: integer, format: int64, example: 95 }
        qty_susulan_dasar:
          type: integer
          format: int64
          description: >
            Jumlah seluruh penerimaan susulan **POSTED** terhadap baris ini. Draft
            tidak dihitung — draft bukan pengiriman.
          example: 0
        selisih_dasar:
          type: integer
          format: int64
          description: >
            `qty_dasar - qty_diterima_dasar`: yang kurang di kiriman pertama. Beku
            setelah dokumen diposting, berapa pun yang menyusul kemudian.
          example: 5
        sisa_dasar:
          type: integer
          format: int64
          description: >
            `selisih_dasar - qty_susulan_dasar`: yang masih jadi tanggungan supplier
            hari ini. Menyusut setiap kali penerimaan susulan diposting.
          example: 5
        qty_retur_dasar:
          type: integer
          format: int64
          description: >
            Jumlah seluruh retur pembelian **POSTED** terhadap baris ini. Draft tidak
            dihitung — draft adalah daftar kemasan, bukan pengiriman.
          example: 0
        qty_dapat_diretur:
          type: integer
          format: int64
          description: >
            `qty_diterima_dasar + qty_susulan_dasar - qty_retur_dasar`: yang masih bisa
            dikirim balik ke supplier.


            Ini sumbu yang berbeda dari tiga angka di atas, dan mencampurnya adalah
            kesalahan yang perlu dihindari: barang yang diretur tetap pernah diterima,
            jadi retur tidak membuat supplier berutang barang lagi dan tidak memberi hak
            atas kiriman susulan. `sisa_dasar` dan `qty_dapat_diretur` bisa sama-sama
            bukan nol pada baris yang sama.
          example: 95
        nama_satuan_dasar: { type: string }
        harga_satuan_input: { type: string, example: "10000.00" }
        diskon_baris: { type: string, example: "0.00" }
        subtotal: { type: string, example: "1000000.00" }
        jumlah_koli:
          type: string
          description: >
            Desimal: satu baris boleh menempati sebagian koli. Diisi petugas gudang
            saat membongkar, jadi biasanya nol saat pertama diinput.
          example: "1.00"
        alokasi_biaya:
          type: string
          description: >
            Bagian ongkir baris ini. Jumlah seluruh baris **persis** sama dengan
            `biaya_angkut`; sisa pembulatan didorong ke baris dengan basis terbesar.
          example: "50000.00"
        harga_pokok_satuan_dasar:
          type: string
          nullable: true
          description: >
            `(subtotal - bagian diskon nota + bagian PPN + alokasi_biaya) /
            qty_dasar` — **bukan** `harga_satuan_input / faktor_konversi`. Null
            sampai dokumen diposting: belum diketahui, bukan hilang.
          example: "10500.0000"
        keterangan_selisih: { type: string, nullable: true }

    PembelianDetailInput:
      type: object
      required: [id_product, id_satuan_input, qty_faktur, harga_satuan_input]
      properties:
        id_product: { type: integer, format: int64, minimum: 1 }
        id_satuan_input:
          type: integer
          format: int64
          minimum: 1
          description: >
            Harus sudah terdaftar di `product_satuan` produk itu; kalau tidak,
            400. Tidak ada foreign key yang mengikatnya ke sana.
        qty_faktur: { type: string, example: "100" }
        qty_diterima:
          type: string
          nullable: true
          description: >
            Boleh dikosongkan, dan artinya sama dengan `qty_faktur`. Tidak boleh
            melebihi `qty_faktur`. Berbeda dari `qty_faktur` mewajibkan
            `keterangan_selisih`.
          example: "95"
        harga_satuan_input: { type: string, example: "10000" }
        diskon_baris: { type: string, example: "0" }
        jumlah_koli: { type: string, example: "0" }
        keterangan_selisih: { type: string, nullable: true, maxLength: 500 }

    RiwayatBeli:
      type: object
      description: >
        Pembelian POSTED terakhir satu supplier atas satu produk.


        **Dua harga, dua pertanyaan berbeda.** `harga_satuan_dasar` adalah harga faktur
        per satuan dasar setelah diskon baris — itu yang dibandingkan dengan penawaran
        supplier berikutnya. `harga_pokok_satuan_dasar` adalah harga setelah diskon nota,
        bagian PPN, dan ongkir — itu yang dipakai menilai margin. Melaporkan salah satu
        saja membuat satu dari dua pertanyaan itu tidak terjawab.


        `harga_satuan_input` sengaja bukan angka pembanding: satuannya mengikuti apa yang
        diketik, jadi 12.000/DUS dan 1.000/PCS terlihat jauh berbeda padahal sama. Tetap
        dilaporkan karena itu angka yang tercetak di kertas.
      properties:
        id_supplier: { type: integer, format: int64 }
        nama_supplier: { type: string }
        id_pembelian: { type: integer, format: int64 }
        nomor: { type: string, example: BL/KODE/2026/08/0001 }
        tanggal: { type: string, format: date-time }
        no_faktur_supplier: { type: string, nullable: true }
        id_pembelian_detail: { type: integer, format: int64 }
        qty_faktur: { type: string, example: "10.0000" }
        id_satuan_input: { type: integer, format: int64 }
        nama_satuan: { type: string, example: DUS }
        faktor_konversi: { type: integer, format: int64, example: 12 }
        qty_dasar: { type: integer, format: int64, example: 120 }
        nama_satuan_dasar: { type: string, example: PCS }
        harga_satuan_input:
          type: string
          description: Per satuan input, seperti tertulis di nota.
          example: "120000.00"
        diskon_baris: { type: string, example: "0.00" }
        subtotal: { type: string, example: "1200000.00" }
        alokasi_biaya: { type: string, example: "60000.00" }
        harga_satuan_dasar:
          type: string
          description: >
            `subtotal / qty_dasar`. Harga faktur per satuan dasar, jadi bisa dibandingkan
            antar supplier yang memakai kemasan berbeda.
          example: "10000.0000"
        harga_pokok_satuan_dasar:
          type: string
          nullable: true
          description: >
            Harga pokok setelah ongkir, disalin dari baris pembeliannya. Tidak pernah
            null pada endpoint ini — hanya dokumen POSTED yang dibaca, dan posting yang
            mengisinya.
          example: "10500.0000"

    SisaPembelian:
      type: object
      properties:
        id: { type: integer, format: int64 }
        nomor: { type: string }
        id_supplier: { type: integer, format: int64 }
        nama_supplier: { type: string }
        status: { type: string, enum: [DRAFT, DIAJUKAN, POSTED, BATAL] }
        status_penerimaan: { type: string, enum: [LENGKAP, KURANG] }
        baris:
          type: array
          items:
            type: object
            properties:
              id_pembelian_detail: { type: integer, format: int64 }
              id_product: { type: integer, format: int64 }
              kode_barang: { type: string }
              nama_product: { type: string }
              qty_dasar: { type: integer, format: int64 }
              qty_diterima_dasar: { type: integer, format: int64 }
              qty_susulan_dasar: { type: integer, format: int64 }
              sisa_dasar: { type: integer, format: int64 }
              nama_satuan_dasar: { type: string }
              keterangan_selisih: { type: string, nullable: true }

    PenerimaanSusulan:
      type: object
      description: >
        Kiriman kedua untuk barang yang belum datang di kiriman pertama.


        Tidak punya `total`, `ppn`, maupun `status_pembayaran`, karena ia bukan
        tagihan: faktur supplier terbit penuh di kiriman pertama dan sudah dibukukan
        penuh di sana. `total_nilai` adalah nilai barang yang datang terlambat, yang
        dibawa masuk ke persediaan — bukan uang yang terutang.
      properties:
        id: { type: integer, format: int64 }
        nomor:
          type: string
          description: Seri sendiri, terpisah dari pembelian.
          example: PS/KODE/2026/08/0001
        tanggal: { type: string, format: date-time }
        id_pembelian: { type: integer, format: int64 }
        nomor_pembelian: { type: string }
        id_supplier:
          type: integer
          format: int64
          description: Disalin dari pembelian, bukan dipilih.
        nama_supplier: { type: string }
        id_ruang:
          type: integer
          format: int64
          description: >
            Disalin dari pembelian. Barang yang perlu pindah ruang setelah diterima
            adalah pekerjaan mutasi, bukan penerimaan.
        nama_ruang: { type: string }
        keterangan: { type: string, nullable: true }
        total_nilai:
          type: string
          description: Nilai barang yang menyusul. Dihitung ulang dari barisnya.
          example: "52500.00"
        status: { type: string, enum: [DRAFT, DIAJUKAN, POSTED, BATAL] }
        detail:
          type: array
          description: Hanya pada endpoint detail. Kuncinya hilang pada list.
          items: { $ref: "#/components/schemas/PenerimaanSusulanDetail" }
        created_by: { type: integer, format: int64 }
        created_at: { type: string, format: date-time }
        diajukan_oleh: { type: integer, format: int64, nullable: true }
        diajukan_pada: { type: string, format: date-time, nullable: true }
        disetujui_oleh: { type: integer, format: int64, nullable: true }
        disetujui_pada: { type: string, format: date-time, nullable: true }
        posted_at: { type: string, format: date-time, nullable: true }
        dibatalkan_oleh: { type: integer, format: int64, nullable: true }
        alasan_batal: { type: string, nullable: true }
        alasan_tolak: { type: string, nullable: true }

    PenerimaanSusulanDetail:
      type: object
      description: >
        Satu baris, menunjuk baris faktur yang dilengkapinya — produknya adalah
        produk baris itu.
      properties:
        id: { type: integer, format: int64 }
        id_pembelian_detail: { type: integer, format: int64 }
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string }
        nama_product: { type: string }
        qty_input: { type: string, example: "5.0000" }
        id_satuan_input: { type: integer, format: int64 }
        nama_satuan: { type: string }
        faktor_konversi:
          type: integer
          format: int64
          description: >
            Snapshot dari `product_satuan` saat baris ditulis. Diambil segar karena
            kuantitasnya adalah hitungan baru dan boleh dalam satuan yang berbeda
            dari yang dipakai faktur.
        qty_dasar: { type: integer, format: int64, example: 5 }
        nama_satuan_dasar: { type: string }
        harga_pokok_satuan_dasar:
          type: string
          description: >
            **Disalin dari baris pembelian**, tidak pernah dihitung ulang dan tidak
            pernah dibaca dari rata-rata bergerak terkini. Harga barang sudah
            ditetapkan faktur; kiriman keduanya tidak mengubahnya. Ini juga yang
            membuat pembelian dan seluruh susulannya berjumlah persis nilai faktur.
          example: "10500.0000"
        nilai:
          type: string
          description: "`qty_dasar x harga_pokok_satuan_dasar`."
          example: "52500.00"

    PenerimaanSusulanDetailInput:
      type: object
      required: [id_pembelian_detail, id_satuan_input, qty_input]
      properties:
        id_pembelian_detail:
          type: integer
          format: int64
          minimum: 1
          description: >
            Harus milik pembelian di header, dan harga pokoknya harus sudah terisi
            (yaitu pembeliannya sudah POSTED). Satu baris pembelian hanya boleh
            muncul sekali per dokumen.
        id_satuan_input:
          type: integer
          format: int64
          minimum: 1
          description: >
            Harus terdaftar di `product_satuan` produk itu. Boleh berbeda dari satuan
            yang dipakai baris fakturnya — lima pcs kurang dari baris yang diketik
            dalam dus adalah kasus yang wajar.
        qty_input:
          type: string
          description: >
            Tidak boleh melebihi sisa baris itu
            (`qty_dasar - qty_diterima_dasar - Σ susulan POSTED`), setelah dikonversi
            ke satuan dasar. `qty x faktor` wajib bilangan bulat.
          example: "5"

    ReturPembelian:
      type: object
      description: >
        Barang yang dikembalikan ke supplier. Cermin dari penerimaan susulan: sama-sama
        menunjuk baris `pembelian_detail` dan sama-sama menyalin harga pokoknya dari
        sana, arah barangnya saja yang berlawanan.


        Tidak punya `ppn` maupun `status_pembayaran`. Berapa yang dikreditkan supplier
        diselesaikan dengan mereka di atas nota; dokumen ini mencatat apa yang keluar
        dari gudang dan berapa nilainya.
      properties:
        id: { type: integer, format: int64 }
        nomor:
          type: string
          description: Seri sendiri, terpisah dari pembelian dan penerimaan susulan.
          example: RB/KODE/2026/08/0001
        tanggal: { type: string, format: date-time }
        id_pembelian: { type: integer, format: int64 }
        nomor_pembelian: { type: string }
        id_supplier:
          type: integer
          format: int64
          description: Disalin dari pembelian, bukan dipilih.
        nama_supplier: { type: string }
        id_ruang:
          type: integer
          format: int64
          description: >
            Disalin dari pembelian — tempat barangnya ditaruh saat datang. Barang yang
            perlu dikembalikan dari ruang lain adalah pekerjaan mutasi lebih dulu.
        nama_ruang: { type: string }
        alasan:
          type: string
          nullable: true
          description: >
            Nullable di skema tetapi wajib diisi aplikasi. Ia satu-satunya catatan kenapa
            barang yang sudah dibayar dikirim balik.
        total:
          type: string
          description: >
            Nilai persediaan yang keluar menurut faktur, dihitung ulang dari barisnya dan
            tidak pernah di-set dari form. **Bukan otomatis sama dengan yang dikreditkan
            supplier**, dan bukan angka yang dicatat kartu stok — lihat
            `harga_pokok_satuan_dasar` pada barisnya.
          example: "1000000.00"
        nilai_kredit_utang:
          type: string
          description: >
            Berapa yang dikreditkan supplier atas barang ini, dan **bukan `total`**.
            `total` adalah nilai persediaan menurut harga pokok, dan harga pokok memuat
            porsi ongkir yang dibayar ke ekspedisi — mengkreditkannya berarti memberi
            supplier uang yang tidak pernah ia terima.


            Dihitung `pembelian.total x nilai_faktur_retur / pembelian.subtotal`, jadi
            diskon nota dan PPN ikut terskala. Nol selama dokumen belum POSTED, dibekukan
            saat posting, dan tetap tertinggal pada dokumen `BATAL` sebagai catatan berapa
            yang pernah diklaim — penghitungan ulang utang hanya menjumlahkan retur POSTED.
          example: "1000000.00"
        status: { type: string, enum: [DRAFT, DIAJUKAN, POSTED, BATAL] }
        detail:
          type: array
          description: Hanya pada endpoint detail. Kuncinya hilang pada list.
          items: { $ref: "#/components/schemas/ReturPembelianDetail" }
        created_by: { type: integer, format: int64 }
        created_at: { type: string, format: date-time }
        diajukan_oleh: { type: integer, format: int64, nullable: true }
        diajukan_pada: { type: string, format: date-time, nullable: true }
        disetujui_oleh: { type: integer, format: int64, nullable: true }
        disetujui_pada: { type: string, format: date-time, nullable: true }
        posted_at: { type: string, format: date-time, nullable: true }
        dibatalkan_oleh: { type: integer, format: int64, nullable: true }
        alasan_batal: { type: string, nullable: true }
        alasan_tolak: { type: string, nullable: true }

    ReturPembelianDetail:
      type: object
      description: >
        Satu baris, menunjuk baris faktur yang barangnya masuk lewat sana — produknya
        adalah produk baris itu.
      properties:
        id: { type: integer, format: int64 }
        id_pembelian_detail: { type: integer, format: int64 }
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string }
        nama_product: { type: string }
        qty_input: { type: string, example: "1.0000" }
        id_satuan_input: { type: integer, format: int64 }
        nama_satuan: { type: string }
        faktor_konversi:
          type: integer
          format: int64
          description: >
            Snapshot dari `product_satuan` saat baris ditulis. Diambil segar karena
            kuantitasnya adalah hitungan baru atas barang yang sedang dikemas dan boleh
            dalam satuan yang berbeda dari yang dipakai faktur.
        qty_dasar: { type: integer, format: int64, example: 12 }
        nama_satuan_dasar: { type: string }
        harga_pokok_satuan_dasar:
          type: string
          description: >
            **Disalin dari baris pembelian**, tidak pernah dibaca dari rata-rata bergerak
            terkini. Tanpa itu pembelian dan returnya tidak saling menghapus — dan itulah
            yang menurut migrasi `000005` menjadi alasan kolom ini ada.
          example: "10000.0000"
        nilai:
          type: string
          description: >
            `qty_dasar x harga_pokok_satuan_dasar` — nilai barangnya menurut faktur.
            **Bukan** yang dicatat `kartu_stok` sebagai keluar: trigger menilai setiap
            baris keluar pada rata-rata bergerak yang berlaku saat itu, karena barangnya
            sudah tercampur dengan stok lama sejak ia datang. Dua-duanya jawaban benar
            untuk pertanyaan yang berbeda.
          example: "120000.00"

    ReturPembelianDetailInput:
      type: object
      required: [id_pembelian_detail, id_satuan_input, qty_input]
      properties:
        id_pembelian_detail:
          type: integer
          format: int64
          minimum: 1
          description: >
            Harus milik pembelian di header, dan harga pokoknya harus sudah terisi (yaitu
            pembeliannya sudah POSTED). Satu baris pembelian hanya boleh muncul sekali per
            dokumen.
        id_satuan_input:
          type: integer
          format: int64
          minimum: 1
          description: >
            Harus terdaftar di `product_satuan` produk itu. Boleh berbeda dari satuan yang
            dipakai baris fakturnya — satu dus dikembalikan dari baris yang diketik dalam
            pcs adalah kasus yang wajar.
        qty_input:
          type: string
          description: >
            Tidak boleh melebihi yang bisa diretur
            (`qty_diterima_dasar + Σ susulan POSTED − Σ retur POSTED`), setelah dikonversi
            ke satuan dasar. Perhatikan bahwa `qty_dasar` tidak ikut: barang yang tidak
            pernah datang tidak bisa dikirim balik. `qty x faktor` wajib bilangan bulat.
          example: "1"

    Mutasi:
      type: object
      description: >
        Perpindahan barang antar `ruang`. Dokumen keempat yang menulis `kartu_stok` dan
        yang pertama menulis ke dua arah sekaligus: satu baris detail menghasilkan
        `MUTASI_KELUAR` di ruang asal dan `MUTASI_MASUK` di ruang tujuan, dalam satu
        transaksi.


        Tidak ada satu pun kolom uang di header, dan ketiadaan itu adalah bentuk modulnya
        — bukan kelalaian. Mutasi tidak menagih siapa pun: ia memindahkan barang, dan
        berapa nilainya diputuskan per baris oleh trigger `kartu_stok` saat posting.


        Alurnya `DRAFT → POSTED → BATAL`, tanpa `DIAJUKAN` — satu-satunya penulis
        `kartu_stok` yang begitu. Karena itu tidak ada `diajukan_oleh`, `disetujui_oleh`,
        maupun `alasan_tolak` di sini.
      properties:
        id: { type: integer, format: int64 }
        nomor:
          type: string
          description: Seri sendiri, terpisah dari dokumen lain.
          example: MT/KODE/2026/08/0001
        tanggal: { type: string, format: date-time }
        id_ruang_asal: { type: integer, format: int64 }
        nama_ruang_asal: { type: string }
        id_ruang_tujuan:
          type: integer
          format: int64
          description: Wajib berbeda dari `id_ruang_asal` (`mutasi_ruang_check`).
        nama_ruang_tujuan: { type: string }
        keterangan: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, POSTED, BATAL] }
        detail:
          type: array
          description: Hanya pada endpoint detail. Kuncinya hilang pada list.
          items: { $ref: "#/components/schemas/MutasiDetail" }
        created_by: { type: integer, format: int64 }
        created_at: { type: string, format: date-time }
        posted_at: { type: string, format: date-time, nullable: true }
        dibatalkan_oleh: { type: integer, format: int64, nullable: true }
        alasan_batal: { type: string, nullable: true }

    MutasiDetail:
      type: object
      description: >
        Satu baris: satu produk dan satu kuantitas, berpindah di antara kedua ruang yang
        disebut header. Menunjuk produk secara langsung, tidak seperti
        `penerimaan-susulan` dan `retur-pembelian` yang menunjuk baris dokumen lain —
        mutasi tidak punya dokumen induk.
      properties:
        id: { type: integer, format: int64 }
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string }
        nama_product: { type: string }
        qty_input: { type: string, example: "30.0000" }
        id_satuan_input: { type: integer, format: int64 }
        nama_satuan: { type: string }
        faktor_konversi:
          type: integer
          format: int64
          description: Snapshot dari `product_satuan` saat baris ditulis.
        qty_dasar: { type: integer, format: int64, example: 30 }
        nama_satuan_dasar: { type: string }
        harga_pokok_satuan_dasar:
          type: string
          nullable: true
          description: >
            **Null sampai dokumennya diposting**, dan itu memang gunanya kolom ini
            nullable. Harga pokok mutasi adalah rata-rata bergerak ruang asal pada saat
            posting, dan aplikasi tidak bisa menghitungnya sendiri: angka itu hanya
            diketahui trigger `kartu_stok`, yang membacanya di dalam advisory lock. Yang
            tersimpan di sini adalah salinan dari apa yang dilaporkan baris keluar.
          example: "10000.0000"

    MutasiDetailInput:
      type: object
      required: [id_product, id_satuan_input, qty_input]
      properties:
        id_product:
          type: integer
          format: int64
          minimum: 1
          description: >
            Boleh muncul lebih dari sekali dalam satu dokumen, tidak seperti tiga dokumen
            yang kuotanya dipegang baris dokumen induk. Di sini kuotanya saldo ruang asal
            dan seluruh baris untuk produk yang sama dijumlahkan terhadapnya — 2 DUS dan
            3 PCS barang yang sama adalah dua baris yang sah.
        id_satuan_input:
          type: integer
          format: int64
          minimum: 1
          description: Harus terdaftar di `product_satuan` produk itu.
        qty_input:
          type: string
          description: >
            Setelah dikonversi ke satuan dasar, jumlah seluruh baris untuk satu produk
            tidak boleh melebihi saldo ruang asal. `qty x faktor` wajib bilangan bulat.
          example: "30"

    StokOpname:
      type: object
      description: >
        Satu sesi hitung fisik terhadap satu `ruang` — isu #15. Dokumen ketujuh yang
        menulis `kartu_stok`, dan satu-satunya yang tidak memindahkan barang ke mana
        pun: yang berubah cuma pengakuan sistem atas apa yang ada di rak.


        Selama `status` `DRAFT` atau `DIAJUKAN`, `id_ruang` **beku**: trigger
        `kartu_stok` menolak posting apa pun ke ruang itu dari modul mana pun.
        `GET /api/v1/ruang` menyertakan `nomor_opname_beku` untuk melihat sebabnya.
      properties:
        id: { type: integer, format: int64 }
        nomor:
          type: string
          description: Seri sendiri, terpisah dari dokumen lain.
          example: SO/KODE/2026/08/0001
        id_ruang: { type: integer, format: int64 }
        nama_ruang: { type: string }
        tgl_buka: { type: string, format: date }
        tgl_tutup:
          type: string
          format: date
          nullable: true
          description: Diisi saat `ajukan` — tanggal hitungannya selesai, bukan dibukukan.
        ts_cutoff:
          type: string
          format: date-time
          description: >
            Diisi server dari `now()` saat dokumen dibuka, tidak pernah dari body.
            Titik waktu snapshot saldo yang ditarik `tarik-saldo`.
        uraian_so: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, DIAJUKAN, POSTED, BATAL] }
        jumlah_baris:
          type: integer
          description: Hanya pada endpoint detail, dari panjang `detail`.
        jumlah_belum_dihitung:
          type: integer
          description: Baris yang `stok_so`-nya masih `null`. Hanya pada endpoint detail.
        detail:
          type: array
          description: Hanya pada endpoint detail. Kuncinya hilang pada list.
          items: { $ref: "#/components/schemas/StokOpnameDetail" }
        created_by: { type: integer, format: int64 }
        created_at: { type: string, format: date-time }
        verified_by:
          type: integer
          format: int64
          nullable: true
          description: >
            Diisi oleh `posting` **dan** `tolak` — skemanya tidak punya kolom
            terpisah untuk penolak.
        ts_verified: { type: string, format: date-time, nullable: true }
        posted_at: { type: string, format: date-time, nullable: true }
        dibatalkan_oleh: { type: integer, format: int64, nullable: true }
        alasan_batal: { type: string, nullable: true }
        ts_batal: { type: string, format: date-time, nullable: true }

    StokOpnameDetail:
      type: object
      description: >
        Satu baris terhitung: satu produk di ruang header, saldo sistem saat
        cutoff, dan hasil hitung fisik terhadapnya.
      properties:
        id: { type: integer, format: int64 }
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string }
        nama_product: { type: string }
        nama_satuan_dasar: { type: string }
        stok_awal:
          type: integer
          format: int64
          description: >
            Saldo sistem dibekukan saat `tarik-saldo` (atau saat baris ditambah
            manual), tidak pernah berubah lagi. Selisih selalu dihitung terhadap
            angka ini, bukan terhadap saldo saat ini.
        stok_so:
          type: integer
          format: int64
          nullable: true
          description: >
            **`null` berarti belum dihitung, bukan nol.** Baris `null` dilewati
            sepenuhnya saat posting — membacanya sebagai nol menghapus seluruh stok
            tercatat produk itu.
        stok_selisih_lebih:
          type: integer
          format: int64
          description: Selalu dihitung ulang dari `stok_so` terhadap `stok_awal`.
        stok_selisih_kurang:
          type: integer
          format: int64
          description: Selalu dihitung ulang. Tidak pernah positif bersamaan dengan yang di atas.
        keterangan: { type: string, nullable: true }
        id_kartu_stok_penyesuaian:
          type: integer
          format: int64
          nullable: true
          description: >
            `null` sampai diposting, dan tetap `null` selamanya untuk baris yang
            selisihnya nol — tidak ada baris `kartu_stok` yang ditulis untuknya.

    StokOpnameDetailInput:
      type: object
      required: [id_product]
      properties:
        id_product:
          type: integer
          format: int64
          minimum: 1
          description: >
            Pasangan `(produk, ruang header)` harus sudah punya baris `kartu_stok` —
            produk yang belum pernah bergerak di ruang ini tidak bisa diopname di
            sini sama sekali.

    Pemakaian:
      type: object
      description: >
        Barang yang dipakai sendiri — perbaikan, kantor, contoh — keluar dari gudang
        tanpa nota penjualan dan tanpa retur. Dokumen kelima yang menulis
        `kartu_stok`, dan yang pertama mengeluarkan barang tanpa lawan transaksi:
        tidak ada supplier, tidak ada pelanggan, tidak ada ruang tujuan.


        Alurnya `DRAFT → DIAJUKAN → DISETUJUI → POSTED`, dengan `DITOLAK` sebagai
        cabang dari `DIAJUKAN`. Dua hal berbeda dari `pembelian`: `DITOLAK` di sini
        **terminal**, tidak kembali ke `DRAFT` — penolakan adalah keputusan bisnis,
        bukan kertas yang salah ketik — dan ada tahap `DISETUJUI` tersendiri sebelum
        posting, karena persetujuan memutuskan berapa yang boleh keluar sementara
        posting mencatat bahwa barangnya benar-benar sudah keluar.
      properties:
        id: { type: integer, format: int64 }
        nomor:
          type: string
          description: Seri sendiri, prefix `PM`.
          example: PM/KODE/2026/08/0001
        tanggal: { type: string, format: date-time }
        id_ruang: { type: integer, format: int64 }
        nama_ruang: { type: string }
        id_pemohon:
          type: integer
          format: int64
          description: >
            Bukan `created_by`. Staf administrasi boleh mengetikkan permintaan atas
            nama montir yang tidak punya akun sendiri — `created_by` mencatat siapa
            yang mengetik, `id_pemohon` siapa yang meminta barangnya.
        nama_pemohon: { type: string }
        keperluan: { type: string }
        status:
          type: string
          enum: [DRAFT, DIAJUKAN, DISETUJUI, DITOLAK, POSTED, BATAL]
        disetujui_oleh:
          type: integer
          format: int64
          nullable: true
          description: >
            Kolom yang sama dipakai untuk mencatat siapa yang menyetujui **maupun**
            siapa yang menolak — skemanya tidak punya kolom `ditolak_oleh` sendiri.
            `pemakaian_penyetuju_check` menjamin nilainya tidak pernah sama dengan
            `id_pemohon`.
        ts_disetujui: { type: string, format: date-time, nullable: true }
        catatan_persetujuan:
          type: string
          nullable: true
          description: Juga menampung alasan penolakan.
        total_hpp:
          type: string
          nullable: true
          description: >
            Null sampai `POSTED`. Jumlah `hpp_total` seluruh baris — nilai barang
            yang dipakai sendiri, satu-satunya angka yang menjawab berapa yang
            dihabiskan.
          example: "600000.00"
        detail:
          type: array
          description: Hanya pada endpoint detail. Kuncinya hilang pada list.
          items: { $ref: "#/components/schemas/PemakaianDetail" }
        created_by: { type: integer, format: int64 }
        created_at: { type: string, format: date-time }
        posted_at: { type: string, format: date-time, nullable: true }
        dibatalkan_oleh: { type: integer, format: int64, nullable: true }
        alasan_batal: { type: string, nullable: true }

    PemakaianDetail:
      type: object
      description: >
        Satu baris: satu produk, apa yang diminta, dan — setelah ditinjau — berapa
        yang benar-benar boleh keluar.
      properties:
        id: { type: integer, format: int64 }
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string }
        nama_product: { type: string }
        qty_input: { type: string, example: "10.0000" }
        id_satuan_input: { type: integer, format: int64 }
        nama_satuan: { type: string }
        faktor_konversi: { type: integer, format: int64 }
        qty_dasar:
          type: integer
          format: int64
          description: >
            Data perencanaan: yang diminta, dibekukan saat baris ditulis dan tidak
            pernah berubah lagi — termasuk sesudah disetujui.
          example: 10
        nama_satuan_dasar: { type: string }
        qty_disetujui_dasar:
          type: integer
          format: int64
          nullable: true
          description: >
            Null sampai disetujui. Yang benar-benar dibaca saat posting, tidak pernah
            `qty_dasar`. 0 berarti baris ini ditolak sendirian meski dokumennya
            secara keseluruhan disetujui.
        hpp_satuan_dasar:
          type: string
          nullable: true
          description: Null sampai diposting, disalin dari baris keluar `kartu_stok`.
        hpp_total: { type: string, nullable: true }
        keterangan: { type: string, nullable: true }

    PemakaianDetailInput:
      type: object
      required: [id_product, id_satuan_input, qty_input]
      properties:
        id_product:
          type: integer
          format: int64
          minimum: 1
          description: >
            Boleh muncul lebih dari sekali dalam satu dokumen — 1 DUS untuk bengkel,
            3 PCS untuk kantor adalah permintaan yang sah, masing-masing dengan
            `keterangan`-nya sendiri.
        id_satuan_input:
          type: integer
          format: int64
          minimum: 1
          description: Harus terdaftar di `product_satuan` produk itu.
        qty_input: { type: string, example: "10" }
        keterangan: { type: string, maxLength: 500, nullable: true }

    SetujuiPemakaianDetailInput:
      type: object
      required: [id_detail, qty_disetujui_dasar]
      properties:
        id_detail: { type: integer, format: int64, minimum: 1 }
        qty_disetujui_dasar:
          type: integer
          format: int64
          minimum: 0
          description: Diperiksa terhadap `qty_dasar` baris itu sendiri di usecase.

    Penjualan:
      type: object
      description: >
        Satu nota keluar — dokumen keenam yang menulis `kartu_stok`, dan yang
        pertama mengeluarkan barang ke pihak luar dengan uang di sisi lain.
        `pembelian` membentuk utang, `mutasi` tidak membentuk apa-apa, ini yang
        pertama membentuk **piutang**, dan hanya pada nota `KREDIT`.


        Alurnya `DRAFT → POSTED → BATAL`, tanpa `DIAJUKAN` — sama seperti
        `mutasi`, tapi alasannya berbeda: kasir tidak bisa membuat pembeli
        menunggu persetujuan atasan untuk nota tunai yang diketik dalam hitungan
        detik. Kendali dua orangnya pindah seluruhnya ke sisi pembatalan:
        `CASHIER` membuat, mengetik baris, dan memposting; `SUPERADMIN` satu-
        satunya yang boleh membatalkan.
      properties:
        id: { type: integer, format: int64 }
        nomor:
          type: string
          description: Seri sendiri, prefix `PJ`.
          example: PJ/KODE/2026/08/0001
        tanggal: { type: string, format: date-time }
        id_ruang: { type: integer, format: int64 }
        nama_ruang: { type: string }
        id_pelanggan:
          type: integer
          format: int64
          nullable: true
          description: >
            Null untuk nota tunai di depan meja. `penjualan_kredit_pelanggan_check`
            mewajibkannya begitu `jenis_pembayaran` KREDIT.
        nama_pelanggan: { type: string }
        subtotal:
          type: string
          description: Jumlah `subtotal` seluruh baris.
          example: "150000.00"
        diskon_nota: { type: string, example: "0.00" }
        pembulatan: { type: string, example: "0.00" }
        total:
          type: string
          description: "`subtotal - diskon_nota + pembulatan`."
          example: "150000.00"
        total_hpp:
          type: string
          nullable: true
          description: >
            Null sampai `POSTED`. Jumlah `hpp_total` seluruh baris — begitu terisi,
            margin nota tinggal `total - total_hpp`, tanpa tabel baru.
          example: "100000.00"
        jenis_pembayaran: { type: string, enum: [TUNAI, KREDIT] }
        status_pembayaran:
          type: string
          enum: [BELUM, SEBAGIAN, LUNAS]
          description: >
            Cache, selalu dihitung ulang. Nota `TUNAI` yang `POSTED` langsung
            `LUNAS` — uangnya diterima di meja dan tidak ada dokumen alokasi yang
            menunjuknya. Nota `KREDIT` tetap `BELUM` sampai modul
            `penerimaan-pembayaran` ada.
        status: { type: string, enum: [DRAFT, POSTED, BATAL] }
        detail:
          type: array
          description: Hanya pada endpoint detail. Kuncinya hilang pada list.
          items: { $ref: "#/components/schemas/PenjualanDetail" }
        created_by: { type: integer, format: int64 }
        created_at: { type: string, format: date-time }
        posted_at: { type: string, format: date-time, nullable: true }
        dibatalkan_oleh: { type: integer, format: int64, nullable: true }
        alasan_batal: { type: string, nullable: true }

    PenjualanDetail:
      type: object
      description: >
        Satu baris: satu produk, apa yang ditagihkan, dan — setelah diposting —
        berapa harga pokoknya.
      properties:
        id: { type: integer, format: int64 }
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string }
        nama_product: { type: string }
        qty_input: { type: string, example: "10.0000" }
        id_satuan_input: { type: integer, format: int64 }
        nama_satuan: { type: string }
        faktor_konversi: { type: integer, format: int64 }
        qty_dasar: { type: integer, format: int64, example: 10 }
        nama_satuan_dasar: { type: string }
        id_harga_jual:
          type: integer
          format: int64
          nullable: true
          description: >
            Usulan, bukan keharusan. Hanya mencatat versi daftar harga mana yang
            jadi asal `harga_satuan_input` — produk yang belum punya harga tetap
            boleh dijual dengan harga ketik manual.
        harga_satuan_input:
          type: string
          description: >
            Yang benar-benar ditagihkan, snapshot seperti `faktor_konversi`. Tidak
            pernah dipaksa sama dengan `product_harga_jual` — tawar-menawar
            terjadi, dan yang tertulis di nota yang benar.
          example: "15000.00"
        diskon_baris: { type: string, example: "0.00" }
        subtotal:
          type: string
          description: "`qty_input x harga_satuan_input - diskon_baris`."
          example: "150000.00"
        hpp_satuan_dasar:
          type: string
          nullable: true
          description: Null sampai diposting, disalin dari baris keluar `kartu_stok`.
          example: "10000.0000"
        hpp_total: { type: string, nullable: true, example: "100000.00" }

    PenjualanDetailInput:
      type: object
      required: [id_product, id_satuan_input, qty_input, harga_satuan_input]
      properties:
        id_product:
          type: integer
          format: int64
          minimum: 1
          description: >
            Boleh muncul lebih dari sekali dalam satu dokumen, mengikuti
            `mutasi`/`pemakaian`: kuotanya saldo ruang, dijumlahkan seluruh baris
            terhadapnya.
        id_satuan_input:
          type: integer
          format: int64
          minimum: 1
          description: Harus terdaftar di `product_satuan` produk itu.
        qty_input: { type: string, example: "10" }
        id_harga_jual:
          type: integer
          format: int64
          minimum: 1
          nullable: true
          description: >
            Kalau diisi, divalidasi: harus milik `id_product`/`id_satuan_input`
            baris ini dan berlaku pada `tanggal` dokumen.
        harga_satuan_input: { type: string, example: "15000" }
        diskon_baris: { type: string, example: "0" }

    PiutangPelanggan:
      type: object
      description: >
        Satu nota `KREDIT` yang `POSTED` dan masih punya sisa — cerminan
        `UtangSupplier` di sisi piutang (isu #10 fase 2). Sejak isu #20,
        `sisa_piutang` adalah `total` dikurangi alokasi efektif dari
        `penerimaan-pembayaran` — bukan lagi selalu sama dengan `total`. Bentuk
        responsnya sendiri tidak berubah; `retur-penjualan` masih di luar cakupan dan
        belum ikut menguranginya.
      properties:
        id_penjualan: { type: integer, format: int64 }
        nomor: { type: string, example: PJ/KODE/2026/08/0001 }
        tanggal: { type: string, format: date-time }
        total: { type: string, example: "150000.00" }
        sisa_piutang: { type: string, example: "150000.00" }

    StokRuang:
      type: object
      description: >
        Saldo satu produk di satu ruang: baris terakhir sebuah rantai `kartu_stok`,
        dibaca dan bukan dihitung ulang.


        Bukan jaminan. Angkanya bisa sudah bergerak sebelum klien menindaklanjutinya —
        yang memutuskan saldo adalah trigger, di dalam advisory lock.
      properties:
        id_ruang: { type: integer, format: int64 }
        nama_ruang: { type: string }
        stok_akhir:
          type: integer
          format: int64
          description: Selalu dalam satuan dasar. `kartu_stok` tidak mengenal satuan lain.
          example: 70
        nilai_akhir:
          type: string
          description: >
            Nilai persediaan atas kuantitas itu. Stok nol memaksa nilainya tepat nol,
            sehingga sisa pembulatan tidak menumpuk jadi rupiah hantu di gudang kosong.
          example: "700000.00"
        harga_pokok_satuan:
          type: string
          description: Rata-rata bergerak yang berlaku di ruang itu.
          example: "10000.0000"
        tanggal_transaksi:
          type: string
          format: date-time
          nullable: true
          description: Tanggal pergerakan terakhir menurut dokumennya.
        terakhir_diperbarui:
          type: string
          format: date-time
          nullable: true
          description: >
            Kapan pergerakan itu dicatat. Berbeda dari `tanggal_transaksi` setiap kali
            dokumennya diketik terlambat, yang merupakan kasus biasa.

    KartuStok:
      type: object
      description: >
        Satu baris riwayat pergerakan satu produk di satu ruang — isu #22 fase 1.
        Setiap kuantitas sudah dalam satuan dasar; `qty_input`/`nama_satuan_input`
        hanya jejak apa yang diketik operator.
      properties:
        id: { type: integer, format: int64 }
        tanggal_transaksi: { type: string, format: date-time }
        jenis_transaksi: { type: string, example: PEMBELIAN }
        stok_awal: { type: integer, format: int64 }
        stok_masuk: { type: integer, format: int64 }
        stok_keluar: { type: integer, format: int64 }
        stok_akhir: { type: integer, format: int64 }
        qty_input: { type: string, nullable: true, description: "Nol untuk baris pembalik." }
        nama_satuan_input: { type: string, nullable: true }
        harga_pokok_satuan: { type: string }
        nilai_masuk: { type: string }
        nilai_keluar: { type: string }
        nilai_akhir: { type: string }
        ref_table: { type: string, example: pembelian }
        ref_id_transaksi: { type: integer, format: int64 }
        nomor_dokumen:
          type: string
          nullable: true
          description: >
            `ref_table` + `ref_id_transaksi` diterjemahkan jadi nomor dokumen yang
            dibaca manusia.
        id_kartu_stok_asal:
          type: integer
          format: int64
          nullable: true
          description: >
            Terisi hanya pada baris pembalik — menunjuk baris asal yang dibalikkannya.
            Ini cara membedakan pembalik dari pergerakan biasa, tanpa menebak dari
            `jenis_transaksi`.
        keterangan: { type: string, nullable: true }

    StokMinimum:
      type: object
      description: >
        Satu produk aktif yang stoknya sudah di titik atau di bawah
        `stok_minimum`-nya sendiri — isu #22 fase 2.
      properties:
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string }
        nama_product: { type: string }
        stok_minimum: { type: integer, format: int64 }
        total_stok:
          type: integer
          format: int64
          description: Jumlah seluruh ruang dalam cakupan (unit aktif, atau satu ruang bila dipersempit).
        selisih:
          type: integer
          format: int64
          description: "`stok_minimum - total_stok`, selalu >= 0 di sini — dasar urutan daftar."
        per_ruang:
          type: array
          description: Rincian per ruang di balik `total_stok`.
          items: { $ref: "#/components/schemas/StokRuang" }

    NilaiPersediaan:
      type: object
      description: Nilai persediaan satu ruang saat ini — isu #22 fase 3.
      properties:
        id_ruang: { type: integer, format: int64 }
        nama_ruang: { type: string }
        total_nilai:
          type: string
          description: >
            Jumlah `nilai_akhir` baris terakhir setiap `(barang, ruang)` di ruang
            ini. Ruang yang dipensiunkan tetap muncul bila masih memegang nilai.

    LabaKotor:
      type: object
      description: Laba kotor satu bulan kalender — isu #22 fase 3.
      properties:
        bulan: { type: string, example: "2026-08" }
        total_penjualan: { type: string, description: "SUM(total) nota POSTED." }
        total_hpp: { type: string, description: "SUM(total_hpp) nota POSTED." }
        laba_kotor: { type: string, description: "total_penjualan - total_hpp." }

    Pergerakan:
      type: object
      description: >
        Rekap satu kelompok `(barang, ruang, jenis_transaksi)` dalam rentang tanggal
        — isu #22 fase 3.
      properties:
        id_product: { type: integer, format: int64 }
        kode_barang: { type: string }
        nama_product: { type: string }
        id_ruang: { type: integer, format: int64 }
        nama_ruang: { type: string }
        jenis_transaksi: { type: string, example: PEMBELIAN }
        total_masuk: { type: integer, format: int64 }
        total_keluar: { type: integer, format: int64 }

    PembayaranUtang:
      type: object
      description: >
        Uang yang keluar ke supplier. Setiap angka uang adalah string desimal, bukan
        angka JSON: `NUMERIC(20,2)` lewat float kehilangan sen, dan angka-angka ini yang
        direkonsiliasi dengan rekening bank.


        Statusnya `DRAFT -> POSTED -> BATAL`, **tanpa `DIAJUKAN`**. Tahap persetujuan di
        `pembelian` ada karena kartu stok append-only sehingga posting yang salah hanya
        bisa dibalik; alokasi pembayaran bisa dibatalkan dan seluruh cache dihitung ulang
        persis, jadi menirunya berarti meniru mekanisme tanpa alasannya.
      properties:
        id: { type: integer, format: int64 }
        nomor:
          type: string
          description: Digenerate server, seri `PU` yang terpisah dari `BL`, `PS`, dan `RB`.
          example: PU/KODE/2026/08/0001
        tanggal: { type: string, format: date-time }
        id_supplier: { type: integer, format: int64 }
        nama_supplier: { type: string }
        metode: { type: string, enum: [TUNAI, TRANSFER, GIRO] }
        no_referensi:
          type: string
          nullable: true
          description: Nomor transfer, nomor warkat giro, atau nomor kuitansi.
        nama_bank: { type: string, nullable: true }
        tanggal_jatuh_tempo_giro: { type: string, format: date, nullable: true }
        tanggal_cair:
          type: string
          format: date
          nullable: true
          description: Terisi tepat ketika `status_giro` bernilai `CAIR`, ditegakkan CHECK.
        status_giro:
          type: string
          nullable: true
          enum: [BELUM_CAIR, CAIR, TOLAK]
          description: >
            Null untuk metode selain `GIRO`, dan wajib ada untuk `GIRO`.


            **Menentukan apakah pembayaran ini mengurangi apa pun.** `BELUM_CAIR` dan
            `TOLAK` meninggalkan setiap utang tepat di tempatnya — menyerahkan giro tidak
            menyelesaikan apa pun, yang menyelesaikan adalah giro yang cair.
        jumlah:
          type: string
          description: Uang yang keluar. Harus lebih dari nol.
          example: "400000.00"
        jumlah_dialokasikan:
          type: string
          description: >
            Dihitung ulang dari baris alokasi, tidak pernah di-set dari form. **Boleh
            lebih kecil dari `jumlah`**, dan itu normal: sisanya mengendap sebagai kredit
            di supplier untuk faktur berikutnya.
          example: "400000.00"
        sisa_belum_dialokasikan:
          type: string
          description: >
            `jumlah − jumlah_dialokasikan`. Dilaporkan alih-alih diserahkan ke klien untuk
            dikurangkan sendiri, karena inilah angka yang menyatakan apakah pembayaran ini
            masih punya ruang.
          example: "0.00"
        status: { type: string, enum: [DRAFT, POSTED, BATAL] }
        keterangan: { type: string, nullable: true }
        alokasi:
          type: array
          description: Hanya pada endpoint detail. Kuncinya hilang pada list.
          items: { $ref: "#/components/schemas/PembayaranUtangAlokasi" }
        created_by: { type: integer, format: int64 }
        created_at: { type: string, format: date-time }
        posted_at: { type: string, format: date-time, nullable: true }
        dibatalkan_oleh: { type: integer, format: int64, nullable: true }
        alasan_batal: { type: string, nullable: true }

    PembayaranUtangAlokasi:
      type: object
      description: >
        Satu bagian pembayaran yang ditujukan ke satu faktur. Hubungan pembayaran dengan
        faktur banyak-ke-banyak, karena itulah alokasi jadi tabel tersendiri.
      properties:
        id: { type: integer, format: int64 }
        id_pembelian: { type: integer, format: int64 }
        nomor_pembelian: { type: string, example: BL/KODE/2026/08/0001 }
        no_faktur_supplier: { type: string, nullable: true }
        total_pembelian:
          type: string
          description: Keadaan faktur sekarang, supaya layar pembayaran bisa menyebut apa yang ditutupnya.
          example: "1050000.00"
        status_pembayaran: { type: string, enum: [BELUM, SEBAGIAN, LUNAS] }
        jumlah: { type: string, example: "400000.00" }
        created_at: { type: string, format: date-time }

    PembayaranUtangAlokasiInput:
      type: object
      required: [id_pembelian, jumlah]
      properties:
        id_pembelian:
          type: integer
          format: int64
          minimum: 1
          description: >
            Fakturnya harus `POSTED` dan milik supplier yang sama dengan pembayarannya.
            `DRAFT` cuma kertas yang sudah diketik dan `BATAL` adalah utang yang ditarik
            kembali; keduanya tidak bisa dibayar.


            Satu faktur hanya boleh muncul sekali per pembayaran.
        jumlah:
          type: string
          description: >
            String desimal, harus lebih dari nol. Tidak boleh melebihi sisa utang faktur
            itu — yang sudah memperhitungkan alokasi efektif lain dan kredit retur POSTED —
            dan seluruh alokasi bersama tidak boleh melebihi `jumlah` pembayarannya.
          example: "400000"

    UtangSupplier:
      type: object
      description: >
        Satu faktur yang masih terbuka: daftar kerja untuk menyusun pembayaran. Seperti
        riwayat harga beli, ini query dan bukan modul — tidak ada tabel dan tidak ada
        migrasi di belakangnya.
      properties:
        id_pembelian: { type: integer, format: int64 }
        nomor: { type: string, example: BL/KODE/2026/08/0001 }
        tanggal: { type: string, format: date-time }
        no_faktur_supplier: { type: string, nullable: true }
        tanggal_faktur: { type: string, format: date, nullable: true }
        jenis_pembayaran: { type: string, enum: [TUNAI, KREDIT] }
        status_pembayaran: { type: string, enum: [BELUM, SEBAGIAN, LUNAS] }
        total:
          type: string
          description: Yang ditagih supplier. Tidak memuat ongkir — itu tagihan ekspedisi.
          example: "1050000.00"
        jumlah_dialokasikan:
          type: string
          description: >
            Hanya alokasi **efektif**: dari pembayaran `POSTED`, dan untuk giro hanya yang
            sudah `CAIR`. Giro yang belum cair tidak dihitung di sini.
          example: "400000.00"
        nilai_kredit_retur:
          type: string
          description: Jumlah `nilai_kredit_utang` dari retur POSTED atas faktur ini.
          example: "0.00"
        sisa_utang:
          type: string
          description: "`total − jumlah_dialokasikan − nilai_kredit_retur`."
          example: "650000.00"

    PenerimaanPembayaran:
      type: object
      description: >
        Uang yang masuk dari pelanggan — cermin `PembayaranUtang`, isu #20. Setiap
        angka uang adalah string desimal, bukan angka JSON: `NUMERIC(20,2)` lewat float
        kehilangan sen, dan angka-angka ini yang direkonsiliasi dengan rekening bank.


        Statusnya `DRAFT -> POSTED -> BATAL`, **tanpa `DIAJUKAN`**, alasannya persis
        sama seperti `PembayaranUtang`: alokasi bisa dibatalkan dan seluruh cache
        dihitung ulang persis, jadi tidak ada residu penilaian yang perlu dilindungi
        tahap persetujuan.
      properties:
        id: { type: integer, format: int64 }
        nomor:
          type: string
          description: >
            Digenerate server, seri `PP` yang terpisah dari `BL`, `PS`, `RB`, `PU`,
            `MT`, `PM`, `PJ`, dan `SO`.
          example: PP/KODE/2026/08/0001
        tanggal: { type: string, format: date-time }
        id_pelanggan: { type: integer, format: int64 }
        nama_pelanggan: { type: string }
        metode: { type: string, enum: [TUNAI, TRANSFER, GIRO] }
        no_referensi:
          type: string
          nullable: true
          description: Nomor transfer, nomor warkat giro, atau nomor kuitansi.
        nama_bank: { type: string, nullable: true }
        tanggal_jatuh_tempo_giro: { type: string, format: date, nullable: true }
        tanggal_cair:
          type: string
          format: date
          nullable: true
          description: Terisi tepat ketika `status_giro` bernilai `CAIR`, ditegakkan CHECK.
        status_giro:
          type: string
          nullable: true
          enum: [BELUM_CAIR, CAIR, TOLAK]
          description: >
            Null untuk metode selain `GIRO`, dan wajib ada untuk `GIRO`.


            **Menentukan apakah pembayaran ini mengurangi apa pun.** `BELUM_CAIR` dan
            `TOLAK` meninggalkan setiap piutang tepat di tempatnya — menyerahkan giro
            tidak menyelesaikan apa pun, yang menyelesaikan adalah giro yang cair.
        jumlah:
          type: string
          description: Uang yang masuk. Harus lebih dari nol.
          example: "600000.00"
        jumlah_dialokasikan:
          type: string
          description: >
            Dihitung ulang dari baris alokasi, tidak pernah di-set dari form. **Boleh
            lebih kecil dari `jumlah`**, dan itu normal: sisanya mengendap sebagai kredit
            di pelanggan untuk nota berikutnya.
          example: "600000.00"
        sisa_belum_dialokasikan:
          type: string
          description: >
            `jumlah − jumlah_dialokasikan`. Dilaporkan alih-alih diserahkan ke klien untuk
            dikurangkan sendiri, karena inilah angka yang menyatakan apakah pembayaran ini
            masih punya ruang.
          example: "0.00"
        status: { type: string, enum: [DRAFT, POSTED, BATAL] }
        keterangan: { type: string, nullable: true }
        alokasi:
          type: array
          description: Hanya pada endpoint detail. Kuncinya hilang pada list.
          items: { $ref: "#/components/schemas/PembayaranAlokasi" }
        created_by: { type: integer, format: int64 }
        created_at: { type: string, format: date-time }
        posted_at: { type: string, format: date-time, nullable: true }
        dibatalkan_oleh: { type: integer, format: int64, nullable: true }
        alasan_batal: { type: string, nullable: true }

    PembayaranAlokasi:
      type: object
      description: >
        Satu bagian pembayaran yang ditujukan ke satu nota. Hubungan pembayaran dengan
        nota banyak-ke-banyak, karena itulah alokasi jadi tabel tersendiri.
      properties:
        id: { type: integer, format: int64 }
        id_penjualan: { type: integer, format: int64 }
        nomor_penjualan: { type: string, example: PJ/KODE/2026/08/0001 }
        total_penjualan:
          type: string
          description: Keadaan nota sekarang, supaya layar pembayaran bisa menyebut apa yang ditutupnya.
          example: "1500000.00"
        status_pembayaran: { type: string, enum: [BELUM, SEBAGIAN, LUNAS] }
        jumlah: { type: string, example: "600000.00" }
        created_at: { type: string, format: date-time }

    PembayaranAlokasiInput:
      type: object
      required: [id_penjualan, jumlah]
      properties:
        id_penjualan:
          type: integer
          format: int64
          minimum: 1
          description: >
            Notanya harus `POSTED`, `KREDIT`, dan milik pelanggan yang sama dengan
            pembayarannya. `DRAFT` cuma kertas yang sudah diketik, `BATAL` adalah
            piutang yang ditarik kembali, dan `TUNAI` tidak pernah jadi piutang sama
            sekali; ketiganya tidak bisa diterima pembayarannya di sini.


            Satu nota hanya boleh muncul sekali per pembayaran.
        jumlah:
          type: string
          description: >
            String desimal, harus lebih dari nol. Tidak boleh melebihi sisa piutang
            nota itu — yang sudah memperhitungkan alokasi efektif lain — dan seluruh
            alokasi bersama tidak boleh melebihi `jumlah` pembayarannya.
          example: "600000"

    Role:
      type: object
      properties:
        id: { type: integer, format: int64 }
        nama: { type: string, example: CASHIER }
        is_aktif: { type: boolean }
        created_at: { type: string, format: date-time }
        created_by: { type: integer, format: int64, nullable: true }
        updated_at: { type: string, format: date-time }
        updated_by: { type: integer, format: int64, nullable: true }

    User:
      type: object
      description: >
        Tidak punya field password, bahkan hash-nya. Satu-satunya jalan hash
        sampai ke klien adalah kalau seseorang menambahkan fieldnya di sini.
      properties:
        id: { type: integer, format: int64 }
        username: { type: string, example: kasir_pagi }
        email: { type: string, format: email, nullable: true }
        nama_lengkap: { type: string, nullable: true }
        is_aktif: { type: boolean }
        roles:
          type: array
          description: >
            Seluruh grant yang dipegang user; satu user boleh punya banyak. Selalu
            array, tidak pernah `null`, jadi `roles.length` aman dibaca di setiap
            baris. Diurutkan berdasarkan nama role lalu unit (grant global lebih
            dulu).


            Isu #12 fase 3: satu grant adalah (role, unit_kerja), bukan cuma role
            lagi, jadi role yang sama bisa muncul lebih dari sekali di sini — satu
            baris per unit tempat ia diberikan. "INVENTARIS di outlet A" dan
            "INVENTARIS di outlet B" adalah dua baris, bukan duplikat.


            Termasuk grant yang rolenya, atau unitnya, dipensiunkan setelah
            diberikan — pemberiannya masih nyata dan masih perlu dicabut. Bedakan
            lewat `is_aktif` di dalamnya.
          items: { $ref: "#/components/schemas/RoleRef" }
        created_at: { type: string, format: date-time }
        created_by: { type: integer, format: int64, nullable: true }
        updated_at: { type: string, format: date-time }
        updated_by: { type: integer, format: int64, nullable: true }

    RoleRef:
      type: object
      description: >
        Bentuk ringkas satu grant di dalam user. Kolom audit role tidak diulang di
        setiap baris daftar user.
      properties:
        id: { type: integer, format: int64 }
        nama: { type: string, example: CASHIER }
        is_aktif: { type: boolean }
        id_user_role:
          type: integer
          format: int64
          description: >
            Id baris user_role itu sendiri — identitas grant ini, bukan
            identitas rolenya. Isu #12 fase 4: ini yang dikirim sebagai
            id_user_role ke POST /auth/switch-context.
        id_unit_kerja:
          type: integer
          format: int64
          nullable: true
          description: >
            Unit tempat grant ini berlaku. `null` berarti berlaku di seluruh unit
            — bentuk grant SUPERADMIN.
        nama_unit_kerja:
          type: string
          nullable: true
          description: Diambil sekalian lewat JOIN, null kalau id_unit_kerja null.
        is_aktif_unit_kerja:
          type: boolean
          nullable: true
          description: >
            Versi is_aktif untuk unitnya. null persis ketika id_unit_kerja null
            — grant global tidak punya unit yang bisa dipensiunkan.

    GrantRequest:
      type: object
      required: [id_role]
      description: >
        Satu role untuk diberikan, dan di mana — isu #12 fase 3. `id_unit_kerja`
        yang tidak diisi (atau `null`) memberi role itu di seluruh unit, bentuk
        grant SUPERADMIN; diisi berarti grant itu hanya berlaku di unit tersebut,
        dan role yang sama boleh diberikan lagi untuk unit lain lewat entri
        `GrantRequest` kedua.

        Bentuk request, mencerminkan model.GrantRequest di Go — beda dari
        `Grant`, bentuk response yang mengisi `LoginResult.grants`/`Session.grants`
        dan membawa kolom tambahan (id_user_role, role, nama_unit_kerja) yang
        tidak pernah dikirim klien.
      properties:
        id_role: { type: integer, format: int64, minimum: 1 }
        id_unit_kerja:
          type: integer
          format: int64
          minimum: 1
          nullable: true

    Grants:
      type: array
      maxItems: 32
      items: { $ref: "#/components/schemas/GrantRequest" }
      description: >
        Grant yang dipegang user. Pasangan (id_role, id_unit_kerja) yang berulang
        dianggap satu — jelas berarti "beri grant itu sekali".


        id_role yang tidak ada atau rolenya sudah `is_aktif: false` ditolak 400,
        begitu juga id_unit_kerja yang tidak ada atau unitnya sudah
        `is_aktif: false` — bukan diabaikan diam-diam.
      example: [2, 3]

    Password:
      type: string
      format: password
      minLength: 8
      maxLength: 72
      description: >
        Hanya dikirim masuk, sekali. Di-hash dengan bcrypt di lapisan usecase dan
        tidak pernah disimpan, dicatat di log, atau dikembalikan.


        Batas 72 adalah batas masukan bcrypt. Validator menghitung rune sedangkan
        bcrypt menghitung byte, jadi password 72 karakter yang mengandung rune
        multibyte tetap ditolak 400, hanya pesannya yang berbeda.

    PageMetadata:
      type: object
      properties:
        page: { type: integer }
        size: { type: integer }
        total_item: { type: integer, format: int64 }
        total_page: { type: integer, format: int64 }

  responses:
    Error:
      description: Request gagal
      content:
        application/json:
          schema:
            type: object
            properties:
              data: { nullable: true, example: null }
              errors: { type: string, example: "supplier not found" }

    ValidationError:
      description: Payload tidak lolos validasi
      content:
        application/json:
          schema:
            type: object
            properties:
              data: { nullable: true, example: null }
              errors: { type: string, example: "validation failed" }
              validation_errors:
                type: object
                additionalProperties: { type: string }
                example: { "Nama": "required" }
